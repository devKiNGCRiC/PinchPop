---
phase: 01-app-shell-deployment-pipeline
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - src/components/NavBar.tsx
  - src/components/MobileNavSheet.tsx
  - src/components/AppShell.tsx
  - src/components/PlaceholderCard.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-04]

must_haves:
  truths:
    - "A persistent 64px-tall top navigation bar renders the PINCHPOP wordmark and links to Home, Play, Gallery, and Profile"
    - "Below 768px the horizontal link row is replaced by an accessible 44x44px hamburger button that opens a Sheet drawer containing the same links"
    - "The nav link matching the current route is marked with a 2px signal-colored bottom border"
    - "Every routed page renders inside a shared shell whose footer carries the PuzzleCam attribution exactly once"
    - "Placeholder pages render heading and body through one shared component rather than duplicated markup"
    - "Implements CONTEXT.md decisions D-04, D-09, D-13"
  artifacts:
    - path: "src/components/NavBar.tsx"
      provides: "Top navigation chrome with responsive desktop link row and mobile hamburger trigger"
      exports: ["NavBar"]
      min_lines: 30
    - path: "src/components/MobileNavSheet.tsx"
      provides: "Radix Sheet-backed mobile nav drawer with accessible trigger"
      exports: ["MobileNavSheet"]
    - path: "src/components/AppShell.tsx"
      provides: "Nested layout route rendering NavBar + Outlet + attribution footer"
      exports: ["AppShell"]
    - path: "src/components/PlaceholderCard.tsx"
      provides: "Shared paper-surface empty-state card consumed by every placeholder page"
      exports: ["PlaceholderCard"]
  key_links:
    - from: "src/components/AppShell.tsx"
      to: "react-router-dom Outlet"
      via: "Outlet rendered inside the main element"
      pattern: "<Outlet"
    - from: "src/components/AppShell.tsx"
      to: "src/components/NavBar.tsx"
      via: "NavBar import and render"
      pattern: "NavBar"
    - from: "src/components/NavBar.tsx"
      to: "src/components/MobileNavSheet.tsx"
      via: "MobileNavSheet rendered in the below-md branch"
      pattern: "MobileNavSheet"
    - from: "src/components/MobileNavSheet.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "shadcn Sheet primitive import"
      pattern: "components/ui/sheet"
    - from: "src/components/PlaceholderCard.tsx"
      to: "src/components/ui/card.tsx"
      via: "shadcn Card primitive import"
      pattern: "components/ui/card"
---

<objective>
Build the four hand-written shell components every route depends on: `NavBar`, `MobileNavSheet`,
`AppShell`, and `PlaceholderCard`.

Purpose: D-04 requires `AppShell` to be a nested layout route rather than per-page wrapping, so it
must exist as a standalone component before `src/router.tsx` can reference it (plan 01-05).
SHELL-04's responsiveness and 01-UI-SPEC.md's navigation contract live entirely in this layer.

Output: `src/components/{NavBar,MobileNavSheet,AppShell,PlaceholderCard}.tsx`, all typechecking and
lint-clean against the shadcn primitives generated in plan 01-02.

Nothing renders these yet — plan 01-05 wires them into the router. `npm run typecheck` still covers
them because `tsconfig.app.json` includes all of `src/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md
@.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md
@.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md
@.planning/phases/01-app-shell-deployment-pipeline/01-02-SUMMARY.md

**Dependency note:** the substantive dependency is plan 01-02 (this plan composes the shadcn
primitives it generated). Plan 01-03 is named in `depends_on` as a serialization dependency — its
`npm ci` step wipes and reinstalls `node_modules`, so it cannot overlap with this plan's builds.

**Locked decisions implemented by this plan:** D-04 (`AppShell` is a nested layout route — nav +
`<Outlet />` + footer in ONE place, never wrapped per page), D-09 (`src/components/` for shared
custom components, `src/components/ui/` reserved for shadcn output), D-13 (no new dependencies —
everything needed is already installed).

**Import rule (01-RESEARCH.md Pitfall 3, 01-PATTERNS.md "Import Convention"):** always import router
APIs from `react-router-dom`. Never from the bare `react-router` package or its `react-router/dom`
subpath — those belong to the separately-versioned v8 package and will fail to resolve or expose a
different API surface.

**Interfaces available from plan 01-02 (already in the repo — do not go exploring for them):**
- `@/lib/utils` exports `cn(...inputs: ClassValue[]): string` (clsx + tailwind-merge helper).
- `@/components/ui/button` exports `Button`, accepting `variant`, `size`, `asChild`, plus native button props.
- `@/components/ui/card` exports the shadcn new-york Card set (`Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`) — confirm exact names by reading the generated file.
- `@/components/ui/sheet` exports `Sheet`, `SheetTrigger`, `SheetContent`, `SheetHeader`, `SheetTitle`, `SheetDescription`, `SheetClose` — confirm exact names by reading the generated file.
- `@/components/ui/separator` exports `Separator`.
- `lucide-react` provides the `Menu` icon for the hamburger trigger.
- Tailwind theme tokens from `src/index.css`: `ink`, `signal`, `danger`, `paper`, `paper-warm`, `paper-border`, `ink-soft`, plus `font-sans` (Inter) and `font-mono` (IBM Plex Mono).

**Navigation link set (01-UI-SPEC.md Navigation Pattern + Route Inventory):** Home to `/`,
Play to `/game`, Gallery to `/gallery`, Profile to `/profile`. Exactly these four, in this order,
with these labels. `/results` and `/share/:slug` are reachable routes but deliberately NOT in the
nav — they are flow destinations, not top-level destinations.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NavBar and MobileNavSheet</name>
  <files>src/components/NavBar.tsx, src/components/MobileNavSheet.tsx</files>
  <read_first>
    - `src/components/ui/sheet.tsx` (generated in plan 01-02 — confirm exported component names and `SheetContent`'s `side` prop)
    - `src/components/ui/button.tsx` (generated — confirm `variant` / `size` / `asChild` prop names)
    - `src/lib/utils.ts` (the `cn` helper signature)
    - `src/index.css` (the Tailwind theme variable names actually emitted in plan 01-02, so utility classes resolve)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` — "Layout & Breakpoints" Navigation pattern paragraph, Typography table (Label role), Spacing Scale exceptions (44x44px hit target), Color section (signal reserved for wordmark, active nav indicator, focus ring, single primary CTA)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md` — `src/components/NavBar.tsx` and `src/components/MobileNavSheet.tsx` pattern assignments
  </read_first>
  <action>
    Create `src/components/MobileNavSheet.tsx` exporting a named `MobileNavSheet` component.

    Build it on the shadcn `Sheet` primitive from `@/components/ui/sheet`. 01-RESEARCH.md's
    Don't-Hand-Roll table explicitly forbids a hand-rolled drawer with manual `useState` and manual
    `aria-*` wiring, because Radix already solves focus trapping, scroll lock, and ARIA roles.

    The trigger is an icon-only button using the `Menu` icon from `lucide-react`, wrapped in
    `SheetTrigger`. It MUST carry the attribute `aria-label="Open navigation menu"` (01-UI-SPEC.md
    checker sign-off item for icon-only controls) and MUST have a minimum 44x44px hit target —
    Tailwind `h-11 w-11`, the Spacing Scale accessibility exception. Use the shadcn `Button` with
    `variant="ghost"` and `size="icon"` so it inherits the signal-colored focus-visible ring instead
    of reinventing focus styling.

    `SheetContent` uses `side="right"` per 01-UI-SPEC.md ("opens a right-side Sheet drawer"), on an
    ink background. Inside, render the four nav links stacked vertically in the fixed order Home to
    `/`, Play to `/game`, Gallery to `/gallery`, Profile to `/profile`, using `NavLink` imported from
    `react-router-dom`. Each link uses the Label type role: `font-mono`, `uppercase`, `text-[13px]`,
    `tracking-[0.08em]`, `leading-[1.4]`, weight 400. Wrap each link in `SheetClose` with `asChild`
    so tapping a link dismisses the drawer — a drawer that stays open after navigation is the classic
    mobile-nav defect.

    `SheetContent` requires an accessible name; include a `SheetTitle` reading `Navigation`. If the
    visual spec calls for no visible heading, apply the `sr-only` class to it rather than omitting it
    — omitting it triggers a Radix Dialog accessibility warning at runtime.

    Create `src/components/NavBar.tsx` exporting a named `NavBar` component: a `header` element,
    full-width, `h-16` (64px), ink background, contents in a horizontally-padded flex row that is
    centered and capped at `max-w-[1200px]` per 01-UI-SPEC.md's `lg` breakpoint behavior.

    Left side: the wordmark `PINCHPOP` as a `NavLink` to `/`, in the Label type role (`font-mono`,
    `uppercase`, `tracking-[0.08em]`), colored with the signal token.

    Right side at `md` (768px) and above: a horizontal row of the same four `NavLink`s in the same
    order, Label type role, in the warm paper text color. The link matching the current route gets a
    2px signal-colored bottom border — use `NavLink`'s render-prop `className` with its `isActive`
    argument, applying `border-b-2 border-signal` when active and `border-b-2 border-transparent`
    otherwise so the row does not shift height on activation. The Home link must use the `end` prop so
    `/` is not reported active on every nested route.

    Right side below `md`: render `MobileNavSheet` instead of the link row. Do the swap with Tailwind
    responsive utilities (`hidden md:flex` on the desktop link row, `md:hidden` on the sheet trigger
    container), never a JavaScript width listener — a resize listener re-renders on every frame and
    creates a layout the CSS approach avoids entirely.

    Do not add a theme toggle, search field, auth control, or any nav item beyond the four specified.
    01-UI-SPEC.md's Route Inventory defines the complete Phase 1 nav surface.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build && grep -q 'react-router-dom' src/components/NavBar.tsx && grep -q 'PINCHPOP' src/components/NavBar.tsx && grep -q 'MobileNavSheet' src/components/NavBar.tsx && grep -q 'h-16' src/components/NavBar.tsx && grep -q 'md:hidden' src/components/NavBar.tsx && grep -q 'aria-label="Open navigation menu"' src/components/MobileNavSheet.tsx && grep -q 'components/ui/sheet' src/components/MobileNavSheet.tsx && grep -q 'h-11' src/components/MobileNavSheet.tsx && echo NAV_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, and `npm run build` all exit 0.
    - `src/components/NavBar.tsx` exports `NavBar`; `src/components/MobileNavSheet.tsx` exports `MobileNavSheet`.
    - Both files import router APIs only from `react-router-dom`: `grep -c 'from "react-router"' src/components/NavBar.tsx src/components/MobileNavSheet.tsx` reports 0 matches, and `grep -c 'react-router/dom' src/components/NavBar.tsx src/components/MobileNavSheet.tsx` reports 0 matches.
    - `grep -c 'aria-label="Open navigation menu"' src/components/MobileNavSheet.tsx` returns 1.
    - `grep -c 'useState' src/components/MobileNavSheet.tsx` returns 0 — Radix owns the open/closed state, per the Don't-Hand-Roll directive.
    - `MobileNavSheet.tsx` contains `SheetTitle` (accessible name present) and `SheetClose` (links dismiss the drawer), and `h-11` (44px hit target).
    - `NavBar.tsx` contains all four route strings `"/"`, `/game`, `/gallery`, `/profile`, and contains neither `/results` nor `/share`.
    - `NavBar.tsx` contains `h-16`, `PINCHPOP`, a `md:flex` desktop row, and a `md:hidden` mobile branch, and contains no `addEventListener` or `resize` handler.
    - `NavBar.tsx` applies `border-b-2` with the signal color conditioned on `NavLink`'s `isActive` argument.
  </acceptance_criteria>
  <done>Responsive navigation chrome exists: a desktop link row with an active-route signal underline, and an accessible Radix-backed mobile drawer below 768px.</done>
</task>

<task type="auto">
  <name>Task 2: Build AppShell and PlaceholderCard</name>
  <files>src/components/AppShell.tsx, src/components/PlaceholderCard.tsx</files>
  <read_first>
    - `src/components/NavBar.tsx` (created in Task 1 — confirm the exact export name AppShell imports)
    - `src/components/ui/card.tsx` (generated in plan 01-02 — confirm exported subcomponent names before composing them)
    - `src/components/ui/separator.tsx` (generated — confirm the `Separator` export)
    - `src/index.css` (theme variable names backing the paper/ink/signal utility classes)
    - `legacy/index.html` (the `.copyright` footer line — the attribution lineage being carried forward)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` — "Layout & Breakpoints" Footer and Page shell paragraphs, Route & Placeholder Page Inventory, Typography table
    - `.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md` — the `AppShell.tsx` pattern snippet, the "Attribution Footer" shared pattern (exact copy string), and the `PlaceholderCard` usage shape
    - `.planning/PROJECT.md` — the non-negotiable PuzzleCam attribution constraint
  </read_first>
  <action>
    Create `src/components/AppShell.tsx` exporting a named `AppShell` component, following
    01-PATTERNS.md's `AppShell.tsx` pattern snippet.

    Structure: a root `div` with `flex min-h-screen flex-col` on the ink background; `NavBar`; a
    `main` element with `flex-1` containing `Outlet` imported from `react-router-dom`; and a `footer`.
    Per D-04 this is the ONLY place nav and footer appear — no page component may render its own nav
    or footer, and no page may wrap itself in `AppShell`.

    The footer is a single centered line in the Label type role (`font-mono`, `uppercase`, `text-xs`,
    `tracking-wide`) at roughly 30% opacity on ink, per 01-UI-SPEC.md's Footer paragraph. Its text
    must be exactly the string fixed by 01-PATTERNS.md's "Attribution Footer" shared pattern:
    PinchPop, an em dash, then "built on PuzzleCam by Unnati-23". This satisfies PROJECT.md's
    non-negotiable attribution constraint — do not paraphrase it, do not shorten it, do not move it
    into a page component, and do not render it more than once anywhere in the app. Place a
    `Separator` from `@/components/ui/separator` above the footer text as the divider called for in
    01-UI-SPEC.md's Component Inventory.

    Add one brief comment above the footer explaining WHY the attribution is there (a PROJECT.md
    constraint), not what the JSX does — CLAUDE.md's convention is sparse, purposeful, why-not-what
    comments, and no JSDoc.

    Create `src/components/PlaceholderCard.tsx` exporting a named `PlaceholderCard` component with a
    TypeScript props interface taking exactly two required string props: `heading` and `body`. This is
    the one-line usage shape 01-PATTERNS.md specifies, so each stub page reduces to a single element
    with those two props.

    Render it as a vertically-centered block on the ink background containing a shadcn `Card` styled
    as the paper-white "photo tray" surface from 01-UI-SPEC.md's Secondary color role: paper-white
    background, `paper-border` border, ink foreground text. Inside, render `heading` in the Heading
    type role (Inter, weight 600, 28px desktop dropping to 22px below `md` — express this with
    responsive Tailwind sizing, not a media-query listener) and `body` in the Body type role (Inter,
    weight 400, 16px, line-height 1.5) in the `ink-soft` muted color. Constrain the card width (for
    example `max-w-md`), center it horizontally, and use generous vertical section padding from the
    Spacing Scale.

    Do NOT hardcode any specific page's copy inside `PlaceholderCard` — every string arrives through
    props so the copy lives in the page components where 01-UI-SPEC.md's Route Inventory table maps
    it. Do not add a CTA, icon, or illustration slot; the placeholder spec is heading plus body only,
    and `/game` is explicitly a dead-end placeholder in this phase.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build && grep -q 'Outlet' src/components/AppShell.tsx && grep -q 'NavBar' src/components/AppShell.tsx && grep -q 'react-router-dom' src/components/AppShell.tsx && test "$(grep -c 'built on PuzzleCam by Unnati-23' src/components/AppShell.tsx)" = "1" && grep -q 'heading' src/components/PlaceholderCard.tsx && grep -q 'components/ui/card' src/components/PlaceholderCard.tsx && echo SHELL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, and `npm run build` all exit 0.
    - `src/components/AppShell.tsx` exports `AppShell`, renders `NavBar`, and renders `Outlet` imported from `react-router-dom`.
    - `grep -rc 'built on PuzzleCam by Unnati-23' src/` reports exactly one matching line, in `src/components/AppShell.tsx` only — the attribution appears once, in the shared shell.
    - The attribution line reads exactly "PinchPop — built on PuzzleCam by Unnati-23" (em dash, name spelled `Unnati-23`), matching 01-PATTERNS.md's Attribution Footer pattern verbatim.
    - `AppShell.tsx` imports and renders `Separator` from `@/components/ui/separator`.
    - `grep -c 'react-router/dom' src/components/AppShell.tsx` returns 0 and `grep -c 'from "react-router"' src/components/AppShell.tsx` returns 0.
    - `src/components/PlaceholderCard.tsx` exports `PlaceholderCard` with a props type declaring required `heading: string` and `body: string`, and imports `Card` from `@/components/ui/card`.
    - `grep -c 'coming soon' src/components/PlaceholderCard.tsx` returns 0 — no page-specific copy is hardcoded in the shared component.
    - `grep -rc 'AppShell' src/pages/` reports 0 matches or the directory does not exist yet — no page wraps itself in the shell (D-04).
  </acceptance_criteria>
  <done>A single nested-layout `AppShell` provides nav, outlet, and a once-only PuzzleCam attribution footer, and `PlaceholderCard` provides the shared paper-surface stub used by every placeholder route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → React component tree | Rendered chrome and placeholder surfaces; no user-supplied data enters this layer in Phase 1 |

*(No auth, no forms, no user input, no persistence, no network calls exist in these components — ASVS V2/V3/V4/V5/V6 remain not applicable per 01-RESEARCH.md "Security Domain".)*

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | Cross-site scripting through unescaped content in `PlaceholderCard` | mitigate | `heading` and `body` are rendered as ordinary JSX children, which React escapes by default. The task explicitly forbids `dangerouslySetInnerHTML`; acceptance criteria for plan 01-05 assert it is absent from `src/`. All Phase 1 copy is compile-time constant from 01-UI-SPEC.md — no runtime or user-supplied strings reach this component. |
| T-01-12 | Denial of Service (accessibility/usability) | Hand-rolled mobile drawer trapping focus incorrectly or failing to close | mitigate | `MobileNavSheet` is built on the Radix-backed shadcn `Sheet`; acceptance criteria assert no local `useState` drawer state, a present `SheetTitle`, and `SheetClose`-wrapped links. |
| T-01-13 | Repudiation | Loss of the required PuzzleCam attribution (PROJECT.md non-negotiable constraint) | mitigate | The exact attribution string is asserted present exactly once in `src/components/AppShell.tsx` by this plan's automated verify, and re-asserted at the phase level in plan 01-05's verification. |
| T-01-SC | Tampering | Package-manager installs (ASVS V10) | mitigate | This plan installs nothing — it consumes only the already-audited dependency tree (`lucide-react`, Radix via shadcn, `clsx`/`tailwind-merge`), all `[OK]` in 01-RESEARCH.md's Package Legitimacy Audit. Any package added here must first be re-audited with `slopcheck install --ecosystem npm` and escalated to a blocking human checkpoint if `[ASSUMED]` or `[SUS]`. |
</threat_model>

<verification>
1. `npm run lint` — exits 0
2. `npm run typecheck` — exits 0
3. `npm run format:check` — exits 0
4. `npm run build` — exits 0
5. `grep -rc 'built on PuzzleCam by Unnati-23' src/` — exactly one match, in `src/components/AppShell.tsx`
6. `grep -rc 'react-router/dom' src/` and `grep -rc 'from "react-router"' src/` — both 0 (Pitfall 3)
</verification>

<success_criteria>
- `NavBar` renders a 64px ink bar with the signal-colored PINCHPOP wordmark and exactly four nav links.
- Active-route indication is a 2px signal bottom border driven by `NavLink`'s `isActive` (SHELL-02 discoverability).
- Below 768px the link row is replaced by a 44x44px hamburger with `aria-label="Open navigation menu"` opening a right-side Radix Sheet whose links close the drawer (SHELL-04).
- `AppShell` is the single nested layout: `NavBar` + `main`/`Outlet` + attribution footer, rendered once (D-04).
- The PuzzleCam attribution string appears exactly once in the codebase, verbatim (PROJECT.md constraint).
- `PlaceholderCard` takes `heading` and `body` props with no hardcoded page copy.
- All router imports come from `react-router-dom` only (Pitfall 3).
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-deployment-pipeline/01-04-SUMMARY.md` when done.
</output>
