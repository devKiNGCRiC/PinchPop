---
phase: 01-app-shell-deployment-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - legacy/index.html
  - legacy/app.js
  - legacy/guide.html
  - legacy/css/styles.css
  - index.html
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - eslint.config.js
  - .prettierrc.json
  - .prettierignore
  - .nvmrc
  - .gitignore
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
  - src/vite-env.d.ts
  - src/assets/react.svg
  - public/vite.svg
autonomous: true
requirements: [SHELL-01, SHELL-04]

must_haves:
  truths:
    - "The legacy vanilla-JS prototype is preserved verbatim under legacy/ and no longer sits at the repo root"
    - "A Vite + React + TypeScript project builds successfully from the repo root"
    - "Node 22 is pinned in exactly one source of truth (.nvmrc) and mirrored by package.json engines"
    - "npm run lint, npm run typecheck, and npm run build all exit 0"
    - "The root README.md retains its original PuzzleCam attribution content (not overwritten by the Vite scaffold)"
  artifacts:
    - path: "package.json"
      provides: "npm manifest with dev/build/lint/typecheck/preview/format scripts, engines.node, Phase 1 dependencies"
      contains: "typecheck"
    - path: ".nvmrc"
      provides: "Single source of truth for the Node version (22)"
    - path: "eslint.config.js"
      provides: "Flat ESLint config with react-hooks/react-refresh rules and eslint-config-prettier applied last"
      contains: "prettier"
    - path: ".prettierrc.json"
      provides: "Prettier formatting config consumed by npm run format"
    - path: "legacy/app.js"
      provides: "Preserved legacy prototype logic for the Phase 2 port"
      min_lines: 1200
    - path: "src/main.tsx"
      provides: "React entry point created by the Vite react-ts scaffold"
  key_links:
    - from: "package.json"
      to: "tsconfig.app.json"
      via: "typecheck script invoking tsc --noEmit -p"
      pattern: "tsc --noEmit -p tsconfig.app.json"
    - from: "package.json engines.node"
      to: ".nvmrc"
      via: "same Node major (22) declared in both, no third hardcoded copy"
      pattern: "\"node\":"
---

<objective>
Migrate the legacy vanilla-JS prototype into `legacy/` and scaffold a Vite + React + TypeScript
application at the repo root, with Node version pinning, npm scripts, and the ESLint + Prettier
toolchain that the Phase 1 CI gate will run.

Purpose: Every other plan in this phase builds on this scaffold. Until `npm run lint`,
`npm run typecheck`, and `npm run build` all pass at the repo root, no CI workflow, no Tailwind
wiring, and no Vercel deploy can be validated.

Output: `legacy/` holding the untouched prototype; a working Vite react-ts project at root;
`.nvmrc`, `package.json` (scripts + engines + Phase 1 deps), `eslint.config.js`,
`.prettierrc.json`, `.prettierignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md
@.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md

**Locked decisions implemented by this plan:** D-09 (folder layout), D-10 (TS strict mode kept),
D-11 (ESLint + Prettier now), D-12 (Vite app at root, legacy prototype moved to `legacy/`),
D-13 (install ONLY Phase 1 dependencies — no `zustand`, no `@mediapipe/tasks-vision`, no
`@supabase/supabase-js`), D-14 (npm), D-15 (`@fontsource` packages), D-16 (Node 22 pinned).

**Package legitimacy status:** all 25 packages in this phase's dependency set were audited with
slopcheck 0.6.1 under `--ecosystem npm` and returned `[OK]` (see 01-RESEARCH.md
"Package Legitimacy Audit"). No `[ASSUMED]` or `[SUS]` packages remain, so no blocking human
legitimacy checkpoint is required for these installs. Do NOT run bare `slopcheck install <pkg>` in
this repo without `--ecosystem npm` — 01-RESEARCH.md documents that it falls back to PyPI and
misidentifies npm packages as `[SLOP]`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the legacy prototype into legacy/</name>
  <files>legacy/index.html, legacy/app.js, legacy/guide.html, legacy/css/styles.css</files>
  <read_first>
    - `index.html` (repo root — legacy prototype entry, about to be displaced by Vite's generated index.html)
    - `app.js` (repo root — 1283-line prototype, moves verbatim)
    - `css/styles.css` (repo root — token source referenced by Phase 2's game-screen port)
    - `guide.html` (repo root)
    - `README.md` (repo root — must survive untouched; carries the PuzzleCam attribution)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md` (D-12: move, do not delete, do not refactor)
  </read_first>
  <action>
    Per D-12, relocate the four legacy prototype artifacts into a new top-level `legacy/` directory
    using `git mv` so rename history is preserved. Create `legacy/` first (`mkdir -p legacy`), then:
    `index.html` to `legacy/index.html`, `app.js` to `legacy/app.js`, `guide.html` to
    `legacy/guide.html`, and the whole `css/` directory to `legacy/css/` (yielding
    `legacy/css/styles.css`).

    Move byte-for-byte. Do NOT refactor, reformat, lint, modernize, or clean up any of them —
    01-RESEARCH.md's "Project Constraints" section states legacy code stays untouched in this phase.
    Do NOT rewrite the relative `./css/styles.css` and `./app.js` references inside
    `legacy/index.html`; because `css/` moves as a unit alongside it, those relative paths stay
    correct within `legacy/`.

    Do NOT move or modify `README.md`, `CLAUDE.md`, `.planning/`, `.claude/`, or `graphify-out/`.
    After the move the repo root must contain no `index.html`, no `app.js`, no `guide.html`, and no
    `css/` directory — Vite's scaffold in Task 2 generates its own root `index.html` and must not
    collide with the legacy one.
  </action>
  <verify>
    <automated>test -f legacy/app.js && test -f legacy/index.html && test -f legacy/guide.html && test -f legacy/css/styles.css && test ! -e app.js && test ! -e index.html && test ! -e guide.html && test ! -d css && echo LEGACY_MOVE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `legacy/app.js`, `legacy/index.html`, `legacy/guide.html`, and `legacy/css/styles.css` all exist.
    - Repo root no longer contains `app.js`, `index.html`, `guide.html`, or a `css/` directory.
    - `wc -l < legacy/app.js` returns at least 1200 (verbatim move, not a rewrite).
    - `grep -c 'PINCH_THRESHOLD' legacy/app.js` returns at least 1 and `grep -c '\-\-signal' legacy/css/styles.css` returns at least 1 (original constants and design tokens intact).
    - `git status --porcelain` reports these paths as renames (status letter `R`), not delete+add.
    - `git status --porcelain README.md` produces no output (README untouched).
  </acceptance_criteria>
  <done>The legacy prototype lives entirely under `legacy/` with unchanged content, and the repo root is clear for Vite's scaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the Vite react-ts project at the repo root and pin Node 22</name>
  <files>index.html, package.json, package-lock.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, eslint.config.js, .gitignore, .nvmrc, src/main.tsx, src/App.tsx, src/App.css, src/index.css, src/vite-env.d.ts, src/assets/react.svg, public/vite.svg</files>
  <read_first>
    - `.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md` — "Standard Stack" (package versions verified 2026-09-09), the "Installation" block, "Recommended Project Structure", the `package.json` `typecheck` script snippet under Code Examples, and Assumption A2 (`.nvmrc` holds a bare `22`)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md` — D-10, D-12, D-14, D-16
    - `README.md` (repo root — confirm it must NOT be replaced by the scaffold's generated README)
  </read_first>
  <action>
    Scaffold the Vite React+TypeScript template at the repo root, per D-12 and D-14.

    Because the repo root is not empty (`.git`, `.planning`, `.claude`, `CLAUDE.md`, `README.md`,
    `legacy/`, `graphify-out/`), do NOT run `npm create vite@latest .` directly — the CLI prompts
    interactively on a non-empty directory. Instead scaffold into a throwaway subdirectory and move
    the output up: run `npm create vite@latest .vite-scaffold -- --template react-ts` from the repo
    root, move every generated entry from `.vite-scaffold/` to the repo root, then delete
    `.vite-scaffold/`.

    Entries to move up: `index.html`, `package.json`, `vite.config.ts`, `tsconfig.json`,
    `tsconfig.app.json`, `tsconfig.node.json`, `eslint.config.js`, `.gitignore`, `public/`, and
    `src/` (containing `main.tsx`, `App.tsx`, `App.css`, `index.css`, `vite-env.d.ts`, `assets/`).
    Explicitly do NOT move the scaffold's generated `README.md` — the existing root `README.md`
    carries the PuzzleCam attribution required by PROJECT.md and must survive intact. Move any other
    template-emitted file too, except `README.md`.

    Set `package.json` `name` to `pinchpop`.

    Per D-16, create `.nvmrc` at the repo root containing exactly the single line `22` (bare major,
    per 01-RESEARCH.md Assumption A2), and add an `engines` field to `package.json` with
    `"node": ">=22.12.0"` — that floor is Vite 8's own engines requirement documented in
    01-RESEARCH.md's Standard Stack table. These two are the single source of truth later referenced
    by GitHub Actions via `node-version-file: ".nvmrc"` and honored by Vercel; do not hardcode a
    Node version anywhere else.

    Per D-10, leave `tsconfig.app.json` strictness untouched — `strict`, `noUnusedLocals`, and
    `noUnusedParameters` stay exactly as the template sets them. Do not relax them to make anything
    compile.

    Add to `package.json` `scripts`, keeping the template's existing `dev`, `build`, `lint`, and
    `preview` entries: `typecheck` with the command exactly `tsc --noEmit -p tsconfig.app.json`
    (this is the command the Phase 1 CI workflow invokes and it is not scaffolded by default),
    `format` running `prettier --write .`, and `format:check` running `prettier --check .`
    (Prettier itself is installed in Task 3).

    Confirm the scaffold's `.gitignore` ignores `node_modules` and `dist`; additionally append
    `graphify-out/` to `.gitignore`, since that directory is currently an untracked local artifact
    directory at the repo root.

    Run `npm install` after the move so `package-lock.json` is generated at the root.
  </action>
  <verify>
    <automated>test -f package.json && test -f .nvmrc && test -f src/main.tsx && test ! -d .vite-scaffold && node -e "const p=require('./package.json'); if(p.scripts.typecheck!=='tsc --noEmit -p tsconfig.app.json') throw new Error('typecheck script: '+p.scripts.typecheck); if(!p.engines||!p.engines.node) throw new Error('engines.node missing'); if(p.name!=='pinchpop') throw new Error('name: '+p.name); console.log('PKG_OK')" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces a `dist/index.html`.
    - `node -p "require('./package.json').scripts.typecheck"` prints exactly `tsc --noEmit -p tsconfig.app.json`.
    - `node -p "require('./package.json').scripts['format:check']"` prints `prettier --check .`.
    - `node -p "require('./package.json').engines.node"` prints a string containing `22`.
    - `cat .nvmrc` prints exactly `22` (single line, no `v` prefix).
    - `node -p "JSON.parse(require('fs').readFileSync('tsconfig.app.json','utf8').replace(/^\\s*\\/\\/.*$/gm,'')).compilerOptions.strict"` prints `true`.
    - `.vite-scaffold/` no longer exists.
    - `git diff --stat README.md` produces no output — the scaffold's README did not overwrite the project README.
    - `grep -c 'graphify-out' .gitignore` returns 1.
  </acceptance_criteria>
  <done>A Vite react-ts project builds from the repo root, Node 22 is pinned in `.nvmrc` + `engines`, and `typecheck`/`format` scripts exist.</done>
</task>

<task type="auto">
  <name>Task 3: Install Phase 1 runtime dependencies and configure ESLint + Prettier</name>
  <files>package.json, package-lock.json, eslint.config.js, .prettierrc.json, .prettierignore</files>
  <read_first>
    - `package.json` (root — current scripts/deps after Task 2)
    - `eslint.config.js` (root — the Vite-scaffolded flat config that must be extended, not replaced)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md` — "Standard Stack" Supporting table (exact package names and versions), "Installation" block, and Pitfall 3 (`react-router-dom` vs `react-router`)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md` — D-11, D-13, D-15
  </read_first>
  <action>
    Install only the Phase 1 dependency set defined by D-13. Runtime dependencies:
    `react-router-dom` (must be this package, NOT bare `react-router` — 01-RESEARCH.md Pitfall 3
    documents that bare `react-router` moved to v8 with a different import surface, while
    `react-router-dom` stays on the v7 Data-mode API that D-01 locks), `@fontsource/inter`, and
    `@fontsource/ibm-plex-mono` (D-15 self-hosted fonts).

    Dev dependencies: `prettier` and `eslint-config-prettier`.

    Do NOT install `zustand`, `@mediapipe/tasks-vision`, or `@supabase/supabase-js` — D-13 defers
    those to Phase 2 / Phase 3+ and explicitly supersedes 01-UI-SPEC.md's Implementation Notes
    install list on this point. Do NOT install `tailwindcss`, `@tailwindcss/vite`, or `@types/node`
    here either; those belong to plan 01-02 where they are actually wired.

    Extend the existing scaffolded `eslint.config.js` per D-11 rather than replacing it: keep the
    template's `typescript-eslint`, `eslint-plugin-react-hooks`, and `eslint-plugin-react-refresh`
    configuration, and append `eslint-config-prettier` as the LAST entry of the exported config array
    so it can disable stylistic rules that conflict with Prettier. Add `dist`, `node_modules`,
    `legacy`, and `src/components/ui` to ESLint's ignore list — `legacy/` is untouched prototype code
    per D-12 and `src/components/ui/` will hold verbatim shadcn-generated files that must not be
    linted against this project's rules.

    Create `.prettierrc.json` with an explicit, non-default-relying config: `semi` true,
    `singleQuote` false, `trailingComma` "all", `printWidth` 100, `tabWidth` 2. Create
    `.prettierignore` listing `node_modules`, `dist`, `legacy`, `package-lock.json`, and
    `src/components/ui`.

    Run `npm run format` once so the scaffolded source is Prettier-clean, then confirm
    `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all pass.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; ['react-router-dom','@fontsource/inter','@fontsource/ibm-plex-mono','prettier','eslint-config-prettier'].forEach(k=>{if(!d[k])throw new Error('missing '+k)}); ['zustand','@mediapipe/tasks-vision','@supabase/supabase-js','react-router'].forEach(k=>{if(d[k])throw new Error('D-13 violation: '+k+' must not be installed')}); console.log('DEPS_OK')" && npm run lint && npm run typecheck && npm run format:check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npm run typecheck` exits 0.
    - `npm run format:check` exits 0.
    - `npm run build` exits 0.
    - `node -p "Object.keys({...require('./package.json').dependencies}).join(',')"` includes `react-router-dom`, `@fontsource/inter`, `@fontsource/ibm-plex-mono`.
    - The same command's output does NOT include `zustand`, `@mediapipe/tasks-vision`, `@supabase/supabase-js`, or a bare `react-router` entry (D-13).
    - `grep -v '^\s*//' eslint.config.js | grep -c 'prettier'` returns at least 1, and the prettier config entry appears after the react-hooks/react-refresh entries in the exported array.
    - `node -p "require('./.prettierrc.json').printWidth"` prints `100`.
    - `grep -v '^#' .prettierignore | grep -c 'legacy'` returns 1.
  </acceptance_criteria>
  <done>All four npm quality gates (`lint`, `typecheck`, `format:check`, `build`) pass, and only D-13's approved dependencies are installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party code enters the build at install time; the only untrusted-input boundary that exists in this plan |
| Local working tree → git remote | Committed config controls the future CI and deploy behavior |

*(No client→API, no user input, no data persistence, and no auth exist in this phase — ASVS V2/V3/V4/V5/V6 are not applicable, per 01-RESEARCH.md "Security Domain".)*

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` of `react-router-dom`, `@fontsource/*`, `prettier`, `eslint-config-prettier` (ASVS V10) | mitigate | All packages appear in 01-RESEARCH.md's Package Legitimacy Audit with a slopcheck `[OK]` verdict under `--ecosystem npm`. Task 3's verify command asserts the exact approved names are present and that no unapproved package (`zustand`, `@mediapipe/tasks-vision`, `@supabase/supabase-js`, bare `react-router`) was installed. Any package NOT in that audit table must be re-audited with `slopcheck install --ecosystem npm <pkg>` and, if `[ASSUMED]`/`[SUS]`, escalated to a blocking human checkpoint before install. |
| T-01-02 | Tampering | Typosquat risk on `react-router-dom` vs bare `react-router` | mitigate | Task 3 pins `react-router-dom` explicitly and the automated verify rejects a bare `react-router` dependency; 01-RESEARCH.md Pitfall 3 documents why the two are not interchangeable. |
| T-01-03 | Information Disclosure | Legacy prototype files moved into `legacy/` | accept | The legacy code is already public in this repo's history, contains no secrets (01-RESEARCH.md: "No secrets are used or required by the current implementation"), and is moved verbatim without introducing new exposure. |
| T-01-04 | Tampering | `.gitignore` failing to exclude `node_modules`/`dist`, allowing build artifacts into git | mitigate | Task 2 verifies the scaffolded `.gitignore` covers `node_modules` and `dist` and appends `graphify-out/`. |
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `npm run lint` — exits 0
2. `npm run typecheck` — exits 0
3. `npm run format:check` — exits 0
4. `npm run build` — exits 0, emits `dist/`
5. `test -f legacy/app.js && test -f legacy/css/styles.css && test ! -e app.js` — legacy migration intact
6. `git status --porcelain README.md` — empty (attribution README untouched)
</verification>

<success_criteria>
- Repo root hosts a buildable Vite + React + TypeScript project (D-12).
- `legacy/` contains the four prototype files verbatim; repo root has no `app.js`/`css/`/`guide.html` and its `index.html` is Vite's.
- `.nvmrc` reads `22`; `package.json` `engines.node` is `>=22.12.0`; no third hardcoded Node version exists (D-16).
- `package.json` scripts include `typecheck` = `tsc --noEmit -p tsconfig.app.json`, plus `format` and `format:check` (D-05/D-06 CI dependency).
- ESLint flat config retains react-hooks/react-refresh rules and applies `eslint-config-prettier` last; `.prettierrc.json` and `.prettierignore` exist (D-11).
- Installed dependencies are exactly D-13's approved set — no zustand, mediapipe, or supabase-js.
- TypeScript strict mode is unchanged from the template (D-10).
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-deployment-pipeline/01-01-SUMMARY.md` when done.
</output>

