# Phase 1: App Shell & Deployment Pipeline - Pattern Map

**Mapped:** 2026-09-09
**Files analyzed:** 27
**Analogs found:** 0 exact code-reuse analogs / 27 (genuine greenfield scaffold) — 3 "brand/token source" analogs found in the legacy prototype (color/type tokens, attribution copy, structural inspiration only, not copyable code)

## Important Scope Note

Confirmed via `ls` on the repo root: no `package.json`, `src/`, `vite.config.ts`, or any prior React/TS code exists anywhere in this repository. The only pre-existing code is the legacy vanilla-JS prototype at repo root (`app.js`, `index.html`, `css/styles.css`, `guide.html`), which per CONTEXT.md D-12 is being **moved verbatim into `legacy/`** in this phase, not ported or referenced as implementation code. It uses DOM manipulation with zero components, zero JSX, zero build tooling — there is no structural analog for React components, hooks, routers, or config files anywhere in this codebase.

**Consequence for this document:** for every new file below, "Closest Analog" is either:
1. **RESEARCH.md Code Examples** (Context7/official-doc-sourced, exact syntax already verified for this stack) — the primary pattern source for all new tooling/config/router files, or
2. **Legacy `css/styles.css` design tokens** — for color/typography/copy values only (not code structure) that must carry forward into the new Tailwind theme and footer attribution text, or
3. **No analog** — genuinely novel file with no precedent in either the repo or research, flagged explicitly.

The planner should treat RESEARCH.md's Code Examples section as the primary "analog" source for this phase, since no in-repo code analog exists for any React/TS/config file.

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `package.json` | config | N/A (build config) | RESEARCH.md Installation section + scripts block | no in-repo analog |
| `.nvmrc` | config | N/A | RESEARCH.md D-16 / Recommended Project Structure | no in-repo analog |
| `vite.config.ts` | config | N/A (build config) | RESEARCH.md Code Examples: `vite.config.ts` | no in-repo analog |
| `tsconfig.json` / `tsconfig.app.json` / `tsconfig.node.json` | config | N/A | RESEARCH.md Code Examples: `tsconfig.json` | no in-repo analog |
| `eslint.config.js` | config | N/A | Vite `react-ts` scaffold default + D-11 (Prettier/hooks extension) | no in-repo analog |
| `vercel.json` | config | request-response (rewrite rule) | RESEARCH.md Code Examples: `vercel.json` | no in-repo analog |
| `.github/workflows/ci.yml` | config (CI pipeline) | event-driven (push/PR trigger) | RESEARCH.md Code Examples: `ci.yml` | no in-repo analog |
| `index.html` (Vite root entry) | config/template | N/A | Legacy `index.html` (structure only — DOCTYPE/head/meta), else Vite scaffold default | partial (markup shell only, not app content) |
| `src/main.tsx` | provider/entry | request-response (app bootstrap) | RESEARCH.md `@fontsource` imports example + React Router v7 docs (Context7) | no in-repo analog |
| `src/router.tsx` | route | request-response | RESEARCH.md Code Examples: Pattern 1 nested layout route | no in-repo analog |
| `src/index.css` | config (styles) | N/A | Legacy `css/styles.css` (token *values* only: colors, fonts) + RESEARCH.md `@import "tailwindcss"` example | partial (tokens only) |
| `src/components/AppShell.tsx` | component (layout) | request-response | RESEARCH.md Code Examples: `AppShell.tsx` snippet; legacy `.hud-top`/`.copyright` for token/copy carry-over | no in-repo structural analog |
| `src/components/NavBar.tsx` | component | request-response | Legacy `.hud-top` brand/status-pill markup (`index.html:17-23`, `css/styles.css:57-88`) — visual/token inspiration only | no in-repo structural analog |
| `src/components/MobileNavSheet.tsx` | component | event-driven (open/close) | shadcn `Sheet` primitive (to be generated by `npx shadcn add sheet`, not yet in repo) | no analog — will exist only after shadcn init runs |
| `src/components/PlaceholderCard.tsx` | component | request-response | UI-SPEC.md Route & Placeholder Page Inventory table (copy contract) | no analog |
| `src/components/ui/*.tsx` (button, card, sheet, separator, skeleton, badge) | component | request-response | Generated verbatim by `npx shadcn@latest add ...` — not hand-written, no analog needed | generated, not authored |
| `src/lib/utils.ts` (`cn()` helper) | utility | transform | shadcn's standard `cn()` boilerplate (generated by `shadcn init`) | generated, not authored |
| `src/pages/HomePage.tsx` | component (route page) | request-response | UI-SPEC.md `/` route spec; no in-repo analog | no analog |
| `src/pages/GamePage.tsx` | component (route page) | request-response | UI-SPEC.md `/game` route spec (uses `PlaceholderCard`) | no analog |
| `src/pages/ResultsPage.tsx` | component (route page) | request-response | UI-SPEC.md `/results` route spec (uses `PlaceholderCard`) | no analog |
| `src/pages/GalleryPage.tsx` | component (route page) | request-response | UI-SPEC.md `/gallery` route spec (uses `PlaceholderCard`) | no analog |
| `src/pages/ProfilePage.tsx` | component (route page) | request-response | UI-SPEC.md `/profile` route spec (uses `PlaceholderCard`) | no analog |
| `src/pages/SharePage.tsx` | component (route page) | request-response | UI-SPEC.md `/share/:slug` route spec (dynamic segment, uses `PlaceholderCard`) | no analog |
| `src/pages/NotFoundPage.tsx` | component (route page) | request-response | UI-SPEC.md Copywriting Contract "Error state" row | no analog |
| `legacy/index.html`, `legacy/app.js`, `legacy/css/styles.css`, `legacy/guide.html` | N/A (file move, not authored) | N/A | Verbatim move of existing root files — no transformation, no pattern needed | direct move, unchanged |

## Pattern Assignments

### `vite.config.ts` (config)

**Analog:** RESEARCH.md Code Examples (sourced from shadcn-ui/ui Context7 docs) — no in-repo analog exists.

**Full pattern to copy verbatim:**
```typescript
// Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/installation/vite.mdx (Context7)
import path from "path"
import tailwindcss from "@tailwindcss/vite"
import react from "@vitejs/plugin-react"
import { defineConfig } from "vite"

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
})
```
**Order dependency (Common Pitfall 1 in RESEARCH.md):** this file must be updated with the Tailwind plugin and alias BEFORE running `npx shadcn@latest init`, or shadcn's CLI will fail to detect Tailwind v4 correctly.

---

### `tsconfig.json` / `tsconfig.app.json` (config)

**Analog:** RESEARCH.md Code Examples — no in-repo analog.

```json
// Source: https://github.com/shadcn-ui/ui/blob/main/apps/v4/content/docs/installation/vite.mdx (Context7)
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```
The same `baseUrl`/`paths` block must also be added to `tsconfig.app.json` (per RESEARCH.md note directly below this example).

---

### `vercel.json` (config, request-response rewrite)

**Analog:** RESEARCH.md Code Examples (sourced from official Vercel docs) — no in-repo analog; this is the exact and only correct syntax per D-02/D-03.

```json
// Source: https://vercel.com/docs/frameworks/frontend/vite (WebFetch, official Vercel docs, 2026-08-26)
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```
No `/api/*` exception per D-03 — copy exactly, do not add exclusions.

---

### `.github/workflows/ci.yml` (config, event-driven CI pipeline)

**Analog:** RESEARCH.md Code Examples — no in-repo CI/workflow analog exists (repo has never had CI).

```yaml
# Sources: actions/checkout@v7, actions/setup-node@v6 (node-version-file input) — RESEARCH.md, verified 2026-09-09
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: "npm"
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
```
Companion `package.json` script required (not scaffolded by default `create-vite react-ts`):
```json
{ "scripts": { "typecheck": "tsc --noEmit -p tsconfig.app.json" } }
```
**Sequencing note (Pitfall 4):** push this workflow and let it run once on a throwaway PR BEFORE running the D-08 branch-protection `gh api` call, so the exact check-context string is known.

---

### `src/router.tsx` + `src/components/AppShell.tsx` (route / component, request-response)

**Analog:** RESEARCH.md Code Examples Pattern 1 (Context7-sourced from `remix-run/react-router` docs) — no in-repo router or layout-component analog exists (legacy app has no routing at all).

```tsx
// src/router.tsx
// Source: https://github.com/remix-run/react-router/blob/main/docs/start/data/routing.md (Context7)
import { createBrowserRouter, RouterProvider } from "react-router-dom";
import { AppShell } from "./components/AppShell";
import HomePage from "./pages/HomePage";
import GamePage from "./pages/GamePage";
import ResultsPage from "./pages/ResultsPage";
import GalleryPage from "./pages/GalleryPage";
import ProfilePage from "./pages/ProfilePage";
import SharePage from "./pages/SharePage";
import NotFoundPage from "./pages/NotFoundPage";

const router = createBrowserRouter([
  {
    element: <AppShell />,
    children: [
      { index: true, element: <HomePage /> },
      { path: "game", element: <GamePage /> },
      { path: "results", element: <ResultsPage /> },
      { path: "gallery", element: <GalleryPage /> },
      { path: "profile", element: <ProfilePage /> },
      { path: "share/:slug", element: <SharePage /> },
      { path: "*", element: <NotFoundPage /> },
    ],
  },
]);

export function AppRouter() {
  return <RouterProvider router={router} />;
}
```

```tsx
// src/components/AppShell.tsx
// Source: https://github.com/remix-run/react-router/blob/main/docs/start/framework/routing.md (Context7, Outlet pattern)
import { Outlet } from "react-router-dom";
import { NavBar } from "./NavBar";

export function AppShell() {
  return (
    <div className="flex min-h-screen flex-col bg-[#0a0a08]">
      <NavBar />
      <main className="flex-1">
        <Outlet />
      </main>
      <footer className="py-4 text-center opacity-30">
        {/* PuzzleCam attribution — PROJECT.md non-negotiable constraint */}
        <p className="font-mono text-xs uppercase tracking-wide">
          PinchPop — built on PuzzleCam by Unnati-23
        </p>
      </footer>
    </div>
  );
}
```
**IMPORTANT — import pitfall:** always import from `"react-router-dom"`, never `"react-router"` or `"react-router/dom"` (RESEARCH.md Pitfall 3 — the bare `react-router` package moved to v8 with a different API surface; D-13 locks `react-router-dom` which stays on v7).

**Anti-pattern to avoid (explicit in RESEARCH.md):** do not wrap every page component in `<AppShell>` manually — use the nested-route pattern above exclusively (D-04).

---

### `src/components/NavBar.tsx` (component, request-response)

**Analog:** No structural/code analog exists — legacy `index.html`/`css/styles.css` provide only the **visual token language** to carry forward (brand wordmark placement, mono/uppercase/letter-spaced label style, signal-accent color), not any reusable markup or logic (legacy nav is a HUD status bar over a video canvas, not a route nav).

**Legacy visual reference** (`index.html:17-23`, tokens only — not copyable JSX):
```html
<div class="hud-top">
  <div class="brand">PUZZLE·CAM <small>by aiwithunnati</small></div>
  <div class="status-pill">
    <span id="statusDot" class="status-dot"></span>
    <span id="statusText">starting camera…</span>
  </div>
</div>
```
**Legacy token values to carry into Tailwind theme** (`css/styles.css:1-16`, values only):
```css
:root{
  --ink:#0a0a08;
  --paper:#eae5d6;
  --signal:#f5c518;
  --danger:#e0533d;
  --mono: 'IBM Plex Mono', 'SFMono-Regular', Menlo, Consolas, monospace;
  --sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}
```
These map directly to UI-SPEC.md's Color table (`#0a0a08` ink, `#f5c518` signal, `#e0533d` danger) — confirm the new Tailwind `@theme` CSS variables in `src/index.css` reuse these exact hex values rather than re-deriving them, per UI-SPEC.md's explicit intent to carry the prototype's brand identity forward.

**Build NavBar as new code** per UI-SPEC.md's Navigation Pattern section (persistent top bar, `h-16`, wordmark left, horizontal links ≥768px, hamburger + `Sheet` below 768px, active-route 2px signal underline) — no code analog, only the token/copy values above apply.

---

### `src/components/MobileNavSheet.tsx` (component, event-driven)

**Analog:** shadcn's `Sheet` component (Radix Dialog primitive) — generated by `npx shadcn@latest add sheet`, does not exist in the repo yet at analysis time. No legacy analog (prototype has no drawer/modal-nav pattern; its only overlay is the strip-completion modal, which is a different concern).

**Don't-hand-roll directive (RESEARCH.md):** use shadcn `Sheet` as the foundation — do not hand-roll `useState` + manual `aria-*` open/close logic; Radix already solves focus-trap/scroll-lock/ARIA roles.

**Accessibility requirement from UI-SPEC.md** (must be honored in the trigger button):
```
aria-label="Open navigation menu"
```
44×44px minimum hit target on the hamburger trigger (UI-SPEC.md Spacing Scale exceptions).

---

### `src/components/PlaceholderCard.tsx` + all `src/pages/*.tsx` (component, request-response)

**Analog:** No code analog exists anywhere (legacy app has no page/route concept). Pattern source is UI-SPEC.md's Route & Placeholder Page Inventory table and Copywriting Contract, which fully specifies the copy each page must render.

**Shape to implement** (from UI-SPEC.md, one-line usage per page):
```tsx
<PlaceholderCard heading="Game screen coming soon" body="The gesture capture and puzzle engine lands in the next phase." />
```
Copy values per route (exact strings, do not paraphrase — UI-SPEC.md Copywriting Contract is locked):
| Route | Heading | Body |
|---|---|---|
| `/game` | "Game screen coming soon" | "The gesture capture and puzzle engine lands in the next phase." |
| `/results` | "Results coming soon" | "Your photo and score will appear here once the capture flow is built." |
| `/gallery` | "Gallery coming soon" | "Your saved photos will live here once accounts and storage are built." |
| `/profile` | "Profile coming soon" | "Your stats and achievements will appear here in a later phase." |
| `/share/:slug` | "Shared memory coming soon" | "Public share pages launch in a later phase." |

`/` (HomePage) is NOT a `PlaceholderCard` usage — it renders the full hero per UI-SPEC.md's Route table: `Display` headline "Capture. Solve. Remember.", one `Body` paragraph, and a signal-filled primary CTA button "Start Playing" linking to `/game` (use React Router's `<Link to="/game">`, not a raw `<a>`).

`NotFoundPage` copy (UI-SPEC.md Copywriting Contract, Error state row):
- Heading: "Page not found."
- Body: "We couldn't find that page. Head back and keep exploring PinchPop."
- CTA: "Back to Home" → `<Link to="/">`

---

### `src/main.tsx` (entry, request-response bootstrap)

**Analog:** RESEARCH.md Code Examples (`@fontsource` imports) — no in-repo entry-point analog (legacy `index.html` loads `app.js` as a plain module script with no bundler-driven bootstrap).

```typescript
// src/main.tsx — import specific weights only (UI-SPEC.md declares exactly 2 weights: 400, 600)
import "@fontsource/inter/400.css";
import "@fontsource/inter/600.css";
import "@fontsource/ibm-plex-mono/400.css";
// + createRoot(...).render(<AppRouter />) per Vite react-ts scaffold default, with AppRouter from src/router.tsx
```
Note: UI-SPEC.md's Typography table specifies weight 600 for Heading/Display (Inter) and only weight 400 for Label (IBM Plex Mono) — do not import `ibm-plex-mono/600.css` or `inter/500.css`/`700.css` (UI-SPEC.md: "do not introduce 500/700 anywhere in this phase").

---

### `src/index.css` (config/styles)

**Analog:** RESEARCH.md Code Examples (Tailwind v4 import syntax) + legacy `css/styles.css` (token values only, not the CSS rules themselves).

```css
/* Source: https://github.com/tailwindlabs/tailwindcss.com upgrade-guide.mdx (Context7) */
@import "tailwindcss";
```
Followed by a `@theme` block (Tailwind v4 CSS-first config) defining the UI-SPEC.md Color table as theme variables — reusing the exact legacy hex values confirmed above (`#0a0a08`, `#f5c518`, `#e0533d`, `#eae5d6`/paper family) rather than shadcn's default neutral palette, per UI-SPEC.md Implementation Notes: "extend the generated Tailwind theme's CSS variables with the token values declared in the Color section above... rather than shadcn's default neutral palette."

**Explicit exclusion:** do NOT `@import` or otherwise reference the legacy `css/styles.css` file itself from the new app — UI-SPEC.md Implementation Notes states it "stays scoped to the Phase 2 game-screen port." Only the token *values* are carried forward, re-expressed as new Tailwind theme variables.

---

## Shared Patterns

### Router/Layout Nesting (D-04)
**Source:** RESEARCH.md Pattern 1 (Code Examples section above)
**Apply to:** `src/router.tsx`, `src/components/AppShell.tsx`, and implicitly every page in `src/pages/` (they render only as `<Outlet />` children, never wrap themselves in nav/footer JSX).

### Design Token Carry-Over from Legacy Prototype
**Source:** `css/styles.css:1-16` (`:root` custom properties) + UI-SPEC.md Color/Typography tables
**Apply to:** `src/index.css` (`@theme` block), and indirectly every component using Tailwind color classes (`bg-[#0a0a08]`, text/accent colors) — ensures the new shell doesn't look like "an unrelated generic SaaS UI bolted onto a photobooth game" (UI-SPEC.md's explicit design goal).
**Values:** ink `#0a0a08`, paper `#ffffff`/`#eae5d6` family, signal `#f5c518`, danger `#e0533d`, mono font stack (IBM Plex Mono), sans font stack (Inter).

### Attribution Footer (non-negotiable, PROJECT.md constraint)
**Source:** legacy `index.html:68` (`<div class="copyright">© 2026 aiwithunnati — all rights reserved</div>`) + RESEARCH.md's `AppShell.tsx` example footer JSX
**Apply to:** `src/components/AppShell.tsx` footer only (single shared location, not per-page)
**Exact new copy (from RESEARCH.md example, matches UI-SPEC.md footer section):**
```
PinchPop — built on PuzzleCam by Unnati-23
```

### Import Convention: `react-router-dom` only
**Source:** RESEARCH.md Pitfall 3
**Apply to:** `src/router.tsx`, `src/components/AppShell.tsx`, `src/components/NavBar.tsx`, every file under `src/pages/` that uses `<Link>` — always `import { ... } from "react-router-dom"`, never `"react-router"` or `"react-router/dom"`.

### Config-Before-Init Ordering (shadcn)
**Source:** RESEARCH.md Common Pitfall 1
**Apply to:** `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `src/index.css` must ALL be updated for Tailwind v4 BEFORE running `npx shadcn@latest init`, which in turn must run BEFORE `src/components/ui/*.tsx` are generated.

### Node Version Single Source of Truth
**Source:** RESEARCH.md Don't Hand-Roll table, row 4
**Apply to:** `.nvmrc` (source of truth) → read by `.github/workflows/ci.yml` via `node-version-file: ".nvmrc"` → read by `package.json` `engines` field → respected by Vercel's build environment automatically. Never hardcode `22` separately in more than one of these places.

## No Analog Found

Files with no close match in either the repo or research beyond spec-driven requirements (planner should rely on UI-SPEC.md's exact tables, already excerpted above, rather than any code precedent):

| File | Role | Data Flow | Reason |
|---|---|---|---|
| `src/components/PlaceholderCard.tsx` | component | request-response | Genuinely new shared component; no reusable stub/empty-state component exists anywhere in this repo's history |
| `src/pages/HomePage.tsx` | component | request-response | First-ever "hero landing page" in this codebase; legacy prototype has no marketing/landing surface, only the camera stage |
| `src/components/ui/*.tsx` | component | request-response | Will be auto-generated by `npx shadcn@latest add`, not hand-authored — no pattern extraction needed, just correct CLI invocation order (see Shared Patterns) |
| `src/lib/utils.ts` | utility | transform | Auto-generated by `shadcn init` (`cn()` helper) — standard boilerplate, not authored from a codebase pattern |

## Metadata

**Analog search scope:** Entire repository root (`ls -la`), `css/styles.css` (grep for tokens/classes), `index.html` (grep for attribution/nav markup) — confirmed no `package.json`, `src/`, or any `.tsx`/`.ts` files exist anywhere in the repo.
**Files scanned:** 6 (repo root listing, `css/styles.css`, `index.html`, `01-CONTEXT.md`, `01-RESEARCH.md`, `01-UI-SPEC.md`)
**Pattern extraction date:** 2026-09-09
