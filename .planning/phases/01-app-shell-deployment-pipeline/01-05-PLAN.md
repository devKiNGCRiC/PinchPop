---
phase: 01-app-shell-deployment-pipeline
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - src/pages/HomePage.tsx
  - src/pages/GamePage.tsx
  - src/pages/ResultsPage.tsx
  - src/pages/GalleryPage.tsx
  - src/pages/ProfilePage.tsx
  - src/pages/SharePage.tsx
  - src/pages/NotFoundPage.tsx
  - src/router.tsx
  - src/main.tsx
  - src/App.tsx
  - src/assets/react.svg
  - index.html
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03, DEPLOY-02]

must_haves:
  truths:
    - "Visiting / shows a landing page introducing PinchPop with a single primary CTA"
    - "Clicking the landing CTA navigates to the game screen"
    - "A results page exists at /results and renders its placeholder content"
    - "A gallery page exists at /gallery and a share page exists at /share/:slug, both rendering placeholder content"
    - "An unmatched URL renders the 404 page with a link back home, inside the same shell"
    - "All seven pages render inside AppShell via a nested layout route, with nav and footer defined only once"
  artifacts:
    - path: "src/router.tsx"
      provides: "createBrowserRouter route tree with AppShell as the parent layout route"
      exports: ["AppRouter"]
      contains: "createBrowserRouter"
    - path: "src/pages/HomePage.tsx"
      provides: "Landing hero with headline, premise paragraph, and Start Playing CTA (SHELL-01, SHELL-02)"
      exports: ["default"]
    - path: "src/pages/ResultsPage.tsx"
      provides: "Results placeholder (SHELL-03)"
      exports: ["default"]
    - path: "src/pages/SharePage.tsx"
      provides: "Dynamic-segment share placeholder proving /share/:slug routing (DEPLOY-02)"
      exports: ["default"]
    - path: "src/pages/NotFoundPage.tsx"
      provides: "404 page rendered by the catch-all route with a Back to Home link"
      exports: ["default"]
    - path: "src/main.tsx"
      provides: "React entry mounting AppRouter with the fontsource and index.css imports"
      contains: "AppRouter"
  key_links:
    - from: "src/main.tsx"
      to: "src/router.tsx"
      via: "AppRouter import and render into the root element"
      pattern: "AppRouter"
    - from: "src/router.tsx"
      to: "src/components/AppShell.tsx"
      via: "AppShell used as the parent route element wrapping all children"
      pattern: "AppShell"
    - from: "src/pages/HomePage.tsx"
      to: "/game"
      via: "react-router-dom Link on the Start Playing CTA"
      pattern: "to=\"/game\""
    - from: "src/router.tsx"
      to: "src/pages/NotFoundPage.tsx"
      via: "catch-all splat route"
      pattern: "path: \"\\*\""
---

<objective>
Create the seven route-level page components, wire them into a `createBrowserRouter` route tree with
`AppShell` as the parent layout route, mount that router from `src/main.tsx`, and verify the running
app against SHELL-01 through SHELL-04.

Purpose: This is the plan that makes the phase's requirements observable. Until the router exists,
`AppShell`, `NavBar`, and `PlaceholderCard` are unreachable code. D-01 (Data mode
`createBrowserRouter`) and D-04 (nested layout route) are both realized here.

Output: `src/pages/*.tsx` (7 files), `src/router.tsx`, an updated `src/main.tsx`, removal of the
Vite demo `App.tsx`, and a human-verified running app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md
@.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md
@.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md
@.planning/phases/01-app-shell-deployment-pipeline/01-VALIDATION.md
@.planning/phases/01-app-shell-deployment-pipeline/01-04-SUMMARY.md

**Locked decisions implemented by this plan:** D-01 (React Router v7 Data mode via
`createBrowserRouter`, NOT `BrowserRouter` + `Routes`/`Route` Declarative mode), D-03 (unmatched
paths fall through to React Router's own `*` route, which renders the 404 page), D-04 (nested layout
route — pages never wrap themselves in `AppShell`), D-09 (`src/pages/` for route-level components),
D-13 (no new dependencies).

**Import rule (01-RESEARCH.md Pitfall 3):** import router APIs only from `react-router-dom` — never
from the bare `react-router` package or its `react-router/dom` subpath.

**Interfaces available from plan 01-04 (do not go exploring for them):**
- `@/components/AppShell` exports `AppShell` — renders `NavBar`, a `main` with `Outlet`, and the attribution footer.
- `@/components/PlaceholderCard` exports `PlaceholderCard`, props `{ heading: string; body: string }`.
- `@/components/ui/button` exports `Button` (`variant`, `size`, `asChild`).
- `@/components/ui/card` exports the shadcn Card set.
- Theme tokens: `ink`, `signal`, `danger`, `paper`, `paper-warm`, `paper-border`, `ink-soft`; `font-sans` (Inter), `font-mono` (IBM Plex Mono).

**Locked copy — 01-UI-SPEC.md Route Inventory and Copywriting Contract are a contract, not a
suggestion. Use these exact strings; do not paraphrase, retitle, or "improve" them:**

| Route | Heading | Body |
|---|---|---|
| `/game` | Game screen coming soon | The gesture capture and puzzle engine lands in the next phase. |
| `/results` | Results coming soon | Your photo and score will appear here once the capture flow is built. |
| `/gallery` | Gallery coming soon | Your saved photos will live here once accounts and storage are built. |
| `/profile` | Profile coming soon | Your stats and achievements will appear here in a later phase. |
| `/share/:slug` | Shared memory coming soon | Public share pages launch in a later phase. |
| `*` (404) | Page not found. | We couldn't find that page. Head back and keep exploring PinchPop. |

Landing page (`/`): Display headline `Capture. Solve. Remember.`, one Body paragraph explaining the
gesture-photobooth premise, and a single signal-filled primary CTA labeled `Start Playing` linking to
`/game`. The 404 page's CTA is labeled `Back to Home` and links to `/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the seven route-level page components</name>
  <files>src/pages/HomePage.tsx, src/pages/GamePage.tsx, src/pages/ResultsPage.tsx, src/pages/GalleryPage.tsx, src/pages/ProfilePage.tsx, src/pages/SharePage.tsx, src/pages/NotFoundPage.tsx</files>
  <read_first>
    - `src/components/PlaceholderCard.tsx` (created in plan 01-04 — confirm the exact prop names and whether the export is named or default)
    - `src/components/ui/button.tsx` (confirm `variant`, `size`, and `asChild` prop names before composing the CTA)
    - `src/components/ui/card.tsx` (needed by `NotFoundPage`, which has a CTA and therefore cannot reuse `PlaceholderCard`)
    - `src/index.css` (confirm the emitted theme utility class names for signal/danger/paper/ink-soft)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` — Route & Placeholder Page Inventory, Copywriting Contract, Typography table (Display/Heading/Body roles and their mobile step-downs), Color section (signal reserved for the single primary CTA per screen; danger reserved for the 404 accent)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md` — the `PlaceholderCard` + pages section with the exact per-route copy table
  </read_first>
  <action>
    Create `src/pages/` and add seven components, each a default export named after its file
    (`HomePage`, `GamePage`, `ResultsPage`, `GalleryPage`, `ProfilePage`, `SharePage`,
    `NotFoundPage`).

    Five of them are one-line consumers of `PlaceholderCard`, passing the exact heading and body
    strings from the locked copy table in this plan's context section: `GamePage`, `ResultsPage`,
    `GalleryPage`, `ProfilePage`, `SharePage`. Do not add extra prose, icons, CTAs, or "back" links to
    these five — `/game` is deliberately a dead-end placeholder in this phase because SHELL-02 only
    requires navigation *to* the game screen, not gameplay.

    `SharePage` additionally calls `useParams` from `react-router-dom` to read the `:slug` segment.
    The copy does not display it, but the value must be genuinely consumed or TypeScript's
    `noUnusedLocals` (kept strict per D-10) will fail the build. Surface it as a `data-share-slug`
    attribute on the page's outermost element — this both satisfies the compiler and gives plan
    01-07's DEPLOY-02 verification a concrete DOM hook proving the dynamic segment resolved after a
    hard refresh.

    `HomePage` is NOT a `PlaceholderCard` usage. Build it as a full-bleed hero on the ink background:
    the headline `Capture. Solve. Remember.` in the Display type role (Inter, weight 600, 48px,
    line-height 1.1, stepping down to 32px below the `md` breakpoint via responsive Tailwind sizing);
    one Body-role paragraph (Inter, 400, 16px, line-height 1.5, warm paper color) explaining the
    gesture-photobooth premise — frame a photo with your hands, pinch to capture, solve the puzzle,
    keep the polaroid; and one primary CTA. The CTA is a shadcn `Button` with `asChild` wrapping a
    `Link` from `react-router-dom` pointing to `/game`, labeled exactly `Start Playing`, filled with
    the signal token and ink-colored text. Exactly one signal-filled CTA on this screen — 01-UI-SPEC.md
    reserves the accent for a single primary CTA per screen, so no secondary signal-colored button.

    `NotFoundPage` also cannot use `PlaceholderCard` (that component has no CTA slot). Compose it
    from the shadcn `Card` set directly, mirroring `PlaceholderCard`'s paper-surface styling for
    visual consistency: heading `Page not found.` in the Heading role, body
    `We couldn't find that page. Head back and keep exploring PinchPop.` in the Body role, and a CTA
    labeled `Back to Home` wrapping a `Link` to `/`. Use the danger token (`#e0533d`) as this page's
    accent — 01-UI-SPEC.md reserves danger for error/404 accents and signal for primary CTAs, so the
    404's accent treatment is danger while its CTA remains a neutral/secondary button variant rather
    than a second signal-filled CTA.

    Use `Link` (or `NavLink`) from `react-router-dom` for every internal navigation — never a raw
    anchor element, which would trigger a full page reload and defeat client-side routing.

    No page renders `NavBar`, a footer, or wraps itself in `AppShell` — D-04 puts all of that in the
    layout route. No page may contain the PuzzleCam attribution string; it belongs in `AppShell`
    exactly once.

    Do not use `dangerouslySetInnerHTML` anywhere. All copy is compile-time constant JSX text.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build && for f in HomePage GamePage ResultsPage GalleryPage ProfilePage SharePage NotFoundPage; do test -f "src/pages/$f.tsx" || { echo "MISSING $f"; exit 1; }; done && grep -q 'Capture. Solve. Remember.' src/pages/HomePage.tsx && grep -q 'Start Playing' src/pages/HomePage.tsx && grep -q 'to="/game"' src/pages/HomePage.tsx && grep -q 'Game screen coming soon' src/pages/GamePage.tsx && grep -q 'Results coming soon' src/pages/ResultsPage.tsx && grep -q 'Gallery coming soon' src/pages/GalleryPage.tsx && grep -q 'Profile coming soon' src/pages/ProfilePage.tsx && grep -q 'Shared memory coming soon' src/pages/SharePage.tsx && grep -q 'data-share-slug' src/pages/SharePage.tsx && grep -q 'Page not found.' src/pages/NotFoundPage.tsx && grep -q 'Back to Home' src/pages/NotFoundPage.tsx && ! grep -rq 'dangerouslySetInnerHTML' src/ && ! grep -rq 'AppShell' src/pages/ && echo PAGES_OK</automated>
  </verify>
  <acceptance_criteria>
    - All seven files exist under `src/pages/` and each has a default export.
    - `npm run lint`, `npm run typecheck`, and `npm run build` all exit 0.
    - Every locked copy string appears verbatim in its assigned file: `Game screen coming soon`, `The gesture capture and puzzle engine lands in the next phase.`, `Results coming soon`, `Your photo and score will appear here once the capture flow is built.`, `Gallery coming soon`, `Your saved photos will live here once accounts and storage are built.`, `Profile coming soon`, `Your stats and achievements will appear here in a later phase.`, `Shared memory coming soon`, `Public share pages launch in a later phase.`, `Page not found.`, `We couldn't find that page. Head back and keep exploring PinchPop.`
    - `src/pages/HomePage.tsx` contains `Capture. Solve. Remember.`, the label `Start Playing`, and a `Link` with `to="/game"`.
    - `src/pages/NotFoundPage.tsx` contains `Back to Home` and a `Link` with `to="/"`.
    - `src/pages/SharePage.tsx` imports `useParams` from `react-router-dom` and emits a `data-share-slug` attribute.
    - `grep -rc 'AppShell' src/pages/` reports 0 matches (D-04 — no page wraps itself in the shell).
    - `grep -rc 'built on PuzzleCam' src/pages/` reports 0 matches (attribution lives only in `AppShell`).
    - `grep -rc 'dangerouslySetInnerHTML' src/` reports 0 matches.
    - `grep -rc '<a href="/' src/pages/` reports 0 matches — internal navigation uses `Link`/`NavLink`, not raw anchors.
  </acceptance_criteria>
  <done>All seven route-level pages exist with 01-UI-SPEC.md's locked copy, the landing hero has its single signal CTA to `/game`, and no page duplicates shell chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the createBrowserRouter route tree and mount it from main.tsx</name>
  <files>src/router.tsx, src/main.tsx, src/App.tsx, src/assets/react.svg, index.html</files>
  <read_first>
    - `src/main.tsx` (current entry — already carries the three `@fontsource` imports and the `./index.css` import added in plan 01-02; those must survive this rewrite)
    - `src/App.tsx` (the Vite demo component being removed)
    - `src/components/AppShell.tsx` (confirm whether the export is named or default before importing it)
    - `index.html` (root — carries `class="dark"` from plan 01-02 and the `#root` mount element)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md` — Architecture Patterns "Pattern 1: Nested Layout Route with createBrowserRouter" (the exact route-object shape), the Anti-Patterns list, and Pitfall 3
    - `.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md` — the `src/router.tsx` pattern snippet
  </read_first>
  <action>
    Create `src/router.tsx` following 01-PATTERNS.md's `src/router.tsx` pattern snippet exactly in
    shape. Import `createBrowserRouter` and `RouterProvider` from `react-router-dom`, import
    `AppShell` from `@/components/AppShell`, and import the seven page components from `@/pages/*`.

    Define a router with ONE top-level route object whose `element` is the `AppShell` element and
    whose `children` array is, in this order: `{ index: true }` for `HomePage`, `path: "game"` for
    `GamePage`, `path: "results"` for `ResultsPage`, `path: "gallery"` for `GalleryPage`,
    `path: "profile"` for `ProfilePage`, `path: "share/:slug"` for `SharePage`, and `path: "*"` for
    `NotFoundPage`. Child paths are relative (no leading slash) because they nest under the layout
    route. Export a named `AppRouter` component that returns `RouterProvider` with that router.

    Per D-01, use Data mode (`createBrowserRouter`) — do NOT use `BrowserRouter` with `Routes`/`Route`
    Declarative mode. 01-RESEARCH.md's Anti-Patterns list calls this out specifically: both exist in
    react-router-dom v7, but Declarative mode lacks the loader/action data APIs later phases will
    want and is the wrong pattern to establish now.

    Per D-04, `AppShell` appears exactly once, as the parent route element. Do not wrap individual
    children in it.

    Per D-03, the `*` catch-all route lives inside the layout route's children so the 404 page renders
    with nav and footer intact — this is the React-Router-side half of DEPLOY-02, paired with the
    `vercel.json` rewrite from plan 01-03.

    Rewrite `src/main.tsx` to mount `AppRouter`: keep the three `@fontsource` imports
    (`@fontsource/inter/400.css`, `@fontsource/inter/600.css`, `@fontsource/ibm-plex-mono/400.css`)
    and the `./index.css` import at the top, then `createRoot` on the `#root` element rendering
    `AppRouter` inside React's `StrictMode`. Remove the `App` import and its usage.

    Delete `src/App.tsx` and the orphaned demo asset `src/assets/react.svg`. Plan 01-02 already
    removed `src/App.css`. Leave `public/vite.svg` and its favicon link alone — replacing the favicon
    is not a Phase 1 requirement and is not specified by 01-UI-SPEC.md.

    In `index.html`, set the document `title` to `PinchPop` (the Vite template ships
    `Vite + React`, which would surface as the browser tab and share-preview title) and confirm
    `lang="en"` and the `class="dark"` attribute added in plan 01-02 are both still on the `html`
    element. Do not add Open Graph or Twitter meta tags — those belong to Phase 8's public sharing
    work, not here.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run format:check && npm run build && grep -q 'createBrowserRouter' src/router.tsx && grep -q 'AppShell' src/router.tsx && grep -q 'share/:slug' src/router.tsx && grep -q 'AppRouter' src/main.tsx && grep -q '@fontsource/inter/400.css' src/main.tsx && test ! -e src/App.tsx && test ! -e src/App.css && ! grep -q 'BrowserRouter>' src/router.tsx && ! grep -rq 'react-router/dom' src/ && grep -q '<title>PinchPop</title>' index.html && echo ROUTER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all exit 0.
    - `src/router.tsx` exports `AppRouter`, calls `createBrowserRouter`, and contains no `BrowserRouter` JSX element and no `Routes`/`Route` component usage (D-01).
    - The route tree contains exactly one `AppShell` reference, used as the parent route `element` (D-04): `grep -c 'AppShell' src/router.tsx` returns 2 (one import, one usage).
    - The children include `index: true`, and the paths `game`, `results`, `gallery`, `profile`, `share/:slug`, and `*` — all seven present.
    - `src/main.tsx` renders `AppRouter`, retains all three `@fontsource` imports and the `./index.css` import, and no longer imports `App`.
    - `src/App.tsx`, `src/App.css`, and `src/assets/react.svg` no longer exist.
    - `grep -rc 'react-router/dom' src/` and `grep -rc 'from "react-router"' src/` both report 0 matches.
    - `index.html` contains `<title>PinchPop</title>`, `lang="en"`, and `class="dark"`, and contains no `og:` or `twitter:` meta tag.
    - `npm run build` output `dist/index.html` exists.
  </acceptance_criteria>
  <done>`createBrowserRouter` serves all seven routes through a single `AppShell` layout route, mounted from `main.tsx`, with the Vite demo scaffolding removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the running app against SHELL-01 through SHELL-04</name>
  <files>none — verification only</files>
  <read_first>
    - `.planning/phases/01-app-shell-deployment-pipeline/01-VALIDATION.md` — "Manual-Only Verifications" table (why these checks are manual: D-06 defers the test runner, so lint/typecheck/build are the only automated gates this phase)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` — Route & Placeholder Page Inventory and Layout & Breakpoints, to compare what renders against what was specified
  </read_first>
  <action>
    Pause execution and hand control to the developer. Run `npm run dev` and report the printed local
    URL, then present the `how-to-verify` checklist below verbatim and wait for the resume signal.

    Do not self-approve, do not infer success from a clean build, and do not skip this checkpoint on
    `workflow.auto_advance` — 01-VALIDATION.md records these as manual-only verifications because
    D-06 defers the test runner, so no automated signal exists that can stand in for them.

    If the developer reports defects, fix them in the owning file (`src/pages/*` for copy,
    `src/components/NavBar.tsx` or `MobileNavSheet.tsx` for nav behavior,
    `src/components/AppShell.tsx` for the footer, `src/index.css` for tokens and fonts), re-run
    `npm run lint`, `npm run typecheck`, and `npm run build`, then re-present this checkpoint. Do not
    mark the plan complete on an unresolved defect.
  </action>
  <what-built>
    A complete client-side app shell: a `createBrowserRouter` route tree with `AppShell` as the
    nested layout route, seven page components carrying 01-UI-SPEC.md's locked copy, responsive
    navigation (desktop link row above 768px, Radix Sheet drawer below), the PinchPop brand token
    palette on a dark default theme, and self-hosted Inter + IBM Plex Mono.

    Every automatable check has already run and passed: `npm run lint`, `npm run typecheck`,
    `npm run format:check`, and `npm run build`. What remains is visual and interaction verification,
    which 01-VALIDATION.md records as manual-only for this phase because D-06 defers introducing a
    test runner until Vitest lands in a later phase.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev` and open the printed local URL (typically
    `http://localhost:5173`). Then check the following:

    1. **SHELL-01 — landing page.** At `/` you should see the headline "Capture. Solve. Remember.",
       one paragraph describing the gesture-photobooth premise, and a single amber ("signal", #f5c518)
       button labeled "Start Playing". Background is near-black ink (#0a0a08). The top bar shows the
       amber "PINCHPOP" wordmark on the left.
    2. **SHELL-02 — navigate to the game screen.** Click "Start Playing". The URL becomes `/game` and
       a white card reads "Game screen coming soon" with the body "The gesture capture and puzzle
       engine lands in the next phase." Then click "Play" in the top nav — same destination. Confirm
       the active nav link has a 2px amber underline and that the underline moves as you navigate.
    3. **SHELL-03 — results page.** Enter `/results` in the address bar. A white card reads "Results
       coming soon" with the body "Your photo and score will appear here once the capture flow is
       built."
    4. **Remaining routes.** Visit `/gallery`, `/profile`, and `/share/anything` — each shows its own
       "coming soon" card. Then visit a nonsense path like `/does-not-exist` — you should see "Page
       not found." with a "Back to Home" button that returns you to `/`. The nav bar and the footer
       line "PinchPop — built on PuzzleCam by Unnati-23" must be visible on every one of these pages,
       including the 404.
    5. **SHELL-04 — responsive layout.** Open DevTools' device toolbar. At 375px wide (iPhone SE) the
       horizontal nav links must be gone, replaced by a hamburger button on the right; tapping it
       opens a drawer from the right side containing Home, Play, Gallery, Profile stacked vertically,
       and tapping any link both navigates and closes the drawer. Press Escape — the drawer closes.
       At 768px and above the hamburger disappears and the horizontal link row returns. At 1280px the
       content stays centered rather than stretching edge to edge. Confirm no horizontal scrollbar
       appears and no text is clipped at 375px, 768px, and 1280px.
    6. **Typography and fonts.** The nav links, wordmark, and footer render in IBM Plex Mono
       (monospace, uppercase, letter-spaced). Headings and body copy render in Inter. Neither should
       fall back to a generic system font — the fonts are bundled locally, so this works offline.

    Report anything that does not match, especially: wrong or paraphrased copy, missing footer
    attribution, nav underline on the wrong link, drawer that does not close on navigation, or any
    layout breakage at 375px.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms all six checks above pass in a browser.</human-check>
    <automated>npm run lint && npm run typecheck && npm run build && echo GATES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Developer confirms checks 1 through 6 pass, or reports specific defects.
    - `npm run lint`, `npm run typecheck`, and `npm run build` exit 0 at the moment of verification.
    - If defects are reported, they are fixed and this checkpoint is re-run before the plan is marked complete — this checkpoint is not auto-approvable.
  </acceptance_criteria>
  <resume-signal>Type "approved" to continue, or describe the issues found.</resume-signal>
  <done>SHELL-01, SHELL-02, SHELL-03, and SHELL-04 are confirmed working in a local browser, and the client-side half of DEPLOY-02 (in-app routing to `/gallery` and `/share/:slug`) is observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL bar → React Router | The only untrusted input in this phase: an arbitrary path, including the `:slug` dynamic segment |
| Browser DOM → React component tree | Rendered pages; all copy is compile-time constant |

*(No auth, no forms, no network calls, no persistence — ASVS V2/V3/V4/V5/V6 remain not applicable per 01-RESEARCH.md "Security Domain".)*

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Tampering | Reflected XSS through the `:slug` URL segment in `SharePage` | mitigate | The slug is consumed only as a `data-share-slug` attribute value, which React escapes; it is never interpolated into HTML, a URL, or a script context. `dangerouslySetInnerHTML` is asserted absent from all of `src/` by Task 1's automated verify. Revisit when Phase 8 makes the slug drive a real data fetch. |
| T-01-01 | Elevation of Privilege (future risk) | Catch-all `*` route paired with `vercel.json`'s unconditional rewrite | accept | Deliberate per D-03: no API routes exist under `/api` in this milestone, so nothing is shadowed. Carried forward from plan 01-03's register — the first phase to add a Vercel Function under `/api` must carve it out of the rewrite. |
| T-01-13 | Repudiation | Loss of the PuzzleCam attribution when pages render outside `AppShell` | mitigate | The route tree places every page, including the 404, inside the single `AppShell` layout route (D-04); Task 1's verify asserts no page file contains the attribution string, and the human checkpoint explicitly checks the footer is present on every route including `/does-not-exist`. |
| T-01-15 | Information Disclosure | Vite dev server exposing the app on a LAN interface during the human checkpoint | accept | `npm run dev` binds to localhost by default; the checkpoint does not use `--host`. No secrets exist in the app at this stage regardless. |
| T-01-SC | Tampering | Package-manager installs (ASVS V10) | mitigate | This plan installs nothing new — it consumes only the audited dependency tree. Any package added here must first be re-audited with `slopcheck install --ecosystem npm` and escalated to a blocking human checkpoint if `[ASSUMED]` or `[SUS]`. |
</threat_model>

<verification>
1. `npm run lint` — exits 0
2. `npm run typecheck` — exits 0
3. `npm run format:check` — exits 0
4. `npm run build` — exits 0, emits `dist/index.html`
5. `grep -rc 'built on PuzzleCam by Unnati-23' src/` — exactly one match, in `src/components/AppShell.tsx`
6. `grep -rc 'react-router/dom' src/` — 0 matches (Pitfall 3)
7. Human checkpoint (Task 3) approved
</verification>

<success_criteria>
- Seven page components exist under `src/pages/` with 01-UI-SPEC.md's locked copy reproduced verbatim.
- `src/router.tsx` uses `createBrowserRouter` Data mode with `AppShell` as the single parent layout route and a `*` catch-all child (D-01, D-03, D-04).
- `src/main.tsx` mounts `AppRouter` and retains the three `@fontsource` imports.
- Vite demo scaffolding (`App.tsx`, `App.css`, `src/assets/react.svg`) is removed; document title is `PinchPop`.
- SHELL-01, SHELL-02, SHELL-03, and SHELL-04 are confirmed by human verification in a local browser.
- All internal navigation uses `Link`/`NavLink` from `react-router-dom`; no raw internal anchors, no `react-router`/`react-router/dom` imports.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-deployment-pipeline/01-05-SUMMARY.md` when done.
</output>
