---
phase: 01-app-shell-deployment-pipeline
plan: 08
type: execute
wave: 8
depends_on: [01-05]
files_modified:
  - index.html
  - package.json
  - package-lock.json
  - src/index.css
  - src/main.tsx
  - src/components/AppShell.tsx
  - src/components/NavBar.tsx
  - src/components/MobileNavSheet.tsx
  - src/components/PlaceholderCard.tsx
  - src/pages/GamePage.tsx
  - src/pages/ResultsPage.tsx
  - src/pages/GalleryPage.tsx
  - src/pages/ProfilePage.tsx
  - src/pages/SharePage.tsx
  - src/pages/NotFoundPage.tsx
  - src/pages/HomePage.tsx
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]

must_haves:
  truths:
    - "Every route renders on a warm paper surface (#FBF1E4) by default, with no forced color-mode class on the html element — DESIGN.md section 3 (paper = primary page background) and 01-UI-SPEC.md Revision 4 Implementation Note 1"
    - "Ink (#221A14) appears only as contained chrome — the nav bar, the footer, and the Home hero visual's bezel frame — and is never a full-page background; DESIGN.md section 3 lists nav/footer/camera-HUD chrome as ink's jobs and section 6 names the arcade-cabinet bezel as the layout device the Home hero visual implements"
    - "Display and Heading text renders in Space Grotesk 700, body copy in Inter 400, button labels in Inter 600, and nav/footer/step-number labels in IBM Plex Mono 400 uppercase — DESIGN.md section 4 type table, 01-UI-SPEC.md Revision 4 Typography"
    - "Exactly three weight values exist in hand-written source: 700 (Space Grotesk), 400 and 600 (Inter), 400 (IBM Plex Mono) — no 500, no mono above 400, no Space Grotesk below 700 — 01-UI-SPEC.md Revision 4 Typography 'exactly 3 weights' rule"
    - "The landing page carries exactly one primary action, Start Playing to /game, rendered as a filled shutter (#FF5A3C) button with a shutter-ink (#221A14) label; it appears twice — once in the hero and once as the closing reprise band — and no second, different action exists anywhere on the page — DESIGN.md section 8, 01-UI-SPEC.md Revision 4 Landing Page Treatment item 8"
    - "The Landing Gradient Wash colors appear only inside the Home hero's decorative layers, and nowhere else in the app — 01-UI-SPEC.md Revision 4 'Landing Gradient Wash' hard rules"
    - "The Home hero carries a substantial hero visual — an ink-bezelled paper 'booth' panel at least 300px tall holding three graduated, staggered, flat paper-raised print cards at different scales and z-layers — so the landing page's dominant supporting element is a real photobooth object rather than faint shapes behind text; DESIGN.md sections 1 (there is always one hero), 6 (centered arcade-kiosk composition), 01-UI-SPEC.md Revision 4 Landing Page Treatment item 5"
    - "The Home hero's print cards are plain flat rounded rectangles in landscape proportion with no print-frame border, no caption zone, and no bottom-heavy Polaroid padding — DESIGN.md sections 11 and 21, 01-UI-SPEC.md Revision 4 motif 5 reconciliation"
    - "The nav wordmark renders Space Grotesk 700 in shutter — DESIGN.md section 7, developer-resolved over 01-UI-SPEC.md Revision 4's mono treatment"
    - "Home renders the full Revision 4 page arc — hero, then a designed three-step 'How PinchPop Works' band, then a closing CTA reprise band, then the AppShell footer — with the three steps staggered vertically and marked by mono numerals in ring markers rather than three interchangeable cards — 01-UI-SPEC.md Revision 4 Landing Page Treatment items 6 and 8, DESIGN.md section 4's numbered-sequence authorization"
    - "No fabricated proof content exists anywhere on Home — zero photographs, zero image tags, zero score/count/testimonial/telemetry strings, zero references to any design-exploration asset — 01-UI-SPEC.md Revision 4 Landing Page Treatment item 7 (supporting/proof content explicitly deferred out of Phase 1) and DESIGN.md section 10"
    - "The five stub-route icons render in ink only, with no per-route decorative color — 01-UI-SPEC.md Revision 4 Route and Placeholder Page Inventory, reconciliation item 2"
    - "The /game puzzle-grid motif renders in ink at low opacity and appears on no other route — 01-UI-SPEC.md Revision 4 motif 6"
    - "Every interactive element shows a 2px shutter focus-visible ring, the hamburger keeps its 44x44px target and aria-label, and every hover/press animation reduces to an instant state change under prefers-reduced-motion — DESIGN.md sections 18, 19, 20"
    - "Every locked Copywriting Contract string is byte-identical, all seven routes still resolve, and the createBrowserRouter route tree plus the AppShell nested-layout architecture are untouched — 01-UI-SPEC.md Revision 4 Copywriting Contract, CONTEXT.md D-01/D-03/D-04"
  artifacts:
    - path: "src/index.css"
      provides: "Revision 4 @theme brand tokens (paper/paper-raised/paper-border/ink/ink-soft/shutter/shutter-ink/danger), the three reserved functional accents, the four Landing Gradient Wash colors, and the light-only :root semantic remapping"
      contains: "--color-shutter: #FF5A3C"
    - path: "src/main.tsx"
      provides: "Space Grotesk 700 plus Inter 400 and 600 plus IBM Plex Mono 400 fontsource imports"
      contains: "@fontsource/space-grotesk/700.css"
    - path: "index.html"
      provides: "Root html element with no forced color-mode class, so paper-on-light is the default rendered state"
      contains: "<html lang=\"en\">"
    - path: "src/components/AppShell.tsx"
      provides: "Paper page surface with the footer as a contained ink island"
      contains: "bg-paper"
    - path: "src/components/NavBar.tsx"
      provides: "64px ink nav with shutter wordmark, paper links, and a 2px shutter active-route underline"
      contains: "border-shutter"
    - path: "src/components/PlaceholderCard.tsx"
      provides: "paper-raised card on paper-border with an optional ink-only lucide icon prop"
      contains: "bg-paper-raised"
      min_lines: 30
    - path: "src/pages/HomePage.tsx"
      provides: "The full Revision 4 landing composition — hero with five Home-only motifs and a substantial ink-bezelled booth visual, a staggered three-step How PinchPop Works band, and a closing CTA reprise band (SHELL-01, SHELL-02)"
      contains: "data-card-stack-item"
      min_lines: 200
    - path: "src/pages/GamePage.tsx"
      provides: "Stub page with the ink-toned 3x3 puzzle-grid motif, scoped to /game only"
      contains: "data-motif=\"puzzle-grid\""
    - path: "src/pages/NotFoundPage.tsx"
      provides: "Calm danger-accented 404 whose secondary CTA carries the shared press feedback without the shutter glow"
      contains: "active:scale-[0.97]"
  key_links:
    - from: "src/index.css"
      to: "src/components/NavBar.tsx"
      via: "shutter theme token consumed as text-shutter / border-shutter / outline-shutter utilities"
      pattern: "shutter"
    - from: "src/main.tsx"
      to: "src/index.css"
      via: "Space Grotesk fontsource import backing the --font-heading stack"
      pattern: "space-grotesk"
    - from: "src/pages/GamePage.tsx"
      to: "src/components/PlaceholderCard.tsx"
      via: "icon prop passing a lucide icon rendered in ink"
      pattern: "icon=\\{Grid3x3\\}"
    - from: "src/pages/HomePage.tsx"
      to: "/game"
      via: "react-router-dom Link on the hero CTA and on the closing reprise CTA — the same single action, twice"
      pattern: "to=\"/game\""
    - from: "src/pages/HomePage.tsx"
      to: "01-UI-SPEC.md Revision 4 Landing Page Treatment items 6 and 8"
      via: "How PinchPop Works band and closing CTA reprise rendered in the same component, below the hero"
      pattern: "How PinchPop Works"
---

<objective>
Bring the already-built Phase 1 shell into compliance with the approved visual direction defined by
the repo-root `DESIGN.md` and `01-UI-SPEC.md` **Revision 4**.

Purpose: Phase 1's structure is correct and shipped; its visual personality is not. The live code on
`main` still renders the original near-black `#0a0a08` / amber `#f5c518` theme with `class="dark"`
forced onto `<html>`. `DESIGN.md` — the project's authoritative visual source of truth — specifies a
warm paper-first arcade-kiosk system with a single coral shutter accent, three job-specific reserved
accents, and motifs scoped to one context each. `01-UI-SPEC.md` Revision 3 reconciled the phase's
design contract to `DESIGN.md` value-for-value; **Revision 4 then extended that reconciliation to the
Landing Page Treatment's full composition**, taking it from a 5-item hero-only list to the complete
8-item page arc: hero → "How PinchPop Works" → supporting/proof content explicitly deferred → closing
CTA reprise → footer.

**This is a token and visual-treatment correction pass, NOT a rearchitecture.** The seven routes, the
`createBrowserRouter` Data-mode route tree, the `AppShell` / `NavBar` / `MobileNavSheet` /
`PlaceholderCard` component boundaries, the spacing scale, the breakpoints, and every locked copy
string all stay exactly as they are. Do not recreate scaffolding, routing, or component structure.
`src/router.tsx` must end this plan with a zero diff.

**The one place this plan asks for real construction rather than retokenization is the Home page.**
Revision 4's composition cannot be satisfied by restyling the existing three-element hero; `/` needs a
hero visual with genuine weight plus two new page sections. Tasks 4 and 5 build that.

Output: a retokenized `src/index.css`, an added heading font, a de-darkened `index.html`, restyled
shell chrome, an ink-only stub-page icon treatment, a rebuilt Home page carrying the full Revision 4
page arc, and a developer-approved visual verification.

**Known scope tradeoff:** at six tasks and fifteen files this plan sits above the usual per-plan
budget, and it is kept whole deliberately — Tasks 4 and 5 both rebuild `src/pages/HomePage.tsx`, so
splitting them across plans would put one file in two plans and serialize them anyway, with no
parallelism gained; the unusually high per-task specificity (near-zero open design decisions) is what
keeps it executable at this size.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md
@.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md
@.planning/phases/01-app-shell-deployment-pipeline/01-VALIDATION.md
@.planning/phases/01-app-shell-deployment-pipeline/01-02-SUMMARY.md
@.planning/phases/01-app-shell-deployment-pipeline/01-04-SUMMARY.md
@.planning/phases/01-app-shell-deployment-pipeline/01-05-SUMMARY.md

## Source-of-truth precedence

1. **`DESIGN.md`** (repo root) — authoritative for every color value, type value, motif scope, layout
   principle, and accessibility rule. **Read-only in this plan. Do not edit it under any circumstance.**
2. **`01-UI-SPEC.md` Revision 4** — the phase-level implementation contract, reconciled to
   `DESIGN.md` value-for-value. Binding for route copy, component inventory, class-level treatment,
   the exact Landing Gradient Wash hues (which `DESIGN.md` deliberately leaves open), and — new in
   Revision 4 — the Home route's full 8-item composition arc.
3. **`01-CONTEXT.md`** — still binding for every non-visual decision: D-01 (Data mode
   `createBrowserRouter`), D-03 (catch-all route), D-04 (nested layout route), D-09 (folder layout),
   D-10 (TypeScript strict, `noUnusedLocals`), D-13 (no new runtime dependencies beyond fonts), D-14
   (npm), D-15 (`@fontsource` self-hosted, weight-specific imports, no variable fonts). Its color and
   typography references are superseded by the two documents above.

`01-PATTERNS.md` is deliberately **not** loaded as context for this plan: its token tables and its
attribution requirement are both stale. Do not consult it for visual values.

**Attribution:** commit `9e84385` removed the PuzzleCam/Unnati attribution from the app and README.
That removal is settled. Do NOT reintroduce it into `AppShell.tsx` or anywhere else, even though
`01-PATTERNS.md` still describes it as non-negotiable.

**Out of scope:** plans 01-06 and 01-07 (CI, branch protection, Vercel linking, production smoke
tests) are untouched by this visual pass and remain future work. Do not modify plans 01-01 through
01-07 or their summaries.

## What Revision 4 changed, and what it did not

Revision 4's own reconciliation note is explicit: it is "an additive scoping correction to one
section (Landing Page Treatment) only." Every token value, typography rule, spacing scale, nav/footer
structure, route inventory, copywriting contract, and component-inventory statement carried over from
Revision 3 byte-for-byte. Concretely, for this plan:

| Area | Revision 4 status | Effect on this plan |
|---|---|---|
| Color table (8 core + 3 reserved + 4 wash) | unchanged from Revision 3 | Task 1's token migration table stands as written — audit, do not rewrite |
| Typography table (56/34, 30/22, 16, 13; three weights) | unchanged | Task 1 and the type-role map stand as written |
| Spacing scale, breakpoints, nav/footer structure | unchanged | Tasks 2 and 3 stand as written |
| Route & Placeholder Page Inventory, Copywriting Contract | unchanged | Task 3 stands as written |
| Landing Page Treatment | **extended from 5 items to 8** | Tasks 4 and 5 are rebuilt against it |

Task 1's first job is to **verify** the first row of that table against the actual file rather than
trust it. If any hex in `01-UI-SPEC.md` Revision 4's Color table differs from `DESIGN.md` §3, stop and
report — `DESIGN.md` §3 wins and the discrepancy is a document defect worth surfacing, not something
to silently paper over.

## Revision 4's Landing Page Treatment — the eight-item composition contract

This is the contract Tasks 4 and 5 implement. Items are quoted in substance from
`01-UI-SPEC.md` Revision 4's Landing Page Treatment section.

| Item | Requirement | Where implemented |
|---|---|---|
| 1 | `paper` background, halftone dot-grid texture, 2–3 blurred Landing Gradient Wash blobs behind the hero, never behind text | Task 4 |
| 2 | Camera-aperture ring centered behind the headline at low opacity | Task 4 |
| 3 | Viewfinder corner brackets framing the headline + CTA block | Task 4 |
| 4 | Display headline, Body subhead, single primary CTA "Start Playing" → `/game` | Task 4 |
| 5 | Tilted card-stack motif adjacent to / below the CTA, plain flat cards, no Polaroid framing | Task 4 — **upgraded to the substantial hero visual** (see below) |
| 6 | "How PinchPop Works" — numbered 3-step section below the hero, `paper-raised` info cards, Heading title + Body description + numeral marker, no new colors | Task 5 |
| 7 | Supporting/proof content — **explicitly scoped out of Phase 1.** No recent-captures strip, no gallery preview, no testimonials, no counts. No placeholder photos, fake scores, or fake counts fill this slot. | Task 5 asserts its absence |
| 8 | Closing CTA reprise — a quiet repeat of the same primary CTA, before the `AppShell` footer, no new decorative motif, no new color, no new copy string | Task 5 |

Item 7 is a **hard prohibition, not a suggestion.** Phase 1 has zero real captured photos and zero
real user data by construction. Nothing in this plan may invent them. This matters specifically
because the composition references below (which the developer asked to draw on) *do* contain exactly
that kind of content — see the next section.

## Stitch Concept 01 — what to learn, what never to copy

`design-exploration/concept-01/home.html` and `design-exploration/concept-01/Home.png` are a
**composition, proportion, spacing, and layering study only.** Concept 01 ("Studio Arcade") is a dark
neon-HUD photobooth in its own palette; it is structurally the strongest of the three explorations,
and its hero ideas are worth learning from. Its *appearance* is not PinchPop's.

**Read it for these five ideas, and reinterpret every one of them in PinchPop's own tokens:**

| Concept 01 idea | PinchPop reinterpretation (this is what the plan asks for) |
|---|---|
| Headline occupies a huge share of the hero, uppercase, tight leading, wrapping to multiple lines, unambiguously dominant | Space Grotesk 700 at `DESIGN.md` §4's fixed 56/34 Display scale, constrained to a narrow measure (`max-w-[15ch]`) with `text-balance` and slight negative tracking so it wraps to 2–3 lines and reads as a slab. Sentence case, not uppercase — uppercase is the mono Label role only (`DESIGN.md` §4/§21). |
| A wide "workbench" panel below the headline: a bordered mat, `max-w-5xl`, `rounded-xl shadow-2xl overflow-hidden`, holding a graduated cluster (small auxiliary column, large centered item, small auxiliary column) | The **booth panel** in Task 4: an `ink` bezel (`DESIGN.md` §6's arcade-cabinet bezel, "screen inside a machine") wrapping a `paper` interior at least 300px / 420px tall, holding three flat `paper-raised` print cards at three different scales, rotations, and z-layers. |
| Elements at deliberately different scales rather than one uniform row | Graduated card sizing (large centre, two smaller flanks) in Task 4; vertical `translate-y` stagger across the three step cards in Task 5. |
| Corner-bracket viewfinder framing around the focal element | Motif 1's four L-brackets in `border-lens-blue`, 2px, framing the headline + CTA block per Revision 4 item 3. |
| A how-it-works row of cards, each with an icon, a numeral marker, a title, a body, and a hairline | Task 5's three step cards: ink lucide glyph, mono numeral inside a ring marker, Heading title, Body copy — in `paper-raised` on `paper-border`, no chips, no accent color. |

**Never copy, under any circumstance:**

- Any HTML, class name, CSS custom property, or literal value from `design-exploration/` — every
  element in this plan is written fresh in the project's own React/Tailwind against the
  `DESIGN.md`-approved tokens. `design-exploration/` is read-only reference; nothing under it is
  imported, linked, referenced, or copied into `src/`.
- Concept 01's palette (near-black surfaces, neon green/cyan, its own coral). PinchPop is
  paper-dominant with `ink` as chrome/islands only.
- Its persistent nav token chip ("⚡ 50 TOKENS") — `DESIGN.md` §7/§21 forbid a persistent
  token/score/status chip in global navigation outright.
- Its telemetry status pill ("WEBCAM READY • OPTICAL TRACKING 60FPS"), its optical-HUD readout panel,
  and its live arcade-feed ticker — `DESIGN.md` §14/§21 scope HUD chrome to Camera and Puzzle only,
  and Phase 1 has no live anything to report.
- Its "ALL-TIME BEST 9,840 PTS" score card, its "RECENT POLAROID CRACKS" photo wall, its
  "1,248 SOLVED TODAY" counter, and every photograph on the page — Revision 4 item 7 defers all
  supporting/proof content out of Phase 1, and `DESIGN.md` §10 forbids depending on stock photography.
- Its second hero CTA ("WATCH GESTURE DEMO") — `DESIGN.md` §8 allows exactly one primary action per
  screen.
- Its eyebrow label above the how-it-works heading ("PHYSICAL OPTICS & ARCHITECTURE") —
  `DESIGN.md` §4 explicitly lists "an eyebrow label above every heading" as a thing to avoid.
- Its 4-step flow. PinchPop's is 3 steps, matching `DESIGN.md` §4's named "How PinchPop Works 3-step
  flow."

## Two token traps — read before writing any class name

Two utility names survive this migration but change meaning. Blindly keeping the existing class names
produces a silently wrong result that still compiles.

| Utility | Meant before | Means after | Required action |
|---|---|---|---|
| `bg-paper` / `text-paper` | `#ffffff`, a **white card** surface | `#FBF1E4`, the **warm page** surface | `AppShell`'s page wrapper now wants `bg-paper`. Card surfaces (`PlaceholderCard`, `NotFoundPage`) must switch to `bg-paper-raised`. |
| `text-danger` / `border-danger` | `#e0533d`, an orange-red | `#C81E3A`, a cooler ruby | Keep the utility names; the value change is the point. `DESIGN.md` section 3 requires `danger` be visibly cooler than `shutter` so failure and action are never confusable. |

## Token migration table (live `main` state -> Revision 4)

Complete rename/revalue map for the `@theme` block in `src/index.css`. Every name in the "Retired"
column must have zero remaining occurrences anywhere in `src/index.css` (comments included) and zero
remaining utility usages under `src/components/*.tsx` and `src/pages/` after this plan. Revision 4
changed no value in this table; it is carried forward from Revision 3 unmodified and re-verified.

| Current on `main` | Revision 4 token | Value | Role (source) |
|---|---|---|---|
| `--color-paper: #ffffff` | `--color-paper` (kept, **revalued**) | `#FBF1E4` | Primary page background (`DESIGN.md` s3) |
| *(new)* | `--color-paper-raised` | `#FFFFFF` | Card/print surfaces on paper (`DESIGN.md` s3) |
| `--color-paper-border: #e3e0d8` | `--color-paper-border` (kept, **revalued**) | `#E7DCC8` | Hairline borders, print-edge lines (`DESIGN.md` s3) |
| `--color-ink: #0a0a08` | `--color-ink` (kept, **revalued**) | `#221A14` | Nav/footer/bezel surface AND default text color (`DESIGN.md` s3) |
| `--color-ink-soft: #6b665c` | `--color-ink-soft` (kept, **revalued**) | `#6B5E4F` | Secondary/muted text on paper (`DESIGN.md` s3) |
| `--color-signal: #f5c518` | **retired** -> `--color-shutter` | `#FF5A3C` | The single primary accent (`DESIGN.md` s3) |
| *(new)* | `--color-shutter-ink` | `#221A14` | Text/icon color on shutter fills (`DESIGN.md` s3) |
| `--color-danger: #e0533d` | `--color-danger` (kept, **revalued**) | `#C81E3A` | Errors/destructive only (`DESIGN.md` s3) |
| `--color-paper-warm: #eae5d6` | **retired** — no replacement | — | Superseded by `paper` / `paper-raised` |
| *(new, reserved)* | `--color-viewfinder` | `#1E9E8C` | Live-tracking teal — Phase 2 (`DESIGN.md` s3) |
| *(new, reserved)* | `--color-token-gold` | `#FFC531` | Score/currency — Phase 5+ (`DESIGN.md` s3) |
| *(new, reserved)* | `--color-neon-violet` | `#7B5CF0` | Puzzle-active tile — Phase 2 (`DESIGN.md` s3) |
| *(new, Home-only)* | `--color-flash-pink` | `#FF3D9A` | Landing Gradient Wash (`01-UI-SPEC.md`) |
| *(new, Home-only)* | `--color-lens-blue` | `#2F80ED` | Landing Gradient Wash (`01-UI-SPEC.md`) |
| *(new, Home-only)* | `--color-darkroom-violet` | `#8B5CF6` | Landing Gradient Wash (`01-UI-SPEC.md`) |
| *(new, Home-only)* | `--color-flash-lime` | `#D6F24C` | Landing Gradient Wash (`01-UI-SPEC.md`) |

The three reserved accents render nowhere in Phase 1 — no live camera feed, no real score, no
interactive puzzle grid exists yet. They are declared so Phase 2 does not have to reopen the token
block. Declaring an unused `@theme` color emits no CSS, so this costs nothing at build time.

**Flash Lime is also declared-but-unused in Phase 1, deliberately.** `01-UI-SPEC.md` Revision 4 calls
it "the highest-contrast decorative pop, used most sparingly of the four." At Phase 1's decoration
density there is no place for it that would not read as a status LED — and a status indicator on Home
is exactly what `DESIGN.md` §14/§21 forbid. Leaving it unrendered is the correct outcome, not an
omission to fix.

**Expected transient state:** after Task 1 retires `--color-signal` and `--color-paper-warm`, the
utilities `text-signal`, `border-signal`, and `text-paper-warm` still appear in components until
Tasks 2 through 5 replace them. Tailwind v4 emits nothing for an unknown utility rather than failing
the build, so all four quality gates stay green throughout — the app will simply look half-migrated
between tasks. This is expected. Do not "fix" it by re-adding retired tokens.

## Retired-value blacklist (asserted absent, comments included)

Every automated verify in this plan asserts these strings are gone. They come from two superseded
revisions and must not reappear in code, class names, or comments.

- Original dark theme: `0a0a08`, `f5c518`, `e0533d`, `eae5d6`, `e3e0d8`, `6b665c`, `--color-signal`,
  `--color-paper-warm`
- The intermediate revision this plan replaces: `FFF8EF`, `FF5A47`, `1D1626`, `5B5548`, `F0E4D3`,
  `--color-coral`, `--color-cream`, `--color-ruby`, `--color-ink-muted`, `--color-card-border`,
  `viewfinder-blue`
- Retired patterns: any `iconChipClassName` or `iconColor` prop, any per-route decorative icon color,
  any Polaroid/print-frame styling on the Home hero's decorative card-stack
- **New in this revision:** any reference to `design-exploration` from anywhere under `src/`, and any
  fabricated-proof string on Home (see the Fabricated-content blacklist below)

Hex comparisons are case-insensitive in the verify scripts, so lowercase reintroductions are caught too.

## Fabricated-content blacklist (Home page, Revision 4 item 7)

Tasks 4 and 5 both assert `src/pages/HomePage.tsx` contains zero case-insensitive matches for any of
these, and zero `<img` elements, zero `url(` references, and zero `design-exploration` mentions:

`testimonial`, `review`, `reviews`, `trusted by`, `high score`, `leaderboard`, `streak`, `token`,
`pts`, `fps`, `players`, `solved`, `recent captures`, and any `N,NNN`-shaped numeral (regex
`[0-9],[0-9]{3}`).

Two of these deserve a note so they are not mistaken for over-reach: `token` is on the list because
`token-gold` is a reserved accent with no Phase 1 job and must not appear on Home at all, and `solved`
is on the list because a solve-count is the single most tempting piece of fake proof to invent. The
locked step-3 title `Solve & Save` and the locked body copy contain no `solved`, so the gate is clean
against the required copy.

## Typography role to Tailwind class map (Revision 4)

`DESIGN.md` section 4 type table; `01-UI-SPEC.md` Revision 4 Typography table (unchanged from
Revision 3). Display is 56px desktop / 34px mobile — **not** 60/36.

| Role | Classes |
|---|---|
| Display | `font-heading text-[34px] leading-[1.05] font-bold md:text-[56px]` |
| Heading | `font-heading text-[22px] leading-[1.15] font-bold md:text-[30px]` |
| Body | `font-sans text-base leading-[1.6] font-normal` |
| Body (emphasis / button label) | `font-sans text-base leading-[1.6] font-semibold` |
| Label (HUD/nav/footer/step numeral) | `font-mono text-[13px] leading-[1.4] font-normal tracking-[0.08em] uppercase` |

Exactly three weight values exist system-wide: Space Grotesk 700, Inter 400 and 600, IBM Plex Mono
400. In hand-written source that means:

- `font-heading` is always paired with `font-bold`, never `font-normal` or `font-semibold`.
- `font-sans` is paired with `font-normal` (body) or `font-semibold` (emphasis and button labels only).
- `font-mono` is always paired with `font-normal` — never `font-semibold`, never `font-bold`.
- `font-medium`, `font-light`, `font-thin`, `font-extralight`, `font-extrabold`, and `font-black` are
  forbidden outright — they would synthesize against font files that were never loaded.

The shadcn-generated files under `src/components/ui/` are exempt and **must not be edited** — the
`button.tsx` base class list ships `font-medium` and `text-sm`, both of which are overridden at the
call site by `font-semibold text-base` (tailwind-merge dedupes the conflicting utilities).

IBM Plex Mono is restricted to the Label/HUD role — nav links, the wordmark, the footer line, and the
`01`/`02`/`03` step numerals in Task 5's How-It-Works band (a genuine sequence marker, which is the
Label role, and the one numbered-step case `DESIGN.md` §4 pre-authorizes). It must not appear on body
copy, headings, or generic emphasis (`DESIGN.md` section 4, section 21).

## Shared press-feedback recipe

`DESIGN.md` section 8 gives every button variant the same scale-on-press feedback; only the glow is
primary-exclusive. `DESIGN.md` section 19 requires all of it to collapse under reduced motion. Both
CTAs therefore share this base:

`transition-all duration-200 ease-out hover:scale-[1.03] active:scale-[0.97] motion-reduce:transition-none motion-reduce:hover:scale-100 motion-reduce:active:scale-100`

The primary CTA additionally layers on `hover:shadow-lg hover:shadow-shutter/25`. The 404 secondary
CTA does not — `DESIGN.md` section 8 makes the secondary treatment explicitly shadowless. Do not add
the glow to the 404 button, and do not omit the scale or `motion-reduce:` classes from it.

Every interactive element also carries
`focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-shutter`
(`DESIGN.md` section 18: 2px shutter ring on every interactive element, no exceptions, never suppressed).

## Three documented reconciliations the executor must not "fix"

Each is resolved in favor of `DESIGN.md` and called out here so a reviewer does not read it as an
oversight.

1. **`PlaceholderCard` and the How-It-Works step cards get no hover lift or rotate.**
   `01-UI-SPEC.md`'s Component Styling Approach describes a lift-plus-rotate hover on cards.
   `DESIGN.md` section 9 scopes that treatment to info/stat cards that are "itself interactive
   (clickable), not... static stat tiles." The five stub cards and the three step cards are static,
   non-clickable informational surfaces, so applying a lift would falsely signal clickability.
   `DESIGN.md` wins: these cards are flat, with a soft ambient shadow and no hover transform. They
   therefore need no `motion-reduce:` variant either, because they have no motion. The static
   `sm:translate-y-*` stagger Task 5 applies is a layout offset, not an animation — it is present at
   all times, transitions nothing, and is unaffected by `prefers-reduced-motion`.

2. **The closing CTA reprise is not a second primary action.** `DESIGN.md` section 8 says "Exactly one
   per screen," and `01-UI-SPEC.md` Revision 4 item 8 requires a closing CTA reprise band. These do not
   conflict: §8 constrains the number of primary *actions* competing for a user's attention, not the
   number of times a single action is offered. The reprise is the **same action** — same locked label
   `Start Playing`, same `/game` target, same `shutter` fill, same shutter-click recipe — offered once
   more at the end of a page the user has now scrolled through. Home therefore contains exactly two
   `Button` elements, both invoking the one primary action, and zero buttons invoking any other action.
   Do not add a "learn more," a demo link, or a secondary variant alongside either instance. Do not
   delete the reprise on §8 grounds.

3. **The Home hero visual's `ink` bezel is a §6 layout device, not §13/§14 HUD chrome.**
   `DESIGN.md` §6 defines the whole system's composition as "centered arcade-kiosk… echoes an arcade
   cabinet's bezel, keeps the 'screen inside a machine' feeling," and §3 lists chrome bands — nav,
   footer, camera viewport frame — as `ink`'s job. Task 4's booth panel is that bezel, rendered
   literally: an `ink` frame around a `paper` interior. It is **not** the Camera screen's viewport
   (§13) and it carries **zero** live readouts, status dots, telemetry strings, or mono data labels, so
   §14's "HUD chrome only on Camera and Puzzle" rule is not engaged. Task 4's verify asserts that
   absence mechanically. Do not add a status dot, a "REC" label, a frame counter, or any mono readout
   to the booth to make it look more alive — that is precisely the failure mode `DESIGN.md` §14 was
   written to prevent, and it is the single most conspicuous thing in the Concept 01 reference.

## Wordmark decision (developer-resolved, no longer open)

`DESIGN.md` section 7 specifies the nav wordmark as Space Grotesk bold. `01-UI-SPEC.md` (Revision 3,
carried unchanged into Revision 4) deliberately keeps it as IBM Plex Mono uppercase instead, citing
section 4's mono/Label role for nav labels, and an earlier draft of this plan left that as an open
question for the final checkpoint. The developer has explicitly resolved it, superseding the UI spec on
this one point: **the wordmark renders in Space Grotesk 700, `shutter`-colored**, matching
`DESIGN.md` section 7 literally. This is a targeted exception to the spec's nav treatment, not a
reopening of any other token or layout decision in that document. Task 2 implements this directly; do
not treat it as still undecided.

## Locked copy (unchanged — reproduce verbatim, do not reword)

| Route | Heading | Body |
|---|---|---|
| `/` | Capture. Solve. Remember. | *(existing HomePage paragraph — preserve the current string byte-for-byte)* |
| `/game` | Game screen coming soon | The gesture capture and puzzle engine lands in the next phase. |
| `/results` | Results coming soon | Your photo and score will appear here once the capture flow is built. |
| `/gallery` | Gallery coming soon | Your saved photos will live here once accounts and storage are built. |
| `/profile` | Profile coming soon | Your stats and achievements will appear here in a later phase. |
| `/share/:slug` | Shared memory coming soon | Public share pages launch in a later phase. |
| `*` | Page not found. | We couldn't find that page. Head back and keep exploring PinchPop. |

Landing CTA label: `Start Playing`. 404 CTA label: `Back to Home`. Footer text: `PinchPop`.

**New copy this plan introduces** (not in the UI spec's Copywriting Contract table — defined here,
locked from this plan forward): a "How PinchPop Works" band on `/` with heading `How PinchPop Works`
and three steps:

| Step | Title | Body |
|---|---|---|
| 01 | Frame | Raise both hands to your webcam and frame the shot with your fingers — no mouse, no controller. |
| 02 | Pinch to Capture | Bring thumb and index finger together to snap an instant photo. |
| 03 | Solve & Save | Drag the scrambled puzzle back together with your hands to reveal your polaroid. |

**The closing CTA reprise band introduces no new copy at all.** `01-UI-SPEC.md` Revision 4 item 8 is
explicit: "no new copy string beyond the CTA text already locked." The band therefore carries the
already-locked headline `Capture. Solve. Remember.` restated at Heading role as a bookend, plus the
already-locked `Start Playing` CTA. Repeating the locked headline is a deliberate reprise, not a
duplication bug — it is the only way to give the band a line of type without inventing a string the
contract forbids. Do not "improve" it by writing a new closing line.

## Motif scoping — the rule that a previous revision broke

`DESIGN.md` section 21 forbids "any motif used outside its scoped context." Concretely, for this plan:

| Motif | Allowed on | Forbidden everywhere else |
|---|---|---|
| Halftone dot-grid, gradient blobs, aperture ring, viewfinder corner brackets, card-stack / booth panel | `/` (Home decorative layers) only | Yes — including stub cards, the 404, and the nav |
| Landing Gradient Wash colors (Flash Pink, Lens Blue, Darkroom Violet, Flash Lime) | `/` hero decorative layers only | Yes — no stub icon tints, no card shadow tints, no badges |
| Puzzle-grid 3x3 outline, rendered in `ink` | `/game` only | Yes |
| Polaroid / print-frame treatment (white border thicker at bottom, caption zone, tape) | **Nothing in Phase 1** — reserved for real captured photos, which do not exist yet | Yes, including the Home booth panel's print cards |
| HUD chrome / live readouts / status dots / telemetry labels | **Nothing in Phase 1** — no camera feed, no puzzle grid exists yet | Yes, including inside the Home booth panel (reconciliation 3) |
| Supporting/proof content (photos, counts, scores, testimonials, activity feeds) | **Nothing in Phase 1** — Revision 4 item 7 defers it | Yes, everywhere |

Stub-page icons render in `ink` only. There is no per-route color assignment and no icon chip.

## Interfaces the executor needs (do not go exploring)

```
// src/components/PlaceholderCard.tsx — current signature on main
export function PlaceholderCard(props: { heading: string; body: string }): JSX.Element

// after Task 2 (additive, non-breaking — existing two-prop call sites keep compiling)
export function PlaceholderCard(props: {
  heading: string;
  body: string;
  icon?: LucideIcon;     // type-only import from "lucide-react"; rendered in ink, aria-hidden
}): JSX.Element

// src/components/ui/button.tsx (generated, do not edit)
// variant: "default" | "outline" | "secondary" | "ghost" | "destructive" | "link"
// size: "default" | "xs" | "sm" | "lg" | "icon" | "icon-xs" | "icon-sm" | "icon-lg"
// asChild: boolean
// base class list includes `text-sm font-medium` — override at the call site with
// `text-base font-semibold`; tailwind-merge resolves the conflict.

// src/lib/utils.ts
export function cn(...inputs: ClassValue[]): string

// src/pages/HomePage.tsx — current content on main (Task 4 rebuilds around it)
// h1: "Capture. Solve. Remember."   (locked, reproduce verbatim)
// p : "Frame a photo with your hands, pinch to capture it, solve the resulting puzzle with
//      gestures, and keep the polaroid-style memory — PinchPop turns your webcam into a
//      gesture-controlled photobooth."   (locked, reproduce byte-for-byte including the em dash)
// Button asChild size="lg" wrapping <Link to="/game">Start Playing</Link>
```

Available `lucide-react` icons for this phase: `Camera`, `Aperture`, `Grid3x3`, `GalleryHorizontal`,
`Sparkles`, `Share2`, `Menu`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retokenize index.css to the DESIGN.md palette, add Space Grotesk, drop the forced color-mode class</name>
  <files>index.html, src/index.css, src/main.tsx, package.json, package-lock.json</files>
  <read_first>
    - `src/index.css` in full — the `@theme` brand block (lines 11-21), the `@theme inline` block including `--font-heading` and `--font-sans` (lines 23-64), the `:root` semantic block (lines 66-102), the `.dark` block (lines 104-137) including its trailing hex-reference comments, and the `@layer base` block (lines 139-149)
    - `src/main.tsx` — the four current imports: `@fontsource/inter/400.css`, `@fontsource/inter/600.css`, `@fontsource/ibm-plex-mono/400.css`, `./index.css`
    - `index.html` — confirm `class="dark"` sits on the `<html>` element (line 2) alongside `lang="en"`
    - `package.json` — confirm the existing `@fontsource/*` major (`^5.3.0`) so the new package matches
    - `DESIGN.md` — section 3 (Color Palette, the authoritative hex table) and section 4 (Typography)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` **Revision 4** — the Color table, the reserved functional accents table, the Landing Gradient Wash table, the Typography table, and Implementation Notes steps 1 through 4. Revision 4's own reconciliation note states it changed only the Landing Page Treatment section, so these tables should be byte-identical to Revision 3's — **verify that against the file rather than assuming it.** If any hex or type value differs from `DESIGN.md` §3/§4, stop and report the discrepancy; `DESIGN.md` wins and the mismatch is a document defect worth surfacing.
    - `.planning/phases/01-app-shell-deployment-pipeline/01-02-SUMMARY.md` — the Deviations section, specifically why raw hex stays in the `@theme` block and why `@fontsource-variable/inter` was rejected (D-15 forbids variable fonts)
    - This plan's **Token migration table** and **Retired-value blacklist**
  </read_first>
  <action>
    Step 1 — install the heading font. `@fontsource/space-grotesk` is not in `01-RESEARCH.md`'s
    Package Legitimacy Audit table, so it enters the tree as an unaudited package and must clear the
    `T-01-SC` gate first. Run
    `python -m slopcheck install --ecosystem npm @fontsource/space-grotesk`
    (the `--ecosystem npm` flag is mandatory — `01-RESEARCH.md` records that bare invocation
    misdetects the ecosystem). On `[OK]`, run `npm install @fontsource/space-grotesk@^5.3.0` so it
    matches the `@fontsource` major already in the tree. On `[SLOP]`, stop and report — the package is
    forbidden. On `[ASSUMED]` or `[SUS]`, stop and escalate to a blocking human checkpoint before
    installing; this escalation is never auto-approvable.

    Step 2 — `src/main.tsx` font imports. Add `import "@fontsource/space-grotesk/700.css";`. Keep all
    three existing font imports exactly as they are: `@fontsource/inter/400.css`,
    `@fontsource/inter/600.css` (Inter 600 is a required Body-emphasis/button-label weight per
    `DESIGN.md` section 4 — do NOT remove it), and `@fontsource/ibm-plex-mono/400.css`. Import
    weight-specific CSS files only — do not install or import any `@fontsource-variable/*` package
    (D-15 forbids variable fonts, and plan 01-02 already had to undo exactly this once). Leave the
    `./index.css` import, the `createRoot` call, `StrictMode`, and the `AppRouter` render untouched.

    Step 3 — `index.html`. Remove `class="dark"` from the `<html>` element so the line reads
    `<html lang="en">`. That forced class is the direct mechanical cause of the all-dark rendering.
    Keep `lang="en"`, `<title>PinchPop</title>`, the favicon link, the viewport meta, and the module
    script tag unchanged. Do not add Open Graph or Twitter meta tags — Phase 8 scope.

    Step 4 — `src/index.css`, the `@theme` brand block. Replace the seven current brand color
    declarations with the Revision 4 set, applying this plan's Token migration table exactly. Keep raw
    hex (not oklch) in this block per the convention plan 01-02 established. Write the values in
    uppercase hex exactly as listed. The resulting block declares, in this order:
    `--color-paper: #FBF1E4`, `--color-paper-raised: #FFFFFF`, `--color-paper-border: #E7DCC8`,
    `--color-ink: #221A14`, `--color-ink-soft: #6B5E4F`, `--color-shutter: #FF5A3C`,
    `--color-shutter-ink: #221A14`, `--color-danger: #C81E3A`, then the three reserved accents
    `--color-viewfinder: #1E9E8C`, `--color-token-gold: #FFC531`, `--color-neon-violet: #7B5CF0`,
    then the four Landing Gradient Wash colors `--color-flash-pink: #FF3D9A`,
    `--color-lens-blue: #2F80ED`, `--color-darkroom-violet: #8B5CF6`, `--color-flash-lime: #D6F24C`.
    Delete `--color-signal` and `--color-paper-warm` outright — do not leave them as aliases. Keep
    `--font-mono` unchanged.

    Rewrite the block's leading comment to describe the Revision 4 source (`DESIGN.md` section 3 via
    `01-UI-SPEC.md`). That comment must not name any string on this plan's Retired-value blacklist —
    the acceptance grep scans comments too. Add a short inline note above the three reserved accents
    stating they are declared but unused in Phase 1, and above the four wash colors stating they are
    Home-decoration only. Neither note may name a blacklisted string.

    Step 5 — `src/index.css`, the `@theme inline` block. Change `--font-heading` from
    `var(--font-sans)` to the literal stack
    `"Space Grotesk", "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`. Leave
    `--font-sans` on its current Inter stack, and leave every `--color-*` and `--radius-*` mapping
    line in that block untouched — those wire shadcn's semantic variables and are not part of this
    revision.

    Step 6 — `src/index.css`, the `:root` semantic block. Replace the oklch values with raw hex from
    the Revision 4 palette, and delete every trailing `/* ... */` hex-reference comment on those
    declarations (each names a retired value and would fail this task's own blacklist grep). Raw hex
    is used here deliberately in place of plan 01-02's oklch convention so every value is directly
    greppable against `DESIGN.md`; Tailwind v4 resolves opacity modifiers on hex custom properties via
    `color-mix()`, so `bg-ink/20`-style utilities keep working. Set:
    `--background: #FBF1E4`, `--foreground: #221A14`, `--card: #FFFFFF`, `--card-foreground: #221A14`,
    `--popover: #FFFFFF`, `--popover-foreground: #221A14`, `--primary: #FF5A3C`,
    `--primary-foreground: #221A14`, `--secondary: #FFFFFF`, `--secondary-foreground: #221A14`,
    `--muted: #E7DCC8`, `--muted-foreground: #6B5E4F`, `--accent: #E7DCC8`,
    `--accent-foreground: #221A14`, `--destructive: #C81E3A`, `--border: #E7DCC8`,
    `--input: #E7DCC8`, `--ring: #FF5A3C`.

    `--primary-foreground` is `shutter-ink` (`#221A14`) and NOT white on purpose: `DESIGN.md` section
    3 and section 20 both record that dark-on-bright is the verified >= 4.5:1 pairing on `shutter`,
    while white-on-coral fails AA for normal text. Do not "improve" this to white.

    Leave `--radius: 0.625rem` alone. `01-UI-SPEC.md` names `rounded-2xl` as the button and card
    corner treatment; the existing `@theme inline` formula already resolves it acceptably, and
    changing `--radius` would ripple through every generated shadcn primitive. The utility class name
    is the contract, not the pixel value. Leave the `--chart-*` and `--sidebar-*` variables untouched;
    nothing in Phase 1 consumes them.

    Step 7 — `src/index.css`, delete the entire `.dark { ... }` rule block. Every value in it is an
    oklch rendering of the retired dark palette and every declaration carries a trailing comment
    naming a blacklisted hex, so rewriting it would either preserve a wrong palette or invent a new
    inaccuracy. Deleting it is safe and verified: no hand-written file under `src/components/*.tsx` or
    `src/pages/` uses a `dark:` utility, no `.dark` class is applied anywhere now that Step 3 removed
    it from `<html>`, and no theme toggle exists in this phase or is planned for it.

    **Keep the `@custom-variant dark (&:is(.dark *));` line at the top of the file.** Removing it
    would make Tailwind fall back to its built-in `prefers-color-scheme` behavior, which would cause
    the `dark:` variants inside the generated `src/components/ui/button.tsx` and `badge.tsx` to
    activate on any developer machine set to OS dark mode. With the custom variant retained and no
    `.dark` ancestor in the tree, those variants are simply inert — which is the intended state.

    Leave the `@layer base` block untouched.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint && npm run typecheck && npm run format:check && npm run build && grep -qF '<html lang="en">' index.html && ! grep -qF 'class="dark"' index.html && for i in '@fontsource/space-grotesk/700.css' '@fontsource/inter/400.css' '@fontsource/inter/600.css' '@fontsource/ibm-plex-mono/400.css'; do grep -qF "$i" src/main.tsx || { echo "MISSING IMPORT: $i"; exit 1; }; done && ! grep -rqF 'fontsource-variable' src/ package.json && grep -qF '"@fontsource/space-grotesk"' package.json && grep -qF '@custom-variant dark' src/index.css && if grep -qE '^\.dark[[:space:]]*\{' src/index.css; then echo ".dark BLOCK NOT REMOVED"; exit 1; fi && for t in '--color-paper: #FBF1E4' '--color-paper-raised: #FFFFFF' '--color-paper-border: #E7DCC8' '--color-ink: #221A14' '--color-ink-soft: #6B5E4F' '--color-shutter: #FF5A3C' '--color-shutter-ink: #221A14' '--color-danger: #C81E3A' '--color-viewfinder: #1E9E8C' '--color-token-gold: #FFC531' '--color-neon-violet: #7B5CF0' '--color-flash-pink: #FF3D9A' '--color-lens-blue: #2F80ED' '--color-darkroom-violet: #8B5CF6' '--color-flash-lime: #D6F24C' 'Space Grotesk' '--background: #FBF1E4' '--foreground: #221A14' '--card: #FFFFFF' '--card-foreground: #221A14' '--primary: #FF5A3C' '--primary-foreground: #221A14' '--secondary: #FFFFFF' '--muted: #E7DCC8' '--muted-foreground: #6B5E4F' '--destructive: #C81E3A' '--border: #E7DCC8' '--input: #E7DCC8' '--ring: #FF5A3C' '--radius: 0.625rem' '--font-mono'; do grep -qF "$t" src/index.css || { echo "MISSING: $t"; exit 1; }; done && for s in '0a0a08' 'f5c518' 'e0533d' 'eae5d6' 'e3e0d8' '6b665c' 'FFF8EF' 'FF5A47' '1D1626' '5B5548' 'F0E4D3' '--color-signal' '--color-paper-warm' '--color-coral' '--color-cream' '--color-ruby' '--color-ink-muted' '--color-card-border' 'viewfinder-blue'; do if grep -iqF "$s" src/index.css; then echo "RETIRED VALUE PRESENT IN index.css: $s"; exit 1; fi; done && echo TOKENS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all exit 0.
    - `index.html` contains the literal `<html lang="en">` and zero occurrences of `class="dark"`.
    - `src/main.tsx` contains all four font imports: `@fontsource/space-grotesk/700.css`, `@fontsource/inter/400.css`, `@fontsource/inter/600.css`, `@fontsource/ibm-plex-mono/400.css`.
    - `grep -rcF 'fontsource-variable' src/ package.json` returns 0 matches (D-15).
    - `package.json` `dependencies` contains `@fontsource/space-grotesk`, and `slopcheck install --ecosystem npm @fontsource/space-grotesk` returned `[OK]` (recorded in the plan summary).
    - The Revision 4 audit was performed and its result recorded in the summary: `01-UI-SPEC.md` Revision 4's Color and Typography tables were compared against `DESIGN.md` §3/§4 and found identical to the Token migration table above, or the discrepancy was reported rather than silently resolved.
    - All fifteen Revision 4 `@theme` color declarations are present verbatim in `src/index.css`, in uppercase hex: `--color-paper: #FBF1E4`, `--color-paper-raised: #FFFFFF`, `--color-paper-border: #E7DCC8`, `--color-ink: #221A14`, `--color-ink-soft: #6B5E4F`, `--color-shutter: #FF5A3C`, `--color-shutter-ink: #221A14`, `--color-danger: #C81E3A`, `--color-viewfinder: #1E9E8C`, `--color-token-gold: #FFC531`, `--color-neon-violet: #7B5CF0`, `--color-flash-pink: #FF3D9A`, `--color-lens-blue: #2F80ED`, `--color-darkroom-violet: #8B5CF6`, `--color-flash-lime: #D6F24C`.
    - `src/index.css` contains zero case-insensitive occurrences — anywhere in the file, including every comment — of `0a0a08`, `f5c518`, `e0533d`, `eae5d6`, `e3e0d8`, `6b665c`, `FFF8EF`, `FF5A47`, `1D1626`, `5B5548`, `F0E4D3`, `--color-signal`, `--color-paper-warm`, `--color-coral`, `--color-cream`, `--color-ruby`, `--color-ink-muted`, `--color-card-border`, or `viewfinder-blue`.
    - `src/index.css` contains the literal `Space Grotesk` inside the `--font-heading` declaration.
    - `src/index.css` `:root` contains all eighteen semantic hex declarations listed in Step 6, with no trailing hex-reference comments on any of them.
    - `--radius: 0.625rem` and `--font-mono` are still present and unchanged.
    - `src/index.css` contains no line matching `^\.dark\s*\{` (the block is deleted) but still contains `@custom-variant dark`.
  </acceptance_criteria>
  <done>The DESIGN.md palette and type system are the app's actual defaults: warm paper background, ink foreground, shutter primary, ruby danger, three reserved accents and four Landing Gradient Wash colors available as utilities, Space Grotesk backing `font-heading`, Inter 600 still loaded, no forced color-mode class on the document root, and no dead `.dark` block carrying retired values.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the shell chrome and PlaceholderCard to the DESIGN.md treatment</name>
  <files>src/components/AppShell.tsx, src/components/NavBar.tsx, src/components/MobileNavSheet.tsx, src/components/PlaceholderCard.tsx</files>
  <read_first>
    - `src/components/AppShell.tsx` — current: outer `div` on `bg-ink`, `NavBar`, `main`, a `Separator` with `bg-paper-border/20`, and a footer with `opacity-30` plus `text-paper-warm`
    - `src/components/NavBar.tsx` — current: `bg-ink` header, `text-signal` wordmark, `text-paper-warm` links, `border-signal` active state via `NavLink`'s `isActive` render prop, the four-entry `NAV_LINKS` array
    - `src/components/MobileNavSheet.tsx` — current: `Sheet`/`SheetTrigger`/`SheetContent`/`SheetTitle`/`SheetClose` composition, `h-11 w-11` trigger with `aria-label="Open navigation menu"`, `border-paper-border bg-ink` content, `text-paper-warm` links
    - `src/components/PlaceholderCard.tsx` — current: `{ heading, body }` props, `border-paper-border bg-paper text-ink` card, `font-semibold` title, `text-ink-soft` body
    - `src/components/ui/button.tsx` and `src/components/ui/card.tsx` — confirm the `variant`, `size`, and `asChild` prop names and which base classes need overriding at the call site (the `button` base ships `text-sm font-medium`)
    - `src/index.css` as revised by Task 1 — confirm the emitted utility names `bg-paper`, `bg-paper-raised`, `border-paper-border`, `bg-ink`, `text-ink`, `text-ink-soft`, `text-shutter`, `border-shutter`, `outline-shutter`, `shadow-ink`
    - `DESIGN.md` — section 5 (spacing and the 44x44px floor), section 7 (Navigation), section 9 (Cards — note the "interactive only" hover rule), section 18 (Interaction States), section 20 (Accessibility)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` **Revision 4** — Component Styling Approach, Layout and Breakpoints (Navigation pattern and Footer), Page shell paragraph (all unchanged from Revision 3)
    - This plan's **Two token traps**, **Typography role to Tailwind class map**, **Three documented reconciliations**, and **Wordmark decision** sections
  </read_first>
  <action>
    Change only styling, plus one additive optional prop on `PlaceholderCard`. Do not alter any
    component's structural JSX, its export name, the `NAV_LINKS` array contents, the
    `Sheet`/`SheetClose` composition, or the `Outlet` placement. `AppShell` remains the single nested
    layout route (D-04). Do not edit anything under `src/components/ui/`.

    `AppShell.tsx`. Change the outer `div`'s `bg-ink` to `bg-paper` so the page surface is warm paper
    and the dark surfaces become contained islands rather than the whole interface — this is the
    single highest-impact change in the plan (`DESIGN.md` section 3: `ink` is nav/footer chrome, not
    the page). Delete the `Separator` element and its import: with an ink footer sitting directly
    against a paper `main`, the divider is both redundant and the wrong color, and leaving an unused
    import would fail `noUnusedLocals` under D-10. Give the `footer` element `bg-ink`, keep its
    `py-4 text-center`, and remove the `opacity-30` from the wrapper so the ink surface stays solid;
    move the reduction onto the text itself. The inner paragraph becomes the Label role at ~30%
    opacity per `01-UI-SPEC.md`'s Footer rule:
    `font-mono text-[13px] leading-[1.4] font-normal tracking-[0.08em] text-paper/30 uppercase`.
    Keep the footer text exactly `PinchPop`. Do not add the PuzzleCam/Unnati attribution back.

    `NavBar.tsx`. Keep `bg-ink` on the `header`, and keep `h-16` (the 64px height `DESIGN.md` section
    7 specifies), `max-w-[1200px]`, and the existing responsive padding. Wordmark: per the **Wordmark
    decision** in this plan's context, replace the mono Label classes
    (`font-mono text-[13px] leading-[1.4] font-normal tracking-[0.08em] uppercase`) with the Display
    role: `font-heading text-[22px] leading-[1] font-bold`. Change `text-signal` to `text-shutter`.
    Keep the `PINCHPOP` text exactly as-is (do not change casing — Space Grotesk 700 at this size reads
    as a wordmark without needing the uppercase-mono treatment). This is the one interactive element in
    `NavBar.tsx`/`MobileNavSheet.tsx` that intentionally carries `font-heading`/`font-bold` instead of
    the mono Label role — every nav *link* stays mono, only the wordmark changes. Desktop links:
    change `text-paper-warm` to `text-paper` and the active-state `border-signal` to
    `border-shutter`, keeping `border-b-2` and the `border-transparent` inactive pairing driven by
    `NavLink`'s `isActive` render prop. Add `flex min-h-11 items-center` to each desktop link so it
    clears the 44px touch floor `DESIGN.md` section 20 requires "throughout, not just on the mobile
    nav." Add the shutter focus ring to the wordmark and to every desktop link:
    `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-shutter`.

    `MobileNavSheet.tsx`. Trigger button: keep `variant="ghost"`, `size="icon"`, the
    `aria-label="Open navigation menu"`, and the `h-11 w-11` override that satisfies the 44px touch
    floor — those are accessibility requirements, not styling. Change its
    `text-paper-warm hover:text-signal` to `text-paper hover:text-shutter` and add
    `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-shutter`.
    `SheetContent`: change `border-paper-border bg-ink` to `border-ink bg-ink` — the drawer is on the
    ink surface per `DESIGN.md` section 7, so a warm paper border would read as a seam. Drawer links:
    change `text-paper-warm` to `text-paper`, add `flex min-h-11 items-center` and the same shutter
    focus-ring classes, keep every other Label class, keep each link wrapped in `SheetClose`, and keep
    the `sr-only` `SheetTitle` — Radix Dialog requires that accessible name.

    `PlaceholderCard.tsx`. Extend the props interface additively so the five existing two-prop call
    sites keep compiling: add `icon?: LucideIcon`, importing the `LucideIcon` type from
    `lucide-react` as a type-only import. There is **no** `iconColor` prop and **no**
    `iconChipClassName` prop — decorative color is Home-only per `DESIGN.md` section 10 and section
    21, so stub icons have no color variety to parameterize. When `icon` is supplied, render it inside
    `CardHeader` above the title as the icon component with `className="size-6 text-ink"` and
    `aria-hidden="true"` — it is decorative and the heading already names the route. The glyph is
    always `text-ink`; never a wash color, never white, never inside a colored chip.

    Restyle the card surface per `DESIGN.md` section 9's info/stat card family: change `bg-paper` to
    `bg-paper-raised` (see the token trap — `bg-paper` now means the warm page background, which
    would make the card invisible against the page), keep `text-ink`, use `rounded-2xl`, and change
    `border border-paper-border` to `border-[1.5px] border-paper-border`. Add a soft ambient shadow
    tinted with ink rather than pure gray or black: `shadow-xl shadow-ink/8`.

    Do **not** add a hover lift or rotate to this card, and do not add any `transition-*` or
    `motion-reduce:*` utility to it — see reconciliation 1 in this plan's context. `DESIGN.md` section
    9 restricts that treatment to interactive cards; these five stub cards are static informational
    surfaces and a hover transform would falsely signal clickability.

    Title takes the Heading role — `font-heading text-[22px] leading-[1.15] font-bold md:text-[30px] text-ink`
    — replacing the current `font-semibold`/`md:text-[28px]`, which used a weight that Space Grotesk
    never loads and a size that does not match `DESIGN.md`'s 30/22 scale. Body takes the Body role:
    `font-sans text-base leading-[1.6] font-normal text-ink-soft`. Keep the outer centering wrapper's
    `min-h-[calc(100vh-4rem)]` and its padding unchanged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint && npm run typecheck && npm run format:check && npm run build && CH="src/components/AppShell.tsx src/components/NavBar.tsx src/components/MobileNavSheet.tsx src/components/PlaceholderCard.tsx" && if grep -rEni 'signal|coral|cream|ruby|ink-muted|card-border|paper-warm|viewfinder-blue|iconChipClassName|iconColor|polaroid|0a0a08|f5c518|e0533d|FFF8EF|FF5A47|1D1626|5B5548|F0E4D3' $CH; then echo "RETIRED VALUE OR PATTERN IN CHROME"; exit 1; fi && if grep -rEn 'font-medium|font-light|font-thin|font-extralight|font-extrabold|font-black' $CH; then echo "FORBIDDEN FONT WEIGHT IN CHROME"; exit 1; fi && if grep -Eqn 'font-bold|font-semibold' src/components/AppShell.tsx src/components/MobileNavSheet.tsx; then echo "NON-400 WEIGHT ON A MONO-ONLY FILE"; exit 1; fi && if [ "$(grep -c 'font-bold' src/components/NavBar.tsx)" -ne 1 ]; then echo "NAVBAR MUST CARRY font-bold EXACTLY ONCE (THE WORDMARK)"; exit 1; fi && if grep -qF 'font-semibold' src/components/NavBar.tsx; then echo "UNEXPECTED font-semibold IN NAVBAR"; exit 1; fi && if grep -rqF 'PuzzleCam' src/; then echo "ATTRIBUTION REINTRODUCED"; exit 1; fi && if grep -qF 'Separator' src/components/AppShell.tsx; then echo "SEPARATOR NOT REMOVED"; exit 1; fi && if grep -qEn 'hover:-translate-y|hover:rotate-' src/components/PlaceholderCard.tsx; then echo "HOVER TRANSFORM ON A NON-INTERACTIVE CARD"; exit 1; fi && for p in 'bg-paper:src/components/AppShell.tsx' 'bg-ink:src/components/AppShell.tsx' 'text-paper/30:src/components/AppShell.tsx' 'font-mono:src/components/AppShell.tsx' 'bg-ink:src/components/NavBar.tsx' 'h-16:src/components/NavBar.tsx' 'font-heading:src/components/NavBar.tsx' 'text-[22px]:src/components/NavBar.tsx' 'text-shutter:src/components/NavBar.tsx' 'border-shutter:src/components/NavBar.tsx' 'outline-shutter:src/components/NavBar.tsx' 'min-h-11:src/components/NavBar.tsx' 'aria-label="Open navigation menu":src/components/MobileNavSheet.tsx' 'h-11 w-11:src/components/MobileNavSheet.tsx' 'SheetClose:src/components/MobileNavSheet.tsx' 'sr-only:src/components/MobileNavSheet.tsx' 'hover:text-shutter:src/components/MobileNavSheet.tsx' 'outline-shutter:src/components/MobileNavSheet.tsx' 'bg-ink:src/components/MobileNavSheet.tsx' 'LucideIcon:src/components/PlaceholderCard.tsx' 'bg-paper-raised:src/components/PlaceholderCard.tsx' 'border-[1.5px] border-paper-border:src/components/PlaceholderCard.tsx' 'rounded-2xl:src/components/PlaceholderCard.tsx' 'shadow-xl shadow-ink/8:src/components/PlaceholderCard.tsx' 'size-6 text-ink:src/components/PlaceholderCard.tsx' 'aria-hidden:src/components/PlaceholderCard.tsx' 'font-heading:src/components/PlaceholderCard.tsx' 'md:text-[30px]:src/components/PlaceholderCard.tsx' 'text-ink-soft:src/components/PlaceholderCard.tsx'; do s="${p%:*}"; f="${p##*:}"; grep -qF "$s" "$f" || { echo "MISSING '$s' IN $f"; exit 1; }; done && grep -qF 'text-paper' src/components/NavBar.tsx && grep -qF 'text-paper' src/components/MobileNavSheet.tsx && echo CHROME_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all exit 0.
    - Zero case-insensitive occurrences across the four files of: `signal`, `coral`, `cream`, `ruby`, `ink-muted`, `card-border`, `paper-warm`, `viewfinder-blue`, `iconChipClassName`, `iconColor`, `polaroid`, or any retired hex (`0a0a08`, `f5c518`, `e0533d`, `FFF8EF`, `FF5A47`, `1D1626`, `5B5548`, `F0E4D3`).
    - Zero occurrences of `font-medium`, `font-light`, `font-thin`, `font-extralight`, `font-extrabold`, or `font-black` across the four files.
    - Zero occurrences of `font-bold` or `font-semibold` in `AppShell.tsx` or `MobileNavSheet.tsx` — those two files render only IBM Plex Mono Label text, weight 400 exclusively. `NavBar.tsx` is the one exception: it carries `font-bold` exactly once, on the Space Grotesk wordmark (see **Wordmark decision**), and carries no `font-semibold` anywhere — every nav link stays mono/400.
    - `AppShell.tsx`: the outer container uses `bg-paper`; the `footer` uses `bg-ink`; the footer text uses `text-paper/30` with the full Label class set; `grep -c 'Separator' src/components/AppShell.tsx` returns 0 (element and import both removed).
    - `grep -rc 'PuzzleCam' src/` returns 0 — the removed attribution stays removed.
    - `NavBar.tsx` retains `bg-ink` and `h-16`, and contains `font-heading`, `text-[22px]`, `text-shutter`, `text-paper`, `border-shutter`, `outline-shutter`, and `min-h-11`; the `NAV_LINKS` array still has exactly four entries with `to` values `/`, `/game`, `/gallery`, `/profile`.
    - `MobileNavSheet.tsx` still contains `aria-label="Open navigation menu"`, `h-11 w-11`, `SheetClose`, and an `sr-only` `SheetTitle`, and now contains `text-paper`, `hover:text-shutter`, and `outline-shutter`.
    - `PlaceholderCard.tsx` still exports the named `PlaceholderCard`; its props interface keeps `heading` and `body` as required strings with `icon` optional and no other new prop — the five existing two-prop call sites compile unchanged.
    - `PlaceholderCard.tsx` contains `LucideIcon`, `bg-paper-raised`, `border-[1.5px] border-paper-border`, `rounded-2xl`, `shadow-xl shadow-ink/8`, `size-6 text-ink`, `aria-hidden`, `font-heading`, `md:text-[30px]`, and `text-ink-soft`.
    - `PlaceholderCard.tsx` contains zero occurrences of `hover:-translate-y` or `hover:rotate-` — the card is deliberately flat per `DESIGN.md` section 9.
  </acceptance_criteria>
  <done>The shell renders warm paper everywhere except the nav bar and footer, which remain contained ink surfaces with shutter accents and shutter focus rings; the placeholder card is a flat paper-raised surface with an ink-tinted ambient shadow and an optional ink-only icon.</done>
</task>

<task type="auto">
  <name>Task 3: Apply ink-only icons to the five stub routes, add the /game puzzle-grid motif, and calm the 404</name>
  <files>src/pages/GamePage.tsx, src/pages/ResultsPage.tsx, src/pages/GalleryPage.tsx, src/pages/ProfilePage.tsx, src/pages/SharePage.tsx, src/pages/NotFoundPage.tsx</files>
  <read_first>
    - `src/pages/GamePage.tsx` — the canonical shape of a one-line `PlaceholderCard` consumer
    - `src/pages/ResultsPage.tsx`, `src/pages/GalleryPage.tsx`, `src/pages/ProfilePage.tsx` — same shape, to keep consistent
    - `src/pages/SharePage.tsx` — confirm the `useParams` usage and the `data-share-slug` wrapper attribute that plan 01-07's DEPLOY-02 check depends on
    - `src/pages/NotFoundPage.tsx` — current: direct shadcn `Card` composition with `border-danger/40`, a `text-danger` title, `bg-paper`, `text-ink-soft` body, and a `variant="secondary"` `Back to Home` button
    - `src/components/PlaceholderCard.tsx` as revised by Task 2 — confirm the exact new prop name `icon` and that there is no color prop
    - `src/components/ui/button.tsx` — confirm `variant`, `size`, `asChild`, and the `text-sm font-medium` base classes that need overriding
    - `DESIGN.md` — section 10 (decoration is reserved for Home/promotional surfaces), section 12 (puzzle-grid language is scoped to the Puzzle screen), section 21 (motif scoping)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` **Revision 4** — Route and Placeholder Page Inventory (per-route icon assignments, all in `ink`), Visual Motif item 6 (puzzle-grid), Copywriting Contract (all unchanged from Revision 3)
    - This plan's **Shared press-feedback recipe**, **Motif scoping**, and **Typography role to Tailwind class map** sections
  </read_first>
  <action>
    The five stub pages keep their existing structure and their locked heading and body strings
    verbatim, and gain exactly one new prop on their `PlaceholderCard`, with the icon imported from
    `lucide-react`: `GamePage` gets `icon={Grid3x3}`, `ResultsPage` gets `icon={Sparkles}`,
    `GalleryPage` gets `icon={GalleryHorizontal}`, `ProfilePage` gets `icon={Camera}`, `SharePage`
    gets `icon={Share2}`. These assignments come straight from `01-UI-SPEC.md`'s Route Inventory
    table. **No page passes a color, a chip class, or a tint of any kind** — every icon renders in
    `ink` via `PlaceholderCard`'s own fixed `text-ink` class. `SharePage` keeps its `useParams` call
    and its `data-share-slug` wrapper attribute untouched.

    `GamePage` additionally gets the puzzle-grid motif (`01-UI-SPEC.md` motif 6), which is scoped to
    `/game` and appears on no other route. Wrap the existing `PlaceholderCard` in a
    `relative` container and add one absolutely-positioned decorative layer before it, carrying
    `aria-hidden="true"`, `pointer-events-none`, `data-motif="puzzle-grid"`, and
    `absolute inset-0 flex items-center justify-center opacity-[0.06]`. Inside it, render an inline
    `svg` with `viewBox="0 0 300 300"` sized `size-[min(70vw,420px)]`, containing nine `rect` elements
    forming a 3x3 grid (100x100 cells at x/y of 0, 100, 200), each with `fill="none"`,
    `stroke="#221A14"`, and `strokeWidth="2"`. The stroke is `ink`, not a decorative color —
    `DESIGN.md` section 10 and section 21 reserve decorative color for Home. The 3x3 shape matches the
    legacy engine's `GRID` constant. Change nothing else in `GamePage`.

    `NotFoundPage` stays deliberately calm: `01-UI-SPEC.md`'s Route Inventory gives the 404 a paper
    background and a `danger` accent with no other decorative elements. Change the card surface from
    `bg-paper` to `bg-paper-raised` (see the token trap — `bg-paper` is now the page background) and
    add `rounded-2xl border-[1.5px]`, keeping `border-danger/40`. Keep the title's `text-danger` and
    move it to the Heading role
    (`font-heading text-[22px] leading-[1.15] font-bold md:text-[30px]`, replacing `font-semibold`
    and `md:text-[28px]`). Move the body to the Body role
    (`font-sans text-base leading-[1.6] font-normal text-ink-soft`).

    Keep `variant="secondary"` on the CTA and add `rounded-2xl border-[1.5px] border-ink` plus
    `font-sans text-base font-semibold` so it reads as `DESIGN.md` section 8's secondary treatment —
    paper-raised fill, 1.5px ink border, Inter 600 label, no shadow — rather than a second shutter
    CTA. Add `min-h-11 px-6` so it clears the 44px touch floor. It must also carry this plan's Shared
    press-feedback recipe, because `DESIGN.md` section 8 gives every button variant the same
    scale-on-press feedback:
    `transition-all duration-200 ease-out hover:scale-[1.03] active:scale-[0.97]` plus
    `motion-reduce:transition-none motion-reduce:hover:scale-100 motion-reduce:active:scale-100`,
    identical to the classes `HomePage`'s CTAs will use in Tasks 4 and 5. Do **not** add
    `hover:shadow-lg hover:shadow-shutter/25` here — the glow is primary-only and the secondary
    treatment is explicitly shadowless. Add
    `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-shutter` so the
    focus ring matches the rest of the app.

    Both 404 copy strings and the `Back to Home` label are unchanged. Do not add gradient blobs,
    corner brackets, halftone texture, an aperture ring, or a card-stack to this page — those are Home
    hero decoration only.

    Do not use `dangerouslySetInnerHTML` anywhere. No page renders `NavBar`, a footer, or wraps itself
    in `AppShell` — D-04 keeps all chrome in the layout route. Add no new dependencies (D-13).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint && npm run typecheck && npm run format:check && npm run build && PG="src/pages/GamePage.tsx src/pages/ResultsPage.tsx src/pages/GalleryPage.tsx src/pages/ProfilePage.tsx src/pages/SharePage.tsx src/pages/NotFoundPage.tsx" && if grep -rEni 'signal|coral|cream|ruby|ink-muted|card-border|paper-warm|viewfinder-blue|iconChipClassName|iconColor|polaroid|0a0a08|f5c518|e0533d|FFF8EF|FF5A47|1D1626|5B5548|F0E4D3' $PG; then echo "RETIRED VALUE OR PATTERN IN PAGES"; exit 1; fi && if grep -rEn 'font-medium|font-light|font-thin|font-extralight|font-extrabold|font-black' $PG; then echo "FORBIDDEN FONT WEIGHT"; exit 1; fi && if grep -rEn 'flash-pink|lens-blue|darkroom-violet|flash-lime|blur-3xl|radial-gradient|data-card-stack-item|data-motif="corner-brackets"|data-motif="aperture-ring"|data-motif="card-stack"|data-motif="halftone"' $PG; then echo "HOME-ONLY MOTIF LEAKED ONTO A NON-HOME ROUTE"; exit 1; fi && if grep -rEn 'dangerouslySetInnerHTML|AppShell' src/pages/; then echo "FORBIDDEN REF IN PAGES"; exit 1; fi && if grep -qF 'shadow-shutter/25' src/pages/NotFoundPage.tsx; then echo "PRIMARY GLOW LEAKED ONTO 404 CTA"; exit 1; fi && for p in 'icon={Grid3x3}:src/pages/GamePage.tsx' 'data-motif="puzzle-grid":src/pages/GamePage.tsx' '#221A14:src/pages/GamePage.tsx' 'aria-hidden:src/pages/GamePage.tsx' 'pointer-events-none:src/pages/GamePage.tsx' 'icon={Sparkles}:src/pages/ResultsPage.tsx' 'icon={GalleryHorizontal}:src/pages/GalleryPage.tsx' 'icon={Camera}:src/pages/ProfilePage.tsx' 'icon={Share2}:src/pages/SharePage.tsx' 'data-share-slug:src/pages/SharePage.tsx' 'useParams:src/pages/SharePage.tsx' 'bg-paper-raised:src/pages/NotFoundPage.tsx' 'border-danger/40:src/pages/NotFoundPage.tsx' 'text-danger:src/pages/NotFoundPage.tsx' 'font-heading:src/pages/NotFoundPage.tsx' 'md:text-[30px]:src/pages/NotFoundPage.tsx' 'text-ink-soft:src/pages/NotFoundPage.tsx' 'border-ink:src/pages/NotFoundPage.tsx' 'font-semibold:src/pages/NotFoundPage.tsx' 'min-h-11:src/pages/NotFoundPage.tsx' 'transition-all duration-200 ease-out:src/pages/NotFoundPage.tsx' 'hover:scale-[1.03]:src/pages/NotFoundPage.tsx' 'active:scale-[0.97]:src/pages/NotFoundPage.tsx' 'motion-reduce:hover:scale-100:src/pages/NotFoundPage.tsx' 'motion-reduce:active:scale-100:src/pages/NotFoundPage.tsx' 'outline-shutter:src/pages/NotFoundPage.tsx'; do s="${p%:*}"; f="${p##*:}"; grep -qF "$s" "$f" || { echo "MISSING '$s' IN $f"; exit 1; }; done && [ "$(grep -c '<rect' src/pages/GamePage.tsx)" -eq 9 ] && for s in 'Game screen coming soon' 'The gesture capture and puzzle engine lands in the next phase.' 'Results coming soon' 'Your photo and score will appear here once the capture flow is built.' 'Gallery coming soon' 'Your saved photos will live here once accounts and storage are built.' 'Profile coming soon' 'Your stats and achievements will appear here in a later phase.' 'Shared memory coming soon' 'Public share pages launch in a later phase.' 'Page not found.' "We couldn't find that page. Head back and keep exploring PinchPop." 'Back to Home'; do grep -rqF "$s" src/pages/ || { echo "MISSING COPY: $s"; exit 1; }; done && echo STUBS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all exit 0.
    - Zero case-insensitive occurrences across the six files of: `signal`, `coral`, `cream`, `ruby`, `ink-muted`, `card-border`, `paper-warm`, `viewfinder-blue`, `iconChipClassName`, `iconColor`, `polaroid`, or any retired hex.
    - Zero occurrences of `font-medium`, `font-light`, `font-thin`, `font-extralight`, `font-extrabold`, or `font-black` across the six files.
    - Zero occurrences across the six files of `flash-pink`, `lens-blue`, `darkroom-violet`, `flash-lime`, `blur-3xl`, `radial-gradient`, `data-card-stack-item`, or any Home-only `data-motif` value — the Landing Gradient Wash and the Home hero motifs never leave `/`.
    - Each stub page passes exactly its assigned icon and nothing else: `icon={Grid3x3}`, `icon={Sparkles}`, `icon={GalleryHorizontal}`, `icon={Camera}`, `icon={Share2}`.
    - `GamePage.tsx` contains `data-motif="puzzle-grid"`, `aria-hidden`, `pointer-events-none`, the literal stroke `#221A14`, and exactly nine `<rect` elements.
    - `SharePage.tsx` still imports `useParams` from `react-router-dom` and still emits `data-share-slug`.
    - `NotFoundPage.tsx` contains `bg-paper-raised`, `border-danger/40`, `text-danger`, `font-heading`, `md:text-[30px]`, `text-ink-soft`, and `border-ink` plus `font-semibold` and `min-h-11` on its CTA.
    - `NotFoundPage.tsx`'s `Back to Home` CTA carries the full shared press-feedback recipe — `transition-all duration-200 ease-out`, `hover:scale-[1.03]`, `active:scale-[0.97]`, `motion-reduce:hover:scale-100`, `motion-reduce:active:scale-100` — plus `outline-shutter`, and contains zero occurrences of `shadow-shutter/25`.
    - Every locked copy string for these six routes still appears verbatim under `src/pages/`.
    - Zero occurrences of `dangerouslySetInnerHTML` or `AppShell` under `src/pages/` (D-04).
  </acceptance_criteria>
  <done>The five stub routes each show a flat paper-raised card with a single ink-toned icon and no decorative color anywhere, `/game` alone carries the ink puzzle-grid motif, and the 404 stays calm with a ruby danger accent while matching the primary CTA's press feel without its glow.</done>
</task>

<task type="auto">
  <name>Task 4: Rebuild the Home hero — dominant headline, four decorative motifs, and the ink-bezelled booth visual</name>
  <files>src/pages/HomePage.tsx</files>
  <read_first>
    - `src/pages/HomePage.tsx` — the current hero. **Copy the existing premise paragraph string out verbatim before editing** so it can be reproduced byte-for-byte, em dash included; only the surrounding markup and classes change.
    - `src/components/ui/button.tsx` — confirm `variant`, `size`, `asChild`, and the `text-sm font-medium` base classes needing override
    - `src/index.css` as revised by Task 1 — confirm the emitted utilities `bg-flash-pink`, `bg-lens-blue`, `bg-darkroom-violet`, `border-lens-blue`, `bg-paper`, `bg-paper-raised`, `bg-ink`, `shadow-ink`, `shadow-shutter`, `text-ink`, `text-ink-soft`, `outline-shutter`
    - `DESIGN.md` — section 1 (there is always one hero; toolkit not checklist), section 3 (60/30/10 and `ink`'s chrome job), section 4 (Display 56/34; avoid mid-headline color accents and eyebrow labels), section 6 (centered arcade-kiosk composition, ~1200px max width, "screen inside a machine"), section 8 (Primary button recipe, exactly one primary action per screen), section 10 (decoration is gradients/shapes, never photography; Home is where it is allowed; no dependency on a hero photo), section 11 (Polaroid treatment is reserved for real captured photos), section 14 (HUD chrome only on Camera/Puzzle), section 19 (`prefers-reduced-motion` everywhere), section 21 (things to avoid)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` **Revision 4** — the Revision 4 Reconciliation Note, Visual Motif items 1 through 7, the Landing Gradient Wash table and its hard rules, the **Landing Page Treatment items 1 through 5** (the hero's share of the eight-item arc), Component Styling Approach, Copywriting Contract
    - `design-exploration/concept-01/Home.png` — **composition/proportion/layering study only.** Look specifically at: how much vertical space the headline block occupies relative to the fold; how the wide panel below the headline is proportioned (roughly 4:5 of the content width, with generous internal padding and a clearly framed interior); and how the objects inside that panel are set at three different scales with the largest in the centre. Do not read it for color, typography, or content.
    - `design-exploration/concept-01/home.html` — the same study at markup level, specifically the `<!-- DYNAMIC HERO WORKBENCH COMPOSITION -->` block: a `max-w-5xl mx-auto` outer panel with a border, generous responsive padding, large corner radius, a heavy shadow, and `overflow-hidden`, wrapping a centred cluster. **Read the structure, copy nothing** — no class name, no custom property, no literal value from that file enters `src/`.
    - This plan's **Stitch Concept 01 — what to learn, what never to copy**, **Motif scoping**, **Fabricated-content blacklist**, **Shared press-feedback recipe**, **Three documented reconciliations** (especially 3), and **Typography role to Tailwind class map** sections
  </read_first>
  <action>
    Rebuild `HomePage`'s hero. This task owns Revision 4 Landing Page Treatment **items 1 through 5**;
    Task 5 adds items 6 and 8 to the same file. Write the hero as its own top-level `<section>` inside
    a `<>` fragment so Task 5 can append two sibling sections without restructuring anything.

    Keep the headline `Capture. Solve. Remember.`, the existing premise paragraph reproduced
    byte-for-byte, and the `Start Playing` CTA wrapping a `react-router-dom` `Link` to `/game`. Do not
    reword, re-case, or split any of the three. Do not accent a word mid-headline with color or italic
    (`DESIGN.md` section 4 names that as a thing to avoid) and do not add an eyebrow label above the
    headline.

    **Hero section shell.** A `relative overflow-hidden` section with
    `flex min-h-[calc(100vh-4rem)] flex-col items-center justify-center px-4 py-16 text-center sm:py-24`.
    `overflow-hidden` is what prevents the blurred blobs from producing a horizontal scrollbar at
    375px — it is load-bearing, not cosmetic. Inside it, one `relative z-10 mx-auto w-full
    max-w-[1200px]` content column (`DESIGN.md` section 6 — centered, never edge-to-edge).

    Every decorative element in this task carries `aria-hidden="true"` and `pointer-events-none`, is
    absolutely positioned, and carries a `data-motif` attribute for verification. The five permitted
    `data-motif` values across the whole page are exactly `halftone`, `blobs`, `aperture-ring`,
    `corner-brackets`, and `card-stack`; a value may appear more than once (the halftone does), but no
    sixth value may be introduced.

    **Motif 1 of 5 — halftone dot-grid** (`01-UI-SPEC.md` motif 3, 4-6% opacity). An `absolute inset-0`
    div with `data-motif="halftone"` at `opacity-[0.05]`, using an inline `style` object with
    `backgroundImage: "radial-gradient(#221A14 1px, transparent 1px)"` and
    `backgroundSize: "16px 16px"`. No image asset, no `url(`. The value is a compile-time constant
    with no interpolation.

    **Motif 2 of 5 — Landing Gradient Wash blobs** (motif 4, 2-3 blobs, ~24-40% opacity, heavily
    blurred). An `absolute inset-0` wrapper with `data-motif="blobs"` containing three
    `absolute rounded-full blur-3xl` divs, each stepping up in size at `md:` so they hold their weight
    against the larger desktop composition:
    `size-72 bg-flash-pink/30 md:size-96` anchored `-top-24 -left-16`,
    `size-64 bg-darkroom-violet/30 md:size-80` anchored `-top-16 -right-20`, and
    `size-56 bg-lens-blue/25 md:size-72` anchored `bottom-0 left-1/2 -translate-x-1/2 translate-y-1/4`.
    All three sit above or below the text column's vertical band, never directly behind the premise
    paragraph — `01-UI-SPEC.md` flags text-over-blob as a contrast risk. These four wash colors appear
    on this page and nowhere else in the app. Flash Lime is deliberately not used (see the Token
    migration table's note) — do not add a fourth blob to "use it up."

    **The headline block.** Inside the content column, a `relative mx-auto w-full max-w-2xl` wrapper.
    This wrapper is the frame boundary for motif 3 below, so it must be a single explicit sizing
    element rather than four independently-placed decorations.

    - **Motif 3 of 5 — viewfinder corner brackets** (motif 1, Lens Blue, 2px stroke;
      Revision 4 item 3 scopes them to the headline + CTA block, so they belong on this wrapper, not
      on the booth panel). One `absolute inset-0` child with `data-motif="corner-brackets"` holding
      four `size-8 md:size-10` divs, each `absolute border-lens-blue` with a 2px stroke on exactly two
      adjacent edges and offset outward from the wrapper's edge by `-top-4 -left-4` / `-top-4 -right-4`
      / `-bottom-4 -left-4` / `-bottom-4 -right-4`: `border-t-2 border-l-2` top-left,
      `border-t-2 border-r-2` top-right, `border-b-2 border-l-2` bottom-left, `border-b-2 border-r-2`
      bottom-right. Sizing them off one shared wrapper is what makes them read as a single cohesive
      viewfinder frame — the most direct callback to the two-hand index-finger framing gesture that is
      the actual capture mechanic — rather than four unrelated decorative corners.

    - **Motif 4 of 5 — camera-aperture ring** (motif 2, 10-15% opacity, Darkroom Violet stroke;
      Revision 4 item 2 places it centered behind the headline). Put the `h1` inside its own
      `relative flex items-center justify-center` wrapper and place the ring layer inside **that**
      wrapper, not the outer one. Doing it structurally is what mechanically guarantees the ring
      cannot sit behind the premise paragraph. The ring layer carries `data-motif="aperture-ring"`,
      `absolute`, and `opacity-[0.14]`, holding an inline `svg` with `viewBox="0 0 400 400"` sized
      `size-[min(88vw,640px)]` with three concentric `circle` elements at `cx="200"` `cy="200"` and
      radii `190`, `150`, `110`, each `fill="none"`, `stroke="#8B5CF6"`, `strokeWidth="2"`. This is a
      decorative suggestion of an iris, not a literal aperture, and it is larger than the previous
      draft on purpose: `DESIGN.md` section 1 requires the hero to actually dominate, and a ring
      smaller than the headline reads as a stray circle.

    - **Headline.** Display role plus a narrow measure so it wraps to two or three lines and reads as a
      slab rather than a single thin line — this is the single biggest lever on hero dominance, since
      `DESIGN.md` section 4 fixes the Display size at 56/34 and forbids going larger:
      `relative z-10 mx-auto max-w-[15ch] text-balance font-heading text-[34px] leading-[1.05] font-bold tracking-[-0.02em] text-ink md:text-[56px]`.
      Sentence case exactly as locked — do not uppercase it. (Concept 01's headline is uppercase; in
      PinchPop uppercase belongs to the mono Label role only, per `DESIGN.md` sections 4 and 21.)

    - **Premise paragraph.** Body role, subordinate, centered under the headline:
      `mx-auto mt-6 max-w-xl font-sans text-base leading-[1.6] font-normal text-ink-soft`. The current
      `font-semibold` and `text-paper-warm` on this element are retired artifacts and must both go.
      The string itself does not change by a single character.

    - **CTA.** `mt-8`, shadcn `Button` with `asChild` and `size="lg"`, wrapping the existing
      `Link to="/game"` labelled `Start Playing`. Class list:
      `rounded-2xl min-h-11 px-8 font-sans text-base font-semibold` (Inter 600 button label per
      `DESIGN.md` section 4; `min-h-11` and the generous horizontal padding satisfy the 44px touch
      floor in section 20; `text-base font-semibold` override the button base's `text-sm font-medium`),
      then `transition-all duration-200 ease-out hover:scale-[1.03] hover:shadow-lg hover:shadow-shutter/25 active:scale-[0.97]`,
      then `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-shutter`, then
      `motion-reduce:transition-none motion-reduce:hover:scale-100 motion-reduce:active:scale-100`. The
      shutter fill and shutter-ink label come from the `--primary` and `--primary-foreground` tokens
      Task 1 set, so the default `Button` variant already produces them — do not hand-code a background
      color, and do not edit `src/components/ui/button.tsx`.

    **Motif 5 of 5 — the booth panel** (`data-motif="card-stack"`, Revision 4 item 5). This replaces
    the previous draft's three tiny rectangles. It is the hero's supporting visual and it must have
    real weight: an actual PinchPop photobooth object, not a faint shape behind text. Place it after
    the headline block, still inside the content column, at `mt-16` (`mx-auto w-full max-w-[960px]`).

    - **Outer bezel.** `relative mx-auto w-full max-w-[960px] rounded-[28px] bg-ink p-3 shadow-2xl shadow-ink/25 md:p-4`.
      This is `DESIGN.md` section 6's arcade-cabinet bezel rendered literally — the machine that
      contains the screen — and section 3 lists chrome frames among `ink`'s jobs. See reconciliation 3:
      it is **not** the Camera viewport and it carries **no** HUD content of any kind.
    - **Print-ejection slot.** One `absolute bottom-1.5 left-1/2 h-1 w-28 -translate-x-1/2 rounded-full bg-paper/20 md:bottom-2`
      div on the bezel. A physical detail of the object — a chute the prints come out of. It is a
      shape, not a readout; it carries no text.
    - **Interior "screen".** `relative flex h-[300px] items-center justify-center overflow-hidden rounded-[18px] bg-paper md:h-[420px]`.
      The explicit height is the point: this is what makes the visual substantial rather than
      decorative. Inside it, first render a second halftone layer — `absolute inset-0`,
      `data-motif="halftone"`, `aria-hidden="true"`, `pointer-events-none`, `opacity-[0.06]`, same
      inline `radial-gradient` recipe as motif 1 — so the interior reads as textured paper rather than
      an empty box. Reusing the `halftone` value is intentional and permitted; do not invent a new
      `data-motif` value for it.
    - **The graduated print cluster.** Three flat cards inside the interior, at three different scales,
      three different rotations, and two z-layers — this is the Concept 01 "objects at graduated
      scales, largest in the centre" learning, reinterpreted entirely in PinchPop tokens. Each card
      carries the attribute `data-card-stack-item` and its **entire `className` must be a single string
      literal on one line** (no template-literal concatenation, no `cn()` call splitting it):
      - back left: `data-card-stack-item` + `absolute z-0 h-28 w-40 -translate-x-[120px] -rotate-[8deg] rounded-xl bg-paper-raised shadow-lg shadow-ink/10 md:h-40 md:w-56 md:-translate-x-[200px]`
      - back right: `data-card-stack-item` + `absolute z-0 h-24 w-36 translate-x-[125px] rotate-[9deg] rounded-xl bg-paper-raised shadow-lg shadow-ink/10 md:h-36 md:w-48 md:translate-x-[205px]`
      - front centre (largest): `data-card-stack-item` + `relative z-10 h-40 w-56 rotate-[2deg] overflow-hidden rounded-xl bg-paper-raised shadow-2xl shadow-ink/20 md:h-56 md:w-80`
      Inside the front card only, one child div fills it with a Landing Gradient Wash so the largest
      element is not a blank rectangle: `absolute inset-0 bg-gradient-to-br from-flash-pink/25 via-darkroom-violet/20 to-lens-blue/25`.
      `01-UI-SPEC.md` motif 5 explicitly authorizes "a low-opacity Landing Gradient Wash fill" here, and
      `DESIGN.md` section 10 explicitly prescribes "a plain color/gradient card in the same frame shape
      rather than a placeholder image" for exactly this situation.
    - **The no-Polaroid rule, restated because a previous revision broke it.** These are plain flat
      cards. No `border` utility, no white print frame, no caption zone, no tape accent, no bottom-heavy
      padding, and a deliberately **landscape** ratio (every card is wider than it is tall) rather than
      a portrait instant-film ratio. `DESIGN.md` sections 11 and 21 reserve the Polaroid/print treatment
      exclusively for actual captured photos, which do not exist anywhere in Phase 1 — styling a
      contentless decorative shape as a Polaroid violates that rule in its own right, independent of the
      Home-decoration allowance. The motif still reads as "a stack of photos is coming," which is the
      whole intent. This task's verify asserts that mechanically, and it does so on whole JSX
      opening tags rather than on single physical lines: it joins the file into one stream,
      extracts each complete tag carrying `data-card-stack-item`, and fails if any extracted tag
      contains a `border-`, a `pb-<n>`, or a `rounded-b` utility. Whole-tag extraction is
      mandatory here, not stylistic: at the repo's `printWidth: 100` Prettier always splits an
      element carrying this marker plus a ~150-character `className` into one-attribute-per-line
      form, leaving `data-card-stack-item` alone on its own line with no class text on it, so a
      line-scoped grep would match only the bare marker and could never see the violation. The
      gate additionally fails if it cannot extract three complete tags (marker plus a closed
      `className="..."`) — a check that has gone blind is treated as a failure, never as a pass.
    - **What must NOT go inside the booth.** No status dot, no `REC`/`READY`/`LIVE` label, no frame
      counter, no timestamp, no mono readout, no progress bar, no `shutter`-colored element of any kind
      (`shutter` is reserved for the primary CTA, the active nav underline, and focus rings), no
      `lucide-react` icon, and no puzzle-grid outline (that motif is `/game`-only). Reconciliation 3
      explains why: the moment the bezel acquires a readout it becomes HUD chrome, which
      `DESIGN.md` sections 14 and 21 scope to Camera and Puzzle only.

    **Absolute prohibitions for this file** (Revision 4 item 7 and `DESIGN.md` section 10): no `<img>`
    element, no `url(` reference, no `background-image` pointing at a file, no import or mention of
    anything under `design-exploration/`, no photograph, no stock-image placeholder, and no fabricated
    proof content — no counts, no scores, no testimonials, no activity feed, no telemetry strings. See
    the **Fabricated-content blacklist**. The screen must read as complete using only abstract
    decoration.

    Add no new dependencies (D-13); every motif is CSS gradients and inline SVG strokes. Do not import
    the legacy `css/styles.css`. Do not use `dangerouslySetInnerHTML`. Do not render `NavBar`, a
    footer, or wrap the page in `AppShell` — D-04 keeps all chrome in the layout route.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint && npm run typecheck && npm run format:check && npm run build && H=src/pages/HomePage.tsx && if grep -Eni 'signal|coral|cream|ruby|ink-muted|card-border|paper-warm|viewfinder-blue|iconChipClassName|iconColor|polaroid|0a0a08|f5c518|e0533d|FFF8EF|FF5A47|1D1626|5B5548|F0E4D3' $H; then echo "RETIRED VALUE OR PATTERN IN HOMEPAGE"; exit 1; fi && if grep -En 'font-medium|font-light|font-thin|font-extralight|font-extrabold|font-black|md:text-\[60px\]|text-\[36px\]|<img|dangerouslySetInnerHTML|AppShell|styles\.css|url\(|design-exploration' $H; then echo "FORBIDDEN CONSTRUCT IN HOMEPAGE"; exit 1; fi && if grep -Eni '[0-9],[0-9]{3}|\btestimonial|\breviews?\b|\btrusted by\b|\bhigh score\b|\bleaderboard\b|\bstreak\b|\btoken\b|\bpts\b|\bfps\b|\bplayers\b|\bsolved\b|\brecent captures\b' $H; then echo "FABRICATED PROOF CONTENT ON HOME"; exit 1; fi && if grep -Eni 'text-shutter|bg-shutter|border-shutter|data-motif="puzzle-grid"' $H; then echo "SHUTTER OUTSIDE THE CTA OR PUZZLE MOTIF LEAKED ONTO HOME"; exit 1; fi && for s in 'Capture. Solve. Remember.' 'Start Playing' 'to="/game"' 'overflow-hidden' 'max-w-[1200px]' 'font-heading' 'text-[34px]' 'md:text-[56px]' 'max-w-[15ch]' 'text-balance' 'tracking-[-0.02em]' 'text-ink-soft' 'data-motif="halftone"' 'radial-gradient(#221A14 1px, transparent 1px)' '16px 16px' 'data-motif="blobs"' 'blur-3xl' 'bg-flash-pink/30' 'bg-darkroom-violet/30' 'bg-lens-blue/25' 'md:size-96' 'data-motif="aperture-ring"' '#8B5CF6' 'viewBox="0 0 400 400"' 'size-[min(88vw,640px)]' 'opacity-[0.14]' 'data-motif="corner-brackets"' 'border-lens-blue' 'border-t-2' 'border-b-2' 'w-full max-w-2xl' 'md:size-10' 'data-motif="card-stack"' 'max-w-[960px]' 'rounded-[28px] bg-ink' 'shadow-2xl shadow-ink/25' 'rounded-[18px] bg-paper' 'h-[300px]' 'md:h-[420px]' 'bg-paper/20' 'data-card-stack-item' 'bg-paper-raised' 'rotate-[9deg]' 'shadow-ink/10' 'from-flash-pink/25' 'via-darkroom-violet/20' 'to-lens-blue/25' 'rounded-2xl' 'min-h-11' 'font-semibold' 'hover:scale-[1.03]' 'hover:shadow-shutter/25' 'active:scale-[0.97]' 'outline-shutter' 'motion-reduce:hover:scale-100' 'motion-reduce:active:scale-100' 'pointer-events-none'; do grep -qF "$s" $H || { echo "MISSING IN HOMEPAGE: $s"; exit 1; }; done && MOTIFS=$(grep -o 'data-motif="[a-z-]*"' $H | sort -u) && [ "$(printf '%s\n' "$MOTIFS" | wc -l)" -eq 5 ] && if printf '%s\n' "$MOTIFS" | grep -Ev 'data-motif="(halftone|blobs|aperture-ring|corner-brackets|card-stack)"'; then echo "UNDECLARED MOTIF VALUE ON HOME"; exit 1; fi && [ "$(grep -c 'data-card-stack-item' $H)" -eq 3 ] && CARDTAGS=$(tr '\n' ' ' < $H | grep -o '<[^<>]*data-card-stack-item[^<>]*>' || true) && if [ "$(printf '%s\n' "$CARDTAGS" | grep -c 'data-card-stack-item')" -ne 3 ] || [ "$(printf '%s\n' "$CARDTAGS" | grep -Ec 'className="[^"]*"')" -ne 3 ]; then echo "CARD-STACK GATE IS BLIND: could not extract 3 complete opening tags - fix the markup, do not weaken the gate"; exit 1; fi && if printf '%s\n' "$CARDTAGS" | grep -Eq 'border-|pb-[0-9]|rounded-b'; then echo "PRINT-FRAME STYLING ON A CARD-STACK ITEM"; exit 1; fi && [ "$(grep -c 'aria-hidden' $H)" -ge 5 ] && [ "$(grep -c '<circle' $H)" -eq 3 ] && [ "$(grep -c '<h1' $H)" -eq 1 ] && git diff --quiet src/router.tsx && echo HERO_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all exit 0.
    - Zero case-insensitive occurrences in `HomePage.tsx` of: `signal`, `coral`, `cream`, `ruby`, `ink-muted`, `card-border`, `paper-warm`, `viewfinder-blue`, `polaroid`, or any retired hex.
    - Zero occurrences of `font-medium` or any other forbidden weight, zero occurrences of `md:text-[60px]` or `text-[36px]` (the retired Display scale), zero `<img` elements, zero `url(`, zero `dangerouslySetInnerHTML`, zero `AppShell`, zero `styles.css`, and zero occurrences of the string `design-exploration` — no Stitch asset is referenced, imported, or linked.
    - **Fabricated-content gate (Revision 4 item 7):** zero case-insensitive matches in `HomePage.tsx` for `[0-9],[0-9]{3}`, `testimonial`, `review`/`reviews`, `trusted by`, `high score`, `leaderboard`, `streak`, `token`, `pts`, `fps`, `players`, `solved`, or `recent captures`. No photo, count, score, testimonial, or activity feed exists on the page.
    - **HUD gate (reconciliation 3):** zero occurrences of `text-shutter`, `bg-shutter`, or `border-shutter` in `HomePage.tsx` (the CTA gets its shutter fill from the `--primary` token via the default `Button` variant, never a hand-written shutter utility), and zero occurrences of `data-motif="puzzle-grid"`.
    - The headline uses `font-heading text-[34px] ... md:text-[56px]` — `DESIGN.md` section 4's 56/34 Display scale — plus `max-w-[15ch]`, `text-balance`, and `tracking-[-0.02em]` so it wraps to a dominant multi-line slab. Exactly one `<h1` exists on the page.
    - The premise paragraph is byte-identical to its pre-task content: `git diff src/pages/HomePage.tsx` shows no character change inside that string, only surrounding markup and classes.
    - Exactly five distinct `data-motif` values are present — `halftone`, `blobs`, `aperture-ring`, `corner-brackets`, `card-stack` — and no sixth value exists. At least five `aria-hidden` occurrences and at least one `pointer-events-none` are present.
    - Halftone layers contain the literal `radial-gradient(#221A14 1px, transparent 1px)` and `16px 16px`.
    - Blob layer contains `blur-3xl` with all three of `bg-flash-pink/30`, `bg-darkroom-violet/30`, `bg-lens-blue/25`, and steps up at `md:` (contains `md:size-96`).
    - Aperture-ring layer contains `viewBox="0 0 400 400"`, the stroke `#8B5CF6`, exactly three `<circle` elements, `size-[min(88vw,640px)]`, and `opacity-[0.14]` — within `01-UI-SPEC.md` motif 2's 10-15% band.
    - Corner-bracket layer contains `border-lens-blue` with both `border-t-2` and `border-b-2`, sizes at `md:size-10`, and its wrapper carries `w-full max-w-2xl` so the four brackets share one frame boundary.
    - **Booth panel:** `HomePage.tsx` contains `max-w-[960px]`, `rounded-[28px] bg-ink`, `shadow-2xl shadow-ink/25`, `rounded-[18px] bg-paper`, `h-[300px]`, `md:h-[420px]`, and `bg-paper/20` (the ejection slot) — the hero visual is a framed object at least 300px tall on mobile and 420px on desktop, not a decorative flourish.
    - **Graduated cluster:** exactly three elements carry `data-card-stack-item`; they use three different size pairs and the rotations include `rotate-[9deg]`; the front card carries the Landing Gradient Wash fill `from-flash-pink/25`, `via-darkroom-violet/20`, `to-lens-blue/25`.
    - **No-Polaroid gate:** the three complete JSX opening tags carrying `data-card-stack-item` — extracted whole-tag, not line-by-line, so the gate still fires after Prettier wraps every attribute onto its own line at `printWidth: 100` — contain no `border-` utility, no `pb-<n>` padding, and no `rounded-b` utility: no print-frame border, no bottom-heavy Polaroid proportions, no caption zone. If three complete tags (each with a closed `className="..."`) cannot be extracted, the gate exits non-zero rather than reporting success — it must never pass blind. Every card is landscape (width value greater than height value in each pair).
    - The CTA carries `rounded-2xl`, `min-h-11`, `font-semibold`, `hover:scale-[1.03]`, `hover:shadow-shutter/25`, `active:scale-[0.97]`, `outline-shutter`, `motion-reduce:hover:scale-100`, and `motion-reduce:active:scale-100`.
    - `git diff --quiet src/router.tsx` exits 0 — the route tree is untouched by this plan.
  </acceptance_criteria>
  <done>The Home hero has real visual weight: a dominant multi-line Space Grotesk headline inside a cohesive viewfinder-bracket frame with a large aperture ring behind it, ambient gradient blobs and halftone texture on warm paper, one shutter CTA, and below it an ink-bezelled photobooth panel at least 300px tall whose paper interior holds three flat, graduated, staggered print cards — a real PinchPop object, carrying no HUD readouts, no Polaroid framing, and no fabricated content.</done>
</task>

<task type="auto">
  <name>Task 5: Add the staggered "How PinchPop Works" band and the closing CTA reprise</name>
  <files>src/pages/HomePage.tsx</files>
  <read_first>
    - `src/pages/HomePage.tsx` as rebuilt by Task 4 — confirm the hero is a top-level `<section>` inside a fragment, and reuse its CTA class list verbatim for the reprise
    - `src/components/PlaceholderCard.tsx` as revised by Task 2 — copy its card recipe (`bg-paper-raised`, `rounded-2xl`, `border-[1.5px] border-paper-border`, `shadow-xl shadow-ink/8`, `size-6 text-ink` icon) rather than inventing a second card style
    - `DESIGN.md` — section 4 (the "How PinchPop Works 3-step flow" is the one content type numbered markers are authorized for; also: avoid eyebrow labels above headings), section 5 (the 4/8/16/24/32/48/64 spacing scale), section 6 (left-aligned reading columns; ~1200px centered), section 8 (exactly one primary action per screen), section 9 (info/stat cards are flat; lift only if interactive), section 19 (`prefers-reduced-motion`), section 21 (no numbered markers on anything that isn't a genuine sequence)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` **Revision 4** — the Revision 4 Reconciliation Note, and **Landing Page Treatment items 6, 7, and 8** (the How-It-Works section, the explicit deferral of supporting/proof content, and the closing CTA reprise)
    - `design-exploration/concept-01/Home.png` — **composition study only**, the "HOW PINCHPOP WORKS" band: note the section heading sitting left rather than centered, and that each card pairs a glyph with a numeral marker and a short body. Do not copy its 4-step count, its eyebrow label, its palette, or the "RECENT POLAROID CRACKS" proof section beneath it — the last of those is exactly what Revision 4 item 7 defers.
    - This plan's **Locked copy**, **Fabricated-content blacklist**, **Three documented reconciliations** (especially 1 and 2), **Shared press-feedback recipe**, and **Typography role to Tailwind class map** sections
  </read_first>
  <action>
    Append two sibling sections after the hero section inside `HomePage`'s fragment. Change nothing
    Task 4 wrote. Add no new `data-motif` value and no decorative layer to either section — Revision 4
    item 6 says the How-It-Works band introduces no new colors, and item 8 says the closing band
    introduces no new decorative motif and no new color.

    **Section A — "How PinchPop Works"** (Revision 4 item 6). Wrapper:
    `mx-auto w-full max-w-[1200px] px-4 py-16` (64px, the page-level step on `DESIGN.md` section 5's
    scale). Do not add an eyebrow label above the heading — `DESIGN.md` section 4 lists that as a
    thing to avoid, and Concept 01's band has one.

    Heading, left-aligned per `DESIGN.md` section 6's "left-aligned reading columns":
    `max-w-[16ch] font-heading text-[22px] leading-[1.15] font-bold text-ink md:text-[30px]`, text
    exactly `How PinchPop Works`. Use `<h2>`.

    Steps: an `<ol className="mt-10 grid gap-6 sm:grid-cols-3">` with three `<li>` children. `<ol>`/`<li>`
    is deliberate — this is a genuine ordered sequence, so the semantics carry the numbering to screen
    readers instead of leaving it purely visual.

    Each `<li>` uses the `PlaceholderCard` recipe so the page reuses one card language rather than
    inventing a second:
    `flex flex-col rounded-2xl border-[1.5px] border-paper-border bg-paper-raised p-6 shadow-xl shadow-ink/8`.
    On top of that, each card takes a **static vertical stagger** at `sm:` and up so the row reads as a
    deliberately composed sequence rather than three interchangeable tiles — this is the Concept 01
    "graduated/staggered elements" learning, reinterpreted as a layout offset on PinchPop's own spacing
    scale: card 1 `sm:translate-y-0`, card 2 `sm:translate-y-6` (24px), card 3 `sm:translate-y-12`
    (48px). This is a permanent layout offset, not an animation: it transitions nothing, is present at
    every moment, and therefore correctly carries no `transition-*` and no `motion-reduce:` variant
    (see reconciliation 1).

    Card interior, in order:

    1. A header row `flex items-center justify-between`:
       - An ink lucide glyph, `className="size-6 text-ink"`, `aria-hidden="true"` — step 1 `Aperture`,
         step 2 `Camera`, step 3 `GalleryHorizontal`. Do **not** use `Grid3x3` here: it is the `/game`
         route's icon and the puzzle-grid language is scoped to the Puzzle screen (`DESIGN.md`
         sections 12 and 21), so keeping it off Home removes any argument that the motif leaked.
       - A step-number marker: a `span` with
         `flex size-9 items-center justify-center rounded-full border-[1.5px] border-paper-border font-mono text-[13px] leading-[1.4] font-normal tracking-[0.08em] text-ink-soft`
         containing `01`, `02`, or `03`. Mono at 13px is the Label role exactly as
         `DESIGN.md` section 4 defines it — do not scale the numeral up to Display size, which would
         break the type contract. The ring marker is what gives it presence instead: it is a small
         echo of the hero's aperture ring, built only from `paper-border`, with no accent color and no
         filled chip (`DESIGN.md` section 15's color-filled chips are the badge system, reserved for
         Phase 7).
    2. Step title, Heading role at the smaller size:
       `mt-6 font-heading text-[22px] leading-[1.15] font-bold text-ink`.
    3. Step body, Body role: `mt-2 font-sans text-base leading-[1.6] font-normal text-ink-soft`.

    Use exactly this copy, reproduced byte-for-byte including the em dash — it is locked by this plan
    from here forward, and it restates the already-locked hero premise paragraph as three discrete
    beats, so it introduces no new product claim:

    | Marker | Title | Body |
    |---|---|---|
    | `01` | `Frame` | `Raise both hands to your webcam and frame the shot with your fingers — no mouse, no controller.` |
    | `02` | `Pinch to Capture` | `Bring thumb and index finger together to snap an instant photo.` |
    | `03` | `Solve & Save` | `Drag the scrambled puzzle back together with your hands to reveal your polaroid.` |

    These three cards are **static and non-clickable** — no hover lift, no hover rotate, no
    `transition-*`, no `<a>`/`<Link>`/`<button>` wrapper. Reconciliation 1 applies to them exactly as
    it applies to `PlaceholderCard`.

    **Nothing goes between Section A and Section B.** Revision 4 item 7 explicitly defers all
    supporting/proof content out of Phase 1: no recent-captures strip, no gallery preview, no
    testimonial row, no "N photos solved" counter, no logo wall, no activity ticker. Phase 1 has zero
    real captured photos and zero real user data, and this plan does not invent either. Concept 01's
    "RECENT POLAROID CRACKS" wall and its live arcade-feed ticker are precisely the content this item
    forbids — do not port them in any reduced form. This task's verify asserts the absence
    mechanically.

    **Section B — closing CTA reprise** (Revision 4 item 8). A quiet bookend band immediately before
    the `AppShell` footer. Wrapper: `border-t border-paper-border` on the section, and inside it
    `mx-auto flex w-full max-w-[1200px] flex-col items-center gap-6 px-4 py-16 text-center`. The
    hairline top border is `paper-border` doing its declared job (`DESIGN.md` section 3: hairline
    borders and dividers) — it is not a new motif and introduces no color.

    Content, in order:

    1. The already-locked headline restated at Heading role as the bookend line, in a `<p>` (not a
       heading element — repeating the `h1` text as an `h2` would put a duplicate entry in the document
       outline for no informational gain):
       `font-heading text-[22px] leading-[1.15] font-bold text-ink md:text-[30px]`, text exactly
       `Capture. Solve. Remember.`. See **Locked copy**: item 8 permits "no new copy string beyond the
       CTA text already locked," so reprising the locked headline is the only way to give this band a
       line of type. Do not write a new closing line.
    2. The same primary CTA, byte-identical to the hero's: shadcn `Button` with `asChild` and
       `size="lg"`, wrapping `<Link to="/game">Start Playing</Link>`, carrying the identical class list
       Task 4 used — `rounded-2xl min-h-11 px-8 font-sans text-base font-semibold`, the shutter-click
       recipe including `hover:shadow-lg hover:shadow-shutter/25`, the shutter focus ring, and all
       three `motion-reduce:` variants. Same action, same label, same target, same treatment.

    Per reconciliation 2, this is not a second primary action and it does not violate `DESIGN.md`
    section 8. Home therefore ends this task with **exactly two `Button` elements**, both invoking the
    one `Start Playing` → `/game` action, and **zero** buttons or links invoking any other action. Do
    not add a secondary variant, a "learn more," or a demo link beside either instance, and do not
    delete the reprise on section-8 grounds.

    Every prohibition from Task 4 still applies to the whole file: no `<img>`, no `url(`, no
    `design-exploration` reference, no fabricated proof content, no new dependency, no
    `dangerouslySetInnerHTML`, no `AppShell`, no legacy `css/styles.css` import.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint && npm run typecheck && npm run format:check && npm run build && H=src/pages/HomePage.tsx && if grep -Eni 'signal|coral|cream|ruby|ink-muted|card-border|paper-warm|viewfinder-blue|polaroid|0a0a08|f5c518|e0533d|FFF8EF|FF5A47|1D1626|5B5548|F0E4D3' $H; then echo "RETIRED VALUE IN HOMEPAGE"; exit 1; fi && if grep -En 'font-medium|font-light|font-thin|font-extralight|font-extrabold|font-black|<img|dangerouslySetInnerHTML|AppShell|styles\.css|url\(|design-exploration|Grid3x3' $H; then echo "FORBIDDEN CONSTRUCT IN HOMEPAGE"; exit 1; fi && if grep -Eni '[0-9],[0-9]{3}|\btestimonial|\breviews?\b|\btrusted by\b|\bhigh score\b|\bleaderboard\b|\bstreak\b|\btoken\b|\bpts\b|\bfps\b|\bplayers\b|\bsolved\b|\brecent captures\b' $H; then echo "FABRICATED PROOF CONTENT ON HOME"; exit 1; fi && for s in 'How PinchPop Works' '<ol' 'sm:grid-cols-3' 'sm:translate-y-6' 'sm:translate-y-12' 'rounded-2xl border-[1.5px] border-paper-border bg-paper-raised p-6 shadow-xl shadow-ink/8' 'size-9 items-center justify-center rounded-full border-[1.5px] border-paper-border' 'size-6 text-ink' 'Aperture' 'Camera' 'GalleryHorizontal' '01' '02' '03' 'Frame' 'Pinch to Capture' 'Solve & Save' 'Raise both hands to your webcam and frame the shot with your fingers' 'Bring thumb and index finger together to snap an instant photo.' 'Drag the scrambled puzzle back together with your hands to reveal your polaroid.' 'border-t border-paper-border' 'md:text-[30px]'; do grep -qF "$s" $H || { echo "MISSING IN HOMEPAGE: $s"; exit 1; }; done && [ "$(grep -c '<li' $H)" -eq 3 ] && [ "$(grep -c '<ol' $H)" -eq 1 ] && [ "$(grep -c '<h1' $H)" -eq 1 ] && [ "$(grep -c '<h2' $H)" -eq 1 ] && [ "$(grep -c '<Button' $H)" -eq 2 ] && [ "$(grep -cF 'to="/game"' $H)" -eq 2 ] && [ "$(grep -cF 'Start Playing' $H)" -eq 2 ] && [ "$(grep -cF 'Capture. Solve. Remember.' $H)" -eq 2 ] && [ "$(grep -cF 'hover:shadow-shutter/25' $H)" -eq 2 ] && if grep -En 'hover:-translate-y|hover:rotate-|<li[^>]*(onClick|href)' $H; then echo "STEP CARDS MUST BE STATIC AND NON-CLICKABLE"; exit 1; fi && MOTIFS=$(grep -o 'data-motif="[a-z-]*"' $H | sort -u) && [ "$(printf '%s\n' "$MOTIFS" | wc -l)" -eq 5 ] && [ "$(grep -c 'data-card-stack-item' $H)" -eq 3 ] && git diff --quiet src/router.tsx && echo ARC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all exit 0.
    - `HomePage.tsx` contains a `How PinchPop Works` `<h2>` (exactly one `<h2>` on the page) followed by exactly one `<ol>` with exactly three `<li>` children in a `sm:grid-cols-3` grid.
    - The three step cards use the `PlaceholderCard` recipe verbatim — the literal string `rounded-2xl border-[1.5px] border-paper-border bg-paper-raised p-6 shadow-xl shadow-ink/8` is present — and are visually staggered: `sm:translate-y-6` and `sm:translate-y-12` both appear.
    - Each step card contains an ink glyph (`size-6 text-ink`) and a ring-marker span carrying `size-9 items-center justify-center rounded-full border-[1.5px] border-paper-border` with a mono 13px numeral. Icons used are `Aperture`, `Camera`, and `GalleryHorizontal`; `Grid3x3` appears zero times in `HomePage.tsx` (the puzzle-grid language stays scoped to `/game`).
    - The three locked step titles (`Frame`, `Pinch to Capture`, `Solve & Save`) and their three locked bodies are reproduced byte-for-byte, em dash included.
    - The step cards are static: zero occurrences of `hover:-translate-y`, `hover:rotate-`, or an `<li>` carrying `onClick`/`href` — no card is clickable or lifts, per reconciliation 1.
    - **Deferral gate (Revision 4 item 7):** nothing sits between the How-It-Works band and the closing band; `HomePage.tsx` contains zero `<img` elements, zero `url(`, zero `design-exploration` references, and zero case-insensitive matches for `[0-9],[0-9]{3}`, `testimonial`, `review`/`reviews`, `trusted by`, `high score`, `leaderboard`, `streak`, `token`, `pts`, `fps`, `players`, `solved`, or `recent captures`.
    - **Closing band (Revision 4 item 8):** a section carrying `border-t border-paper-border` sits after the How-It-Works band, containing the reprised line `Capture. Solve. Remember.` at Heading role in a `<p>` and the second `Start Playing` CTA.
    - **Single-action gate (reconciliation 2):** `HomePage.tsx` contains exactly two `<Button` elements, exactly two `to="/game"` occurrences, exactly two `Start Playing` occurrences, exactly two `hover:shadow-shutter/25` occurrences, and exactly two `Capture. Solve. Remember.` occurrences. No third button, no secondary variant, no other action label exists on the page.
    - Task 4's hero is unchanged: exactly five distinct `data-motif` values and exactly three `data-card-stack-item` elements still present; exactly one `<h1>`.
    - `git diff --quiet src/router.tsx` exits 0.
  </acceptance_criteria>
  <done>Home renders the full Revision 4 page arc — hero, then a left-headed, vertically staggered three-step "How PinchPop Works" band with ring-marked mono numerals and ink glyphs, then nothing at all where proof content would go, then a quiet closing CTA reprise above the footer — with exactly one primary action offered twice and zero fabricated content anywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify the DESIGN.md / Revision 4 visual direction in a running browser</name>
  <files>none — verification only</files>
  <read_first>
    - `.planning/phases/01-app-shell-deployment-pipeline/01-VALIDATION.md` — the Manual-Only Verifications table (D-06 defers the test runner, so lint/typecheck/format/build are the only automated gates this phase)
    - `DESIGN.md` — sections 1, 3, 4, 6, 7, 10, 11, 14, 19, 20, 21, to compare what renders against the authoritative direction
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` **Revision 4** — the Revision 3 and Revision 4 Reconciliation Notes, the Color table, the Typography table, the Visual Motif section, and the full eight-item Landing Page Treatment
    - `.planning/phases/01-app-shell-deployment-pipeline/01-05-SUMMARY.md` — the record of why the previous checkpoint was stopped rather than approved, so the same failure mode is not repeated
    - This plan's **Three documented reconciliations** and **Wordmark decision** sections — the wordmark
      question, the closing-CTA-reprise question, and the booth-bezel question are all already resolved,
      so checks 2, 6, and 8 below are confirmations, not open decisions
  </read_first>
  <action>
    Pause execution and hand control to the developer. Run `npm run dev`, report the printed local
    URL, then present the `how-to-verify` checklist below verbatim and wait for the resume signal.

    Do not self-approve. Do not infer success from a clean build — the previous checkpoint on this
    phase was rejected on an implementation that passed every automated gate, because the defect was
    aesthetic. Do not skip this checkpoint on `workflow.auto_advance`; `01-VALIDATION.md` records
    these as manual-only verifications.

    If the developer reports defects, fix them in the owning file (`src/index.css` for tokens and
    fonts, `index.html` for the color mode, `src/components/NavBar.tsx` or `MobileNavSheet.tsx` for
    nav behavior, `src/components/AppShell.tsx` for the shell surfaces and footer,
    `src/components/PlaceholderCard.tsx` for the card treatment, `src/pages/HomePage.tsx` for the hero,
    the How-It-Works band, and the closing band, `src/pages/GamePage.tsx` for the puzzle-grid motif,
    `src/pages/NotFoundPage.tsx` for the 404), re-run all four quality gates, and re-present this
    checkpoint. Do not mark the plan complete on an unresolved defect.
  </action>
  <what-built>
    A full `DESIGN.md` / `01-UI-SPEC.md` Revision 4 visual pass over the existing Phase 1 shell: the
    `@theme` brand tokens and shadcn semantic variables swapped to the paper / ink / shutter / danger
    palette, with three reserved functional accents and four Home-only Landing Gradient Wash colors
    declared; Space Grotesk 700 installed and wired as the heading face, with Inter 400 and 600 for
    body and button labels and IBM Plex Mono 400 for labels; the forced color-mode class removed from
    the document root and the dead dark rule block deleted; the shell restyled so only the nav bar and
    footer remain ink islands on a warm paper page; `PlaceholderCard` given a paper-raised surface, an
    ink-tinted ambient shadow, and an optional ink-only icon; the five stub routes given ink icons with
    no decorative color; `/game` alone given the ink puzzle-grid motif; the 404 calmed to a ruby accent
    with matching press feedback and no glow.

    The Home page was rebuilt rather than restyled, to deliver Revision 4's full composition arc: a
    hero with a dominant multi-line Space Grotesk headline inside a cohesive viewfinder-bracket frame,
    a large aperture ring behind it, ambient gradient blobs and halftone texture, one shutter CTA, and
    a substantial ink-bezelled photobooth panel holding three flat graduated print cards; then a
    left-headed, vertically staggered three-step "How PinchPop Works" band; then deliberately nothing
    where proof content would sit; then a quiet closing CTA reprise above the footer.

    Unchanged on purpose: all seven routes, the `createBrowserRouter` route tree (`src/router.tsx`
    has a zero diff), the `AppShell` nested-layout architecture, every component boundary, and every
    locked copy string.

    Every automatable check has already run and passed: `npm run lint`, `npm run typecheck`,
    `npm run format:check`, and `npm run build`. What remains is visual and interaction verification,
    which `01-VALIDATION.md` records as manual-only for this phase because D-06 defers the test
    runner to a later phase.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev` and open the printed local URL (typically
    `http://localhost:5173`). Do a hard reload (Ctrl+Shift+R) so the new fonts and CSS are not served
    from cache. Then check the following.

    1. **The dark interface is gone; the base is warm paper.** Every page body is a warm off-white
       (`#FBF1E4` — noticeably creamy, not white and not grey). The only dark surfaces anywhere are
       the top nav bar, the footer strip, and the Home hero's booth frame — all a warm brown-black
       (`#221A14`). If any full page still renders dark, the color-mode class removal or the `:root`
       token swap did not take. The overall balance should read roughly 60% paper, 30% ink chrome,
       10% accent.
    2. **Nav and footer.** The wordmark "PINCHPOP" at top-left is coral (`#FF5A3C`) and renders in
       **Space Grotesk bold** — visibly the same geometric display face as the hero headline, not the
       monospace nav-link style (this was an open question between `DESIGN.md` and the UI spec; it is
       now resolved in favor of Space Grotesk — confirm it shipped that way, this is not a decision to
       make here). Nav links themselves stay warm paper, uppercase, monospace, on the dark bar. Click
       through Home / Play / Gallery / Profile and confirm the active link carries a 2px coral
       underline that moves as you navigate. Tab through the nav with the keyboard — every focused link
       (and the wordmark) shows a 2px coral ring. The footer is a single dark strip reading "PinchPop"
       in dimmed monospace uppercase. There must be no "built on PuzzleCam by Unnati-23" text anywhere.
    3. **The Home headline actually dominates.** On `/`, "Capture. Solve. Remember." renders in Space
       Grotesk — a geometric sans with squared terminals and a tall x-height, visibly *not* Inter and
       visibly not a generic system font — and it wraps onto two or three lines as a solid slab rather
       than sitting on one thin line. It should be unambiguously the largest, most dominant thing on
       the first screen. If it reads as a small caption floating in whitespace, that is a defect.
    4. **Hero decoration reads as deliberate, not noise.** Behind and around the headline: a faint
       dotted halftone texture; two or three large soft blurred blobs (pink, violet, blue) sitting
       above and below the text band, not behind the paragraph; a large faint concentric-circle
       aperture ring centered behind the headline (it should be big — comparable to or larger than the
       headline block, not a small stray circle); and four blue L-shaped corner brackets that clearly
       frame the headline-plus-CTA block as one cohesive viewfinder rectangle. Confirm none of these
       degrade the legibility of the grey premise paragraph.
    5. **The hero visual is a real object.** Below the CTA sits a wide dark-framed panel — an arcade
       bezel with a warm paper "screen" inside it, roughly 300px tall on mobile and 420px on desktop,
       with a small light slot along its bottom edge like a print chute. Inside the paper screen are
       three white cards at three clearly different sizes, tilted at different angles, overlapping,
       with the largest in the centre carrying a soft pink-violet-blue gradient wash. This must read as
       a substantial photobooth object anchoring the page — if it looks like a few faint decorative
       shapes, or if the panel is short and empty, flag it.
    6. **The booth carries no HUD, and the cards are NOT Polaroids.** Two specific defects to look for
       inside that panel: (a) any status dot, "REC"/"READY"/"LIVE" label, frame counter, timestamp,
       percentage, or monospace readout — there must be none, because Phase 1 has nothing live to
       report; and (b) any card styled as an instant-film frame — a white print border, a thicker
       margin at the bottom, a caption strip, or a piece of tape. The cards must be plain flat
       landscape rounded rectangles. Either defect is the specific kind this revision exists to
       prevent.
    7. **The CTA feels like a shutter.** "Start Playing" is coral-filled with dark (not white) text
       and a comfortably large tap area. Hover it — it grows very slightly and gains a soft coral
       glow. Press and hold — it compresses slightly. Click it: the URL becomes `/game`.
    8. **"How PinchPop Works" is a designed section, not three generic cards.** Scroll down. The
       heading sits left-aligned (not centered) with no small label above it. Below it are exactly
       three white cards, and on desktop they are **visibly staggered vertically** — the second sits
       lower than the first, the third lower again. Each card pairs a dark icon on the left with a
       small circled monospace numeral (01 / 02 / 03) on the right, then a Space Grotesk title
       ("Frame," "Pinch to Capture," "Solve & Save") and a sentence of body copy. The cards are flat —
       no hover lift, no rotate, no shadow change on hover. On mobile they stack to one column and the
       stagger disappears.
    9. **Nothing fake fills the page.** Between the how-it-works band and the bottom of the page there
       must be **no** photo strip, no gallery preview, no "N photos solved today" counter, no score,
       no testimonial, no logo wall, and no scrolling activity ticker. Phase 1 has no real photos and
       no real users, and inventing any of it is a defect, not a nicety. The page should feel complete
       without them.
    10. **The closing band is a quiet reprise.** Just above the dark footer there is a thin hairline
        rule, then a centered line reading "Capture. Solve. Remember." at heading size, then a second
        "Start Playing" button identical to the first. This repetition of the headline is deliberate —
        it is the only line of copy the design contract permits in this band. Confirm there is no
        third button anywhere on the page, and no second *different* action (no "watch demo," no
        "learn more") beside either CTA.
    11. **Stub pages carry no decorative color.** Visit `/game`, `/results`, `/gallery`, `/profile`,
        and `/share/anything`. Each shows a flat white card on the warm paper background with a soft
        warm shadow, a thin warm border, and **a single dark icon above the heading**. Every icon must
        be the same dark ink color — if any icon is pink, blue, violet, lime, or sits inside a colored
        square chip, that is a defect (the per-route color pattern was deliberately retired). The cards
        should **not** lift or tilt when you hover them — they are not clickable, so they should not
        behave as if they are. Confirm the heading and body copy on each page are word-for-word what
        they were before this change.
    12. **`/game` alone has the puzzle grid.** On `/game` only, a very faint dark 3x3 grid outline sits
        behind the card. It must not be tinted any color, and it must not appear on any other route —
        including inside the Home booth panel.
    13. **404 stays calm — but still responds to press.** Visit `/does-not-exist`. You get
        "Page not found." with a deep ruby (`#C81E3A`) accent — noticeably cooler and more magenta than
        the coral CTA, so failure and action are never confusable — the same body copy, and a
        "Back to Home" button that is white-filled with a thin dark border, *not* coral. Hover it: it
        grows very slightly, the same amount "Start Playing" does, but with **no** coral glow. Press
        and hold: it compresses, exactly like the primary CTA. There must be no blobs, brackets,
        halftone, aperture ring, booth panel, or card stack on this page. Clicking the button returns
        you to `/`.
    14. **Responsive behavior (SHELL-04).** Open DevTools' device toolbar. At **375px**: horizontal
        nav links are gone, replaced by a hamburger on the right that is comfortably tappable
        (44x44px); tapping opens a right-side drawer with Home/Play/Gallery/Profile stacked
        vertically; tapping a link both navigates and closes the drawer; Escape also closes it. The
        headline steps down to 34px and no text is clipped; the booth panel is ~300px tall and its
        three cards still fit inside it without spilling; the "How PinchPop Works" cards stack to a
        single column. At **768px**: the hamburger disappears, the horizontal link row returns, the
        headline steps up to 56px, the booth panel grows to ~420px, and the three step cards sit in one
        staggered row. At **1280px**: content stays centered within roughly 1200px rather than
        stretching edge to edge. At all three widths, confirm no horizontal scrollbar appears — the
        blurred blobs and the booth's rotated cards are the most likely causes if one does.
    15. **Typography and contrast.** Headings (including the "How PinchPop Works" title, the three step
        titles, and the closing reprise line) render in Space Grotesk bold; body paragraphs in Inter
        regular; button labels in Inter semibold (slightly heavier than body copy, clearly not bold
        Space Grotesk); nav links, the wordmark, the footer, and the circled step numerals in IBM Plex
        Mono uppercase with wide letter-spacing. Monospace must appear **only** in those
        nav/footer/label/step-numeral positions — never on a heading or a body paragraph. Nothing falls
        back to a generic system font (fonts are bundled locally, so this works offline). All body
        text is comfortably readable against paper and white; flag anything washed out, particularly
        the muted secondary paragraph text and the dimmed footer line.
    16. **Reduced motion.** If your OS has a "reduce motion" accessibility setting, enable it and
        reload. Both "Start Playing" buttons and "Back to Home" must stop scaling on hover and press
        entirely, with no residual transform. The step cards' vertical stagger is a static layout
        offset and correctly does **not** disappear — that is not a defect.

    Report anything that does not match. Most important to flag: any page still rendering dark; a hero
    headline that does not dominate; a booth panel that is short, empty, or carries a HUD readout; a
    booth card styled as a Polaroid; any fabricated photo, count, score, or testimonial anywhere; a
    "How PinchPop Works" band that reads as three interchangeable generic cards rather than a
    deliberately staggered sequence; a missing closing reprise band or a third button; a stub icon that
    is not plain dark ink; monospace text outside the nav/footer/label/step-numeral role; decorative
    elements degrading body-text legibility; a horizontal scrollbar at 375px; a stub or step card that
    lifts on hover; a 404 button that does not react to press the way the landing CTA does; or any copy
    string whose wording changed.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms all sixteen checks above pass in a real browser, or reports specific defects.</human-check>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint && npm run typecheck && npm run format:check && npm run build && echo GATES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Developer explicitly confirms checks 1 through 16 pass, or reports specific defects.
    - `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` all exit 0 at the moment of verification.
    - If defects are reported, they are fixed in the owning file, all four gates are re-run, and this checkpoint is re-presented before the plan is marked complete — this checkpoint is not auto-approvable and is never satisfied by a clean build alone.
  </acceptance_criteria>
  <resume-signal>Type "approved" to continue, or describe the issues found.</resume-signal>
  <done>The developer has confirmed in a live browser that the DESIGN.md / Revision 4 direction renders correctly — warm paper base with contained ink chrome, coral shutter accent, ruby danger, a Space Grotesk wordmark and headings with Inter body and mono labels, a dominant hero headline over a substantial HUD-free booth visual whose cards carry no Polaroid framing, a staggered three-step "How PinchPop Works" band, no fabricated proof content anywhere, a quiet closing CTA reprise, ink-only stub icons, an ink puzzle grid scoped to `/game`, intact responsive behavior at 375/768/1280px, and working reduced-motion handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL bar -> React Router | The only untrusted input in this phase: an arbitrary path, including the `:slug` dynamic segment |
| Browser DOM -> React component tree | Rendered pages; all copy is compile-time constant |
| npm registry -> local dependency tree | One new package (`@fontsource/space-grotesk`) enters the tree in Task 1 |
| `design-exploration/` -> `src/` | **No boundary crossing is permitted.** The Stitch concepts are read-only planning references; no file under `design-exploration/` is imported, linked, copied, or referenced from application code |

*(No auth, no forms, no network calls, no persistence — ASVS V2/V3/V4/V5/V6 remain not applicable per `01-RESEARCH.md` Security Domain.)*

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install @fontsource/space-grotesk` in Task 1 (ASVS V10) | mitigate | The package is absent from `01-RESEARCH.md`'s Package Legitimacy Audit table, so it is treated as unaudited. Run `python -m slopcheck install --ecosystem npm @fontsource/space-grotesk` before installing, per plan 01-02's established precedent and `01-RESEARCH.md`'s explicit `--ecosystem npm` caveat. `[SLOP]` blocks outright; `[ASSUMED]` or `[SUS]` escalates to a blocking, non-auto-approvable human checkpoint before the install proceeds. Version-pinned to `^5.3.0` to match the `@fontsource` major already audited `[OK]` in the tree. |
| T-01-14 | Tampering | Reflected XSS through the `:slug` URL segment in `SharePage` | mitigate | Unchanged from plan 01-05 and explicitly preserved: the slug is consumed only as a `data-share-slug` attribute value, which React escapes, and is never interpolated into HTML, a URL, or a script context. Task 3 asserts `dangerouslySetInnerHTML` is absent from all of `src/pages/`. |
| T-01-16 | Information Disclosure | Inline `style` attributes carrying the halftone `backgroundImage` gradient on `HomePage` (two instances after Task 4 — the section layer and the booth interior layer) | accept | Both values are the identical compile-time constant string with no user-controlled interpolation, so neither can become a CSS injection vector. React's `style` object API forbids raw declaration strings regardless. Tasks 4 and 5 additionally assert zero `url(` occurrences in the file, so no external resource can be pulled in through a background declaration. Revisit only if a motif value ever becomes dynamic. |
| T-01-17 | Denial of Service (usability) | Decorative blur/scale animations degrading low-power devices or triggering motion sensitivity | mitigate | Both primary CTAs carry `motion-reduce:` variants disabling transform and transition under `prefers-reduced-motion` (`DESIGN.md` section 19); the stub cards and the three How-It-Works step cards carry no motion at all (their `sm:translate-y-*` stagger is a static layout offset, not a transition); every decorative layer is `pointer-events-none` and `aria-hidden="true"` so none intercepts input or reaches assistive tech; the hero section and the booth interior are both `overflow-hidden` so blurred blobs and rotated cards cannot induce layout overflow. |
| T-01-18 | Information Disclosure (accessibility) | Decorative-only content announced to screen readers, or color used as a sole status carrier | mitigate | Every `data-motif` layer and every `data-card-stack-item` sits inside an `aria-hidden="true"` decorative wrapper; the step-card glyphs are `aria-hidden` because the adjacent title names the step, and the sequence itself is carried semantically by `<ol>`/`<li>` rather than by the visual numerals alone; the stub icons are `aria-hidden` because the adjacent heading already names the route; the hamburger keeps `aria-label="Open navigation menu"` and the drawer keeps its `sr-only` `SheetTitle`. Phase 1 has no live status of any kind, so `DESIGN.md` section 14's color-never-sole-indicator rule has nothing to apply to yet — recorded for Phase 2. |
| T-01-19 | Repudiation / Tampering (provenance) | Stitch concept markup or assets leaking from `design-exploration/` into shipped application code | mitigate | `design-exploration/` is planning reference only, with its own palette and its own fabricated content. Tasks 4 and 5 both assert `src/pages/HomePage.tsx` contains zero occurrences of the string `design-exploration`, zero `<img` elements, and zero `url(` references, so no Stitch asset can be linked or embedded; every element is written fresh against the `DESIGN.md`-approved tokens. `DESIGN.md` itself is read-only in this plan and is not modified. |
| T-01-13 | Repudiation | Attribution text state | mitigate | Commit `9e84385` removed the PuzzleCam/Unnati attribution as a settled decision. Task 2 asserts `grep -rc 'PuzzleCam' src/` returns 0, preventing accidental reintroduction from the stale `01-PATTERNS.md` guidance. |
| T-01-01 | Elevation of Privilege (future risk) | Catch-all `*` route paired with `vercel.json`'s unconditional rewrite | accept | Carried forward unchanged from plans 01-03 and 01-05, per D-03. No API routes exist under `/api` in this milestone, so nothing is shadowed. Untouched by this visual pass. |
| T-01-15 | Information Disclosure | Vite dev server exposure during the Task 6 human checkpoint | accept | `npm run dev` binds to localhost by default and the checkpoint does not pass `--host`. No secrets exist in the app at this stage regardless. |
</threat_model>

<verification>
1. `npm run lint` — exits 0
2. `npm run typecheck` — exits 0
3. `npm run format:check` — exits 0
4. `npm run build` — exits 0, emits `dist/index.html`
5. `grep -in '0a0a08\|f5c518\|e0533d\|eae5d6\|e3e0d8\|6b665c\|FFF8EF\|FF5A47\|1D1626\|5B5548\|F0E4D3' src/index.css` — 0 matches; no retired hex survives in any declaration or comment
6. `grep -rEni 'signal|coral|cream|ruby|ink-muted|card-border|paper-warm|viewfinder-blue|iconChipClassName|iconColor|polaroid' src/components/*.tsx src/pages/` — 0 matches; every retired token name, prop, and pattern is gone from hand-written source
7. `grep -rEn 'font-medium|font-light|font-thin|font-extralight|font-extrabold|font-black' src/components/*.tsx src/pages/` — 0 matches; only weights 400, 600, and 700 are used outside generated shadcn files
8. `grep -rEn 'flash-pink|lens-blue|darkroom-violet|flash-lime' src/components/*.tsx src/pages/GamePage.tsx src/pages/ResultsPage.tsx src/pages/GalleryPage.tsx src/pages/ProfilePage.tsx src/pages/SharePage.tsx src/pages/NotFoundPage.tsx` — 0 matches; the Landing Gradient Wash never leaves `HomePage.tsx`
9. `grep -c 'class="dark"' index.html` — 0 matches
10. `grep -cE '^\.dark[[:space:]]*\{' src/index.css` — 0 matches, while `@custom-variant dark` is still present
11. `grep -rc 'PuzzleCam' src/` — 0 matches
12. `grep -rc 'AppShell' src/pages/` — 0 matches (D-04 intact)
13. `grep -rc 'react-router/dom' src/` — 0 matches (`01-RESEARCH.md` Pitfall 3 intact)
14. `grep -rc 'design-exploration' src/` — 0 matches; no Stitch reference reaches application code
15. `grep -rc '<img' src/pages/HomePage.tsx` — 0 matches; Home depends on no photography (Revision 4 item 7, `DESIGN.md` section 10)
16. `git diff --quiet src/router.tsx` — exits 0; the route tree is untouched by this plan
17. `git status --porcelain DESIGN.md` — empty; `DESIGN.md` is read-only in this plan and must end it with a zero diff
18. Human checkpoint (Task 6) approved by the developer
</verification>

<success_criteria>
- Every `DESIGN.md` section 3 color token exists in `src/index.css` with its exact hex, and no retired token name or hex from either superseded revision survives anywhere in the file, comments included.
- The `01-UI-SPEC.md` Revision 4 audit was performed: its Color and Typography tables were compared against `DESIGN.md` §3/§4 and confirmed unchanged from Revision 3 (or the discrepancy was reported rather than silently resolved).
- Light/paper is the actual default rendered state: no color-mode class on `<html>`, `--background` is `#FBF1E4`, the dead dark rule block is deleted, and `@custom-variant dark` is retained so the generated shadcn components' `dark:` variants stay inert instead of firing on OS dark mode.
- Space Grotesk 700 is installed via `@fontsource` (slopcheck `[OK]`), imported in `src/main.tsx`, and wired to `--font-heading`; Inter 400 **and** 600 and IBM Plex Mono 400 remain imported; no variable font is present.
- The warm paper page surface is live across all seven routes, with `ink` appearing only as contained chrome — the nav bar, the footer, and the Home hero's booth bezel — and never as a page background.
- Typography obeys the three-weight ceiling: Space Grotesk 700 for Display/Heading at the 56/34 and 30/22 scales, Inter 400 for body and 600 for button labels, IBM Plex Mono 400 confined to nav, wordmark, footer, and the three step numerals.
- Home renders Revision 4's full eight-item composition arc: hero (items 1–5) → "How PinchPop Works" (item 6) → deliberately nothing where proof content would sit (item 7) → closing CTA reprise (item 8) → `AppShell` footer.
- The Home hero's supporting visual is a substantial object: an `ink` bezel at least 300px/420px tall wrapping a `paper` interior that holds three flat, graduated, staggered `paper-raised` print cards, with no Polaroid framing, no HUD readout, and no `shutter`-colored element.
- The "How PinchPop Works" band is a designed sequence — left-aligned heading, no eyebrow label, semantic `<ol>`/`<li>`, ink glyphs, ring-marked mono numerals, and a visible `sm:translate-y` stagger — not three interchangeable cards.
- Home contains exactly one primary action, offered exactly twice: two `Start Playing` buttons targeting `/game`, two shutter glows, and zero buttons or links invoking any other action.
- Home contains zero fabricated proof content: no `<img>`, no `url(`, no `design-exploration` reference, no photo, count, score, testimonial, or activity feed.
- No Landing Gradient Wash color appears on any route other than `/`; the five stub routes carry ink-only icons with no decorative color, no icon chips, and no hover transform; `/game` alone carries the ink-toned puzzle-grid motif.
- The 404 uses the ruby `danger` accent, matches the primary CTA's scale-on-press feedback, and carries no glow and no decorative motifs.
- All accessibility requirements survive: `aria-label="Open navigation menu"`, the 44x44px hamburger and nav-link targets, `sr-only` `SheetTitle`, 2px shutter `focus-visible` rings on every interactive element, `aria-hidden` plus `pointer-events-none` on every decorative layer, semantic `<ol>`/`<li>` for the step sequence, and `motion-reduce:` variants on every animated element.
- Every locked Copywriting Contract string is byte-identical to its pre-plan value; no heading, body, or CTA text was reworded, and the closing band introduces no new copy string.
- The seven routes, the `createBrowserRouter` route tree, the `AppShell` nested-layout architecture, and all component boundaries are unchanged — `src/router.tsx` has a zero diff, `DESIGN.md` has a zero diff, and nothing under `src/components/ui/` was edited.
- All four quality gates pass, and the developer has approved the Task 6 visual checkpoint.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-deployment-pipeline/01-08-SUMMARY.md` when done.

Record in it: the slopcheck verdict for `@fontsource/space-grotesk`; the result of Task 1's
`01-UI-SPEC.md` Revision 4 Color/Typography audit; the developer's verdict on the Task 6 checks,
especially check 3 (headline dominance), check 5 (booth panel weight), check 8 (the staggered
How-It-Works band), and check 10 (the closing reprise); and any defect-fix round trips through the
checkpoint.
</output>
