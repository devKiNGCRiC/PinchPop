---
phase: 01-app-shell-deployment-pipeline
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - package.json
  - package-lock.json
  - src/index.css
  - components.json
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/badge.tsx
autonomous: true
requirements: [SHELL-04]

must_haves:
  truths:
    - "Tailwind v4 utility classes compile and apply in the Vite build with no tailwind.config.js and no PostCSS pipeline"
    - "The @ path alias resolves to src/ in both the TypeScript compiler and the Vite bundler"
    - "shadcn/ui is initialized and the six Phase 1 primitives exist under src/components/ui/"
    - "The PinchPop brand tokens (ink #0a0a08, signal #f5c518, danger #e0533d, paper family) are available as Tailwind theme variables, replacing shadcn's default neutral palette"
    - "IBM Plex Mono and Inter are self-hosted via @fontsource and bound to font-mono / font-sans theme variables"
    - "Implements CONTEXT.md decisions D-09, D-13, D-14, D-15"
  artifacts:
    - path: "vite.config.ts"
      provides: "React + Tailwind v4 plugins and the @ -> ./src resolve alias"
      contains: "@tailwindcss/vite"
    - path: "src/index.css"
      provides: "Tailwind v4 entry, shadcn CSS variable layer, and PinchPop brand token overrides"
      contains: "#f5c518"
    - path: "components.json"
      provides: "shadcn configuration (new-york style, neutral base, css variables enabled)"
      contains: "new-york"
    - path: "src/lib/utils.ts"
      provides: "shadcn cn() helper composing clsx + tailwind-merge"
      exports: ["cn"]
    - path: "src/components/ui/sheet.tsx"
      provides: "Radix-backed Sheet primitive for the mobile nav drawer (SHELL-04)"
    - path: "src/components/ui/button.tsx"
      provides: "Button primitive for the home CTA and nav trigger"
  key_links:
    - from: "vite.config.ts"
      to: "src/"
      via: "resolve.alias mapping @ to path.resolve(__dirname, './src')"
      pattern: "path.resolve"
    - from: "tsconfig.app.json"
      to: "src/"
      via: "compilerOptions.paths mapping @/* to ./src/*"
      pattern: "@/\\*"
    - from: "src/index.css"
      to: "tailwindcss"
      via: "@import \"tailwindcss\" (v4 CSS-first entry, not @tailwind directives)"
      pattern: "@import \"tailwindcss\""
    - from: "src/main.tsx"
      to: "@fontsource packages"
      via: "weight-specific CSS imports"
      pattern: "@fontsource/"
---

<objective>
Wire Tailwind v4 into the Vite build, add the `@` path alias, initialize shadcn/ui, generate the six
Phase 1 primitives, and replace shadcn's default neutral palette with PinchPop's brand tokens.

Purpose: 01-RESEARCH.md Pitfall 1 documents a hard ordering dependency — `npx shadcn@latest init`
inspects the project for `@tailwindcss/vite` plus an `@import "tailwindcss";` CSS entry to decide
whether to emit a v4 or a legacy v3 setup. Getting this order wrong silently produces a
`tailwind.config.js`-based scaffold incompatible with the CSS-variable token model that 01-UI-SPEC.md
and every downstream component assume.

Output: Tailwind-v4-wired `vite.config.ts` and tsconfigs, a brand-themed `src/index.css`,
`components.json`, `src/lib/utils.ts`, and `src/components/ui/{button,card,sheet,separator,skeleton,badge}.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md
@.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md
@.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md
@.planning/phases/01-app-shell-deployment-pipeline/01-01-SUMMARY.md

**Locked decisions implemented by this plan:** D-09 (`src/components/ui/` for shadcn primitives),
D-13 (only Tailwind + shadcn deps + `@types/node` added here — still no zustand/mediapipe/supabase),
D-14 (npm), D-15 (`@fontsource` self-hosted fonts, already installed in plan 01-01).

**Hard ordering constraint (01-RESEARCH.md Pitfall 1, 01-PATTERNS.md "Config-Before-Init Ordering"):**
Task 1 (Tailwind wiring + aliases) MUST complete before Task 2 (`shadcn init` + `shadcn add`), and
Task 2 MUST complete before Task 3 (brand token overlay), because `shadcn init` rewrites
`src/index.css` with its own CSS-variable layer.

**Brand token values (01-UI-SPEC.md Color table, confirmed identical to the legacy prototype's
`:root` custom properties in `legacy/css/styles.css`):** ink `#0a0a08`, signal `#f5c518`, danger
`#e0533d`, paper-white `#ffffff`, paper-warm `#eae5d6`, paper-border `#e3e0d8`, ink-soft `#6b665c`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Tailwind v4 and the @ path alias into Vite and TypeScript</name>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, tsconfig.app.json, src/index.css</files>
  <read_first>
    - `vite.config.ts` (root — the Vite-scaffolded config that must gain the Tailwind plugin and alias)
    - `tsconfig.json` and `tsconfig.app.json` (root — must both gain `baseUrl` and `paths`)
    - `src/index.css` (root — scaffolded template CSS, to be replaced by the Tailwind v4 entry)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md` — Code Examples: `vite.config.ts`, `src/index.css`, `tsconfig.json`; Pitfall 1; State of the Art table (v4 replaces `@tailwind` directives)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md` — "Pattern Assignments" for `vite.config.ts` and `tsconfig.json` (verbatim-copy patterns)
  </read_first>
  <action>
    Install `tailwindcss` and `@tailwindcss/vite` as dependencies and `@types/node` as a dev
    dependency (`@types/node` is required for `path`/`__dirname` usage in `vite.config.ts`, per
    01-RESEARCH.md's Supporting stack table). Do not install `postcss`, `autoprefixer`, or
    `tailwindcss-animate` — Tailwind v4's Vite plugin replaces the PostCSS pipeline entirely, and
    `tailwindcss-animate` is deprecated in favor of `tw-animate-css`, which shadcn pulls in itself.

    Rewrite `vite.config.ts` to match 01-PATTERNS.md's verbatim pattern for this file: default-import
    `path` from `"path"`, `tailwindcss` from `"@tailwindcss/vite"`, `react` from
    `"@vitejs/plugin-react"`, and `defineConfig` from `"vite"`; export a `defineConfig` call whose
    `plugins` array is `[react(), tailwindcss()]` and whose `resolve.alias` maps the key `"@"` to
    `path.resolve(__dirname, "./src")`.

    Add to `tsconfig.json` a `compilerOptions` block containing `"baseUrl": "."` and
    `"paths": { "@/*": ["./src/*"] }`, keeping the existing `files` and `references` entries intact.
    Add the identical `baseUrl` + `paths` pair to `tsconfig.app.json`'s existing `compilerOptions`
    — 01-RESEARCH.md notes both files need it, and omitting `tsconfig.app.json` is what makes
    `npm run typecheck` fail on `@/` imports even when the editor resolves them. Do not change any
    strictness flags (D-10).

    Replace the entire contents of `src/index.css` with the single Tailwind v4 entry line
    `@import "tailwindcss";` — no `@tailwind base/components/utilities` directives (removed in v4),
    no leftover Vite template CSS. Brand theme tokens are deliberately NOT added yet; Task 2's
    `shadcn init` rewrites this file, and Task 3 layers the tokens on top of shadcn's output.

    Delete `src/App.css` and remove its import from `src/App.tsx` if the scaffold left one — the
    template's demo CSS conflicts with the Tailwind reset and is dead weight. `src/App.tsx` itself is
    removed later in plan 01-05 when `src/router.tsx` takes over; leave it compiling for now.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && grep -v '^\s*//' vite.config.ts | grep -c '@tailwindcss/vite' && node -e "const fs=require('fs');const strip=s=>s.replace(/^\s*\/\/.*$/gm,'');for(const f of ['tsconfig.json','tsconfig.app.json']){const j=JSON.parse(strip(fs.readFileSync(f,'utf8')));if(!j.compilerOptions||!j.compilerOptions.paths||!j.compilerOptions.paths['@/*'])throw new Error('missing @/* paths in '+f);}console.log('ALIAS_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `npm run typecheck` exits 0.
    - `grep -v '^\s*//' vite.config.ts | grep -c 'tailwindcss()'` returns at least 1.
    - `grep -v '^\s*//' vite.config.ts | grep -c 'path.resolve(__dirname, "./src")'` returns 1.
    - Both `tsconfig.json` and `tsconfig.app.json` parse with `compilerOptions.paths["@/*"]` equal to `["./src/*"]` and `compilerOptions.baseUrl` equal to `"."`.
    - `src/index.css` contains the line `@import "tailwindcss";` and does NOT contain the string `@tailwind ` (v3 directive form).
    - No `tailwind.config.js` or `tailwind.config.ts` exists at the repo root (`test ! -e tailwind.config.js && test ! -e tailwind.config.ts`).
    - `node -p "Object.keys({...require('./package.json').dependencies,...require('./package.json').devDependencies}).filter(k=>['tailwindcss','@tailwindcss/vite','@types/node'].includes(k)).length"` prints `3`.
    - `src/App.css` no longer exists and `grep -c 'App.css' src/App.tsx` returns 0.
  </acceptance_criteria>
  <done>Tailwind v4 compiles through the Vite plugin, `@/` resolves in both tsc and Vite, and no v3-era config artifacts exist — the exact preconditions `shadcn init` inspects.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and generate the six Phase 1 primitives</name>
  <files>components.json, src/lib/utils.ts, src/index.css, package.json, package-lock.json, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/sheet.tsx, src/components/ui/separator.tsx, src/components/ui/skeleton.tsx, src/components/ui/badge.tsx</files>
  <read_first>
    - `vite.config.ts` and `tsconfig.app.json` (root — confirm Task 1's Tailwind plugin and `@/*` alias landed; `shadcn init` fails or misdetects without them)
    - `src/index.css` (root — the Tailwind v4 entry that `shadcn init` will rewrite)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` — "Design System" table (style `new-york`, base color `neutral`, CSS variables yes, dark mode default) and "Component Inventory" (the exact six components and why `skeleton`/`badge` are installed now)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-RESEARCH.md` — Installation block, Pitfall 1 warning signs, and the Supporting stack table naming `tw-animate-css` (not `tailwindcss-animate`)
  </read_first>
  <action>
    Run `npx shadcn@latest init` from the repo root. Answer its prompts with the 01-UI-SPEC.md
    Design System preset: style `new-york`, base color `neutral`, CSS variables `yes`. If the CLI
    supports non-interactive flags in the installed version, prefer them
    (`--style new-york --base-color neutral --yes`) so the run is reproducible; otherwise answer the
    prompts with those exact values.

    Before accepting the result, check Pitfall 1's warning signs from 01-RESEARCH.md: the generated
    `components.json` must NOT contain a `tailwind.config` path pointing at a nonexistent
    `tailwind.config.js`, and the CLI must not have reported "Tailwind CSS not detected." If either
    occurs, stop — that means Task 1's wiring did not land correctly; fix `vite.config.ts` /
    `src/index.css` and re-run init rather than proceeding.

    Confirm init produced `components.json`, `src/lib/utils.ts` exporting a `cn` helper composing
    `clsx` and `tailwind-merge`, and a rewritten `src/index.css` containing shadcn's CSS-variable
    layer (`:root` and `.dark` blocks) plus a `@theme inline` mapping. Confirm the transitive deps
    `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, and `tw-animate-css` are
    now in `package.json`. If `tailwindcss-animate` appears instead of `tw-animate-css`, that is the
    deprecated v3 plugin flagged in 01-RESEARCH.md's State of the Art table — remove it and install
    `tw-animate-css`.

    Then generate exactly the six primitives from 01-UI-SPEC.md's Component Inventory with
    `npx shadcn@latest add button card sheet separator skeleton badge`. `skeleton` and `badge` are
    intentionally installed now even though Phase 1's UI does not consume them — 01-UI-SPEC.md
    reserves them so Phase 2 and Phase 7 do not have to re-run the CLI.

    Leave every file under `src/components/ui/` exactly as generated — do not reformat, re-lint, or
    hand-edit them. Plan 01-01 already added `src/components/ui` to the ESLint and Prettier ignore
    lists for this reason. Brand color customization happens in Task 3 through `src/index.css`
    variables, never by editing the generated component source.
  </action>
  <verify>
    <automated>test -f components.json && test -f src/lib/utils.ts && for c in button card sheet separator skeleton badge; do test -f "src/components/ui/$c.tsx" || { echo "MISSING $c"; exit 1; }; done && node -e "const c=require('./components.json'); if(c.style!=='new-york')throw new Error('style: '+c.style); if(!c.tailwind||c.tailwind.baseColor!=='neutral')throw new Error('baseColor wrong'); if(c.tailwind.cssVariables!==true)throw new Error('cssVariables not true'); if(c.tailwind.config)throw new Error('v3-style tailwind.config path present: '+c.tailwind.config); console.log('SHADCN_OK')" && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists with `style` = `new-york`, `tailwind.baseColor` = `neutral`, `tailwind.cssVariables` = `true`, and an empty-or-absent `tailwind.config` value (v4 shape, not v3).
    - All six files exist: `src/components/ui/button.tsx`, `card.tsx`, `sheet.tsx`, `separator.tsx`, `skeleton.tsx`, `badge.tsx`.
    - `src/lib/utils.ts` exports `cn` (`grep -c 'export function cn' src/lib/utils.ts` returns 1) and imports from both `clsx` and `tailwind-merge`.
    - `node -p "Object.keys({...require('./package.json').dependencies,...require('./package.json').devDependencies}).filter(k=>['class-variance-authority','clsx','tailwind-merge','lucide-react','tw-animate-css'].includes(k)).length"` prints `5`.
    - `node -p "!!{...require('./package.json').dependencies,...require('./package.json').devDependencies}['tailwindcss-animate']"` prints `false` (deprecated v3 plugin absent).
    - `src/index.css` now contains a `:root` block and a `.dark` block with shadcn CSS variables (`grep -c -- '--background' src/index.css` returns at least 1).
    - `npm run typecheck` and `npm run build` both exit 0.
  </acceptance_criteria>
  <done>shadcn/ui is initialized against the Tailwind v4 setup, the six Phase 1 primitives exist under `src/components/ui/`, and the project still typechecks and builds.</done>
</task>

<task type="auto">
  <name>Task 3: Overlay PinchPop brand tokens onto the shadcn theme and bind the self-hosted fonts</name>
  <files>src/index.css, src/main.tsx</files>
  <read_first>
    - `src/index.css` (root — shadcn's generated CSS-variable layer produced by Task 2; this is the file being extended)
    - `src/main.tsx` (root — Vite entry that must gain the `@fontsource` weight imports)
    - `legacy/css/styles.css` (lines 1-16 — the legacy `:root` custom properties that are the canonical source of the ink/paper/signal/danger hex values)
    - `.planning/phases/01-app-shell-deployment-pipeline/01-UI-SPEC.md` — Color table, Typography table (exactly two weights: 400 and 600), Spacing Scale, and Implementation Notes ("extend the generated Tailwind theme's CSS variables with the token values declared in the Color section... rather than shadcn's default neutral palette")
    - `.planning/phases/01-app-shell-deployment-pipeline/01-PATTERNS.md` — "Design Token Carry-Over from Legacy Prototype" and the `src/main.tsx` `@fontsource` pattern
  </read_first>
  <action>
    Extend `src/index.css` (do not replace shadcn's generated structure) so the app renders in
    PinchPop's brand identity instead of shadcn's default neutral palette, per 01-UI-SPEC.md
    Implementation Notes.

    Add a `@theme` block declaring the brand palette as named Tailwind theme colors using the exact
    hex values shared by 01-UI-SPEC.md's Color table and `legacy/css/styles.css`'s `:root`:
    `--color-ink: #0a0a08`, `--color-signal: #f5c518`, `--color-danger: #e0533d`,
    `--color-paper: #ffffff`, `--color-paper-warm: #eae5d6`, `--color-paper-border: #e3e0d8`,
    `--color-ink-soft: #6b665c`. Do not re-derive, lighten, or approximate these values.

    In the same `@theme` block, declare the two font stacks so Tailwind's `font-sans` and
    `font-mono` utilities resolve to the self-hosted families: `--font-sans` starting with
    `Inter` then the legacy fallback chain (`-apple-system`, `BlinkMacSystemFont`, `Segoe UI`,
    `sans-serif`), and `--font-mono` starting with `IBM Plex Mono` then
    (`SFMono-Regular`, `Menlo`, `Consolas`, `monospace`) — the same fallback chains
    `legacy/css/styles.css` already declares.

    Override shadcn's semantic CSS variables so its primitives inherit the brand rather than
    neutral gray. In the `:root`/`.dark` variable blocks shadcn generated, map at minimum:
    `--background` to the ink value, `--foreground` to the warm paper value, `--primary` to signal
    with `--primary-foreground` set to ink (signal is a light amber — dark text on it is the legible
    pairing), `--destructive` to danger, `--card` to paper-white with `--card-foreground` set to ink,
    `--border` to the paper-border value, `--muted-foreground` to the ink-soft value, and `--ring` to
    signal (01-UI-SPEC.md reserves signal for the focus-visible ring). Keep whatever variable format
    shadcn generated (raw hex vs `oklch()`) consistent within the file — convert the brand hex values
    into that format rather than mixing notations.

    Per 01-UI-SPEC.md the app defaults to dark mode: ensure the dark theme is the default rendering
    by applying shadcn's `dark` class on the root `<html>` element in `index.html` at the repo root
    (add `class="dark"` to the `<html>` tag) rather than toggling it at runtime — there is no theme
    switcher in this phase.

    Add the `@fontsource` weight imports at the top of `src/main.tsx`, before the app imports:
    `@fontsource/inter/400.css`, `@fontsource/inter/600.css`, and
    `@fontsource/ibm-plex-mono/400.css`. Import exactly these three and no others — 01-UI-SPEC.md's
    Typography section declares exactly two weights (400, 600) and states "do not introduce 500/700
    anywhere in this phase", and IBM Plex Mono is only used for the 400-weight Label role.

    Do NOT `@import` or otherwise reference `legacy/css/styles.css` from the new app — 01-UI-SPEC.md
    Implementation Notes scope that file to the Phase 2 game-screen port. Only the token values carry
    forward.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && node -e "const fs=require('fs');const css=fs.readFileSync('src/index.css','utf8');['#0a0a08','#f5c518','#e0533d','#eae5d6'].forEach(h=>{if(!css.toLowerCase().includes(h))throw new Error('missing token '+h)});if(css.includes('legacy/css/styles.css'))throw new Error('legacy stylesheet must not be imported');const m=fs.readFileSync('src/main.tsx','utf8');['@fontsource/inter/400.css','@fontsource/inter/600.css','@fontsource/ibm-plex-mono/400.css'].forEach(i=>{if(!m.includes(i))throw new Error('missing font import '+i)});['/500.css','/700.css','ibm-plex-mono/600.css'].forEach(b=>{if(m.includes(b))throw new Error('forbidden font weight '+b)});console.log('TOKENS_OK')" && grep -c 'class="dark"' index.html</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contains all four brand hex values `#0a0a08`, `#f5c518`, `#e0533d`, `#eae5d6` (case-insensitive), plus `#ffffff`, `#e3e0d8`, and `#6b665c`.
    - `src/index.css` declares `--font-sans` containing `Inter` and `--font-mono` containing `IBM Plex Mono`.
    - `src/index.css` does NOT contain the string `legacy/css/styles.css`.
    - `src/main.tsx` imports exactly `@fontsource/inter/400.css`, `@fontsource/inter/600.css`, `@fontsource/ibm-plex-mono/400.css` and contains no `/500.css`, `/700.css`, or `ibm-plex-mono/600.css` import.
    - `grep -c 'class="dark"' index.html` returns 1.
    - `npm run build` exits 0 and the emitted `dist/assets/*.css` contains `#f5c518` or its `oklch` equivalent (`grep -rl 'f5c518\|0.86 0.16 90' dist/assets` finds at least one file; if shadcn emitted oklch, assert the oklch form instead).
    - `npm run lint`, `npm run typecheck`, and `npm run format:check` all exit 0.
  </acceptance_criteria>
  <done>Tailwind's theme exposes PinchPop's brand tokens, shadcn primitives inherit them via semantic CSS variables, dark mode is the default, and both self-hosted font families are bundled at exactly the two declared weights.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | `shadcn init`/`add` install transitive runtime dependencies and write executable component source into the repo |
| shadcn CLI → repo working tree | The CLI writes files (`components.json`, `src/lib/utils.ts`, `src/components/ui/*.tsx`) that ship in the production bundle |

*(No client→API, no user input, no auth, no persistence exist in this phase — ASVS V2/V3/V4/V5/V6 remain not applicable, per 01-RESEARCH.md "Security Domain".)*

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` of `tailwindcss`, `@tailwindcss/vite`, `@types/node`, plus shadcn's transitive `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `tw-animate-css` (ASVS V10) | mitigate | Every one of these names appears with a slopcheck `[OK]` verdict in 01-RESEARCH.md's Package Legitimacy Audit under `--ecosystem npm`. Task 2's verify asserts the exact expected transitive set landed. If `shadcn add` pulls in any package NOT in that audit table, it must be re-audited with `slopcheck install --ecosystem npm <pkg>` and, if `[ASSUMED]`/`[SUS]`, escalated to a blocking human checkpoint before the plan is marked complete. 01-RESEARCH.md Assumption A1 explicitly flags that the transitive list was inferred rather than read from shadcn's own manifest, so an unexpected package is a real possibility here, not a theoretical one. |
| T-01-05 | Tampering | Deprecated `tailwindcss-animate` (v3 plugin) installed instead of `tw-animate-css` | mitigate | Task 2's action instructs removal-and-replace, and its acceptance criteria assert `tailwindcss-animate` is absent from the manifest. |
| T-01-06 | Tampering | `npx shadcn@latest` executing an unpinned remote CLI against the working tree | accept | shadcn is the officially published CLI for the chosen component system and is `[OK]` in the legitimacy audit; the alternative (hand-copying component source) removes the audited distribution channel without reducing risk. Its output is reviewed by the acceptance criteria before commit. |
| T-01-07 | Information Disclosure | Vite production build emitting source maps for the app bundle (ASVS V14) | accept | 01-RESEARCH.md's Security Domain section records that Vite's default production build already strips dev-only code and that default source-map behavior is acceptable for this project's IP posture; no requirement specifies otherwise. Revisit only if a later phase introduces proprietary client logic. |
</threat_model>

<verification>
1. `npm run lint` — exits 0
2. `npm run typecheck` — exits 0
3. `npm run format:check` — exits 0
4. `npm run build` — exits 0
5. `test -f components.json && test -f src/lib/utils.ts` and all six `src/components/ui/*.tsx` files exist
6. `test ! -e tailwind.config.js && test ! -e tailwind.config.ts` — confirms the v4 CSS-first setup, not a v3 fallback
7. `grep -c 'f5c518' src/index.css` (or the oklch equivalent) returns at least 1 — brand tokens present
</verification>

<success_criteria>
- Tailwind v4 compiles via `@tailwindcss/vite` with no `tailwind.config.*` and no PostCSS config.
- `@/` resolves to `src/` in `vite.config.ts`, `tsconfig.json`, and `tsconfig.app.json`.
- `components.json` reflects the 01-UI-SPEC.md preset (new-york / neutral / css variables) in v4 shape.
- The six Phase 1 shadcn primitives exist verbatim under `src/components/ui/` (D-09).
- `src/index.css` exposes PinchPop brand tokens and remaps shadcn's semantic variables to them; the legacy stylesheet is not imported.
- Dark mode is the default via `class="dark"` on the root `<html>`.
- Exactly three `@fontsource` weight imports exist in `src/main.tsx` (D-15, 01-UI-SPEC.md two-weight rule).
- No dependency outside D-13's approved scope was added.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-deployment-pipeline/01-02-SUMMARY.md` when done.
</output>
